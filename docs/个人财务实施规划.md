# 猫账个人财务实施规划

> 状态：当前实施、架构和任务唯一事实源
>
> 产品主线：`origin/main`
>
> Web 归档：`origin/catledger-web` / `web-v2-final`

## 0. 文档使用与当前看板

### 0.1 唯一事实源

- 根目录 `AGENTS.md`：长期协作、安全、分支、数据和验证硬约束；
- [个人财务用户需求.md](./个人财务用户需求.md)：产品定位、范围和体验；
- [个人财务业务规则与验收.md](./个人财务业务规则与验收.md)：会计、隔离、导入和隐私的对错标准；
- [个人财务架构设计.md](./个人财务架构设计.md)：小程序、云函数、MySQL 与存储边界；
- 本文：当前状态、冻结契约、任务与下一步；
- `现行说明/`：当前代码已具备行为；
- 代码、迁移与测试：已落地行为的最终证据。

### 0.2 仓库与产品边界

- `origin/main` 是微信小程序产品权威主线；
- `origin/catledger-web` 和 `web-v2-final` 完整保留迁移前 Web 版；
- 新产品不维护桌面端、PWA 或移动 Web，不从旧 Web 代码机械移植页面；
- 首发目标是中国用户、多用户隔离和完整批量账单闭环。

### 0.3 当前工程与生产基线

| 范围 | 当前事实 |
| --- | --- |
| 客户端 | 微信原生小程序，WXML/WXSS/JavaScript |
| 后端 | CloudBase 云函数：`catledger-api`、`catledger-import` |
| 数据库 | CloudBase MySQL，尚未建立首版 schema |
| 文件 | CloudBase 临时云存储，尚未接入 |
| 正式发布 | 尚未创建可审核的小程序版本 |
| Web 版 | 完整冻结于 `catledger-web` 与 `web-v2-final`；本轮不更新 8080/8082 |

### 0.4 状态定义

- `未开始`：未领取或依赖未满足；
- `进行中`：已有负责人、分支和工作区；
- `待验收`：候选已交付，等待复核或集成；
- `已完成`：已集成、验证、更新文档并安全清理；
- `阻塞`：存在已记录且无法在授权内解决的外部依赖或用户决策。

### 0.5 当前任务看板

| ID | 目标 | 依赖 | 状态 | 负责人 / 分支 | 验收与边界 |
| --- | --- | --- | --- | --- | --- |
| MINI-1901 | 固化 Web 并建立小程序干净基线 | 用户确认主线切换 | 进行中 | 当前主负责人 / `codex/mini-1901-foundation` | Web 分支与标签指向迁移前最终提交；`main` 通过普通提交移除旧运行代码；微信开发者工具可导入最小小程序；不改本地 Web 数据和运行容器 |
| MINI-1902 | 微信身份、用户初始化与默认分类 | MINI-1901 | 未开始 | 待领取 | 服务端可信身份；幂等初始化；首版 MySQL 迁移与用户隔离测试 |
| MINI-1903 | 账户、手动记账与交易列表 | MINI-1902 | 未开始 | 待领取 | 支出/收入/转账闭环；月份筛选；正式账本事务与统计 |
| MINI-1904 | 支付宝/微信单文件导入 | MINI-1903 | 未开始 | 待领取 | 上传、解析、证据、账户映射和安全入账闭环 |
| MINI-1905 | 多文件整理、去重与失败隔离 | MINI-1904 | 未开始 | 待领取 | 并发、幂等、冲突、整批原子入账和隐私门禁 |
| MINI-1906 | 财务总览、导出、注销与上线准备 | MINI-1905 | 未开始 | 待领取 | 统计口径、数据生命周期、隐私指引、真机与审核清单 |

### 0.6 当前主负责人

- 当前阶段：MINI-1901；
- 主负责人：当前主会话；
- 基线：Web 最终提交 `39a0ea55`；
- 分支/worktree：`codex/mini-1901-foundation` / `/Users/gaohongxiang/projects/catledger-mini-1901`；
- 文件所有权：根配置、`miniprogram/`、`cloudfunctions/`、`shared/`、`migrations/`、README、AGENTS 和活动文档；
- 不触碰主工作树中的用户未跟踪文件，不改本地 Web 容器、卷或 8080/8082 服务。

## 1. MINI-1901 冻结契约

1. 用普通提交删除旧 Web/Go 运行代码，保留连续 Git 历史，不使用 orphan 分支、reset 或 force push。
2. `catledger-web` 与 `web-v2-final` 必须先指向 `39a0ea55`，再改变 `main`。
3. 新基线只提供可导入的原生小程序外壳、可信微信身份的云函数入口和后续目录，不伪装数据库或账单导入已经完成。
4. 仓库默认不含真实 AppID、AppSecret、云环境 ID、数据库凭据或业务数据。
5. 旧 Web 代码需要参考时从归档分支按模块重写或移植，经新契约验收后进入 `main`，不整树合并。

## 19. 当前下一步

1. 完成 MINI-1901 的最小小程序、云函数目录和配置校验，并安全快进合入、推送 `origin/main`。
2. 使用微信开发者工具导入根目录；由用户配置自己的 AppID 和云开发环境，完成开发版预览。
3. 启动 MINI-1902，先冻结用户、分类和账户的首版 MySQL schema，再实现微信身份初始化。
4. 在 MINI-1902 之前不接入正式数据库、不上传真实账单、不承诺审核或正式发布完成。
