# 个人财务扩展架构与实施计划

> 状态：唯一实施基线与任务状态源
> 主项目：gaohongxiang/ezbookkeeping
> 基线提交：57f6a263
> 制定日期：2026-08-13
> 最后更新：2026-08-14
> 适用范围：持久导入、同来源去重、跨来源对账、贷款与可信财务总览

## 0. 文档使用、仓库边界与任务看板

### 0.1 唯一事实源

每个第一次进入项目的开发会话先完整阅读根目录 `AGENTS.md` 和本文；涉及产品或会计口径时再阅读 `个人财务需求.md`。

- `AGENTS.md` 保存长期协作和工程强约束；
- 需求文档保存产品目标、业务规则和验收口径；
- 本文保存技术架构、实施顺序、任务状态、负责人和完成证据。

任务看板默认由主负责人会话统一修改。执行会话读取本文、领取任务并回报“待验收”；只有主负责人在复核、合并、验证和安全清理后才能标记“已完成”。

### 0.2 仓库与参考边界

主仓库是当前 ezBookkeeping fork。所有产品代码、迁移、测试和长期文档都必须落在本仓库。

贷款算法参考仓库为 `/Users/gaohongxiang/projects/loan-cost-calculator`，只允许读取产品说明、领域算法和金标测试：

- 不修改、重置、清理或提交参考仓库；
- 不建立跨仓库运行时路径依赖；
- 需要的算法在本仓库重新实现或复制明确选择的部分，并建立本项目自己的测试；
- 当前产品是自托管 Web/PWA，不是微信小程序。

### 0.3 已确认的工程基线

- 后端：Go 1.26、Gin、XORM；
- 前端：Vue 3、TypeScript、Pinia、Vuetify/Framework7、Vite；
- 数据库：SQLite、MySQL、PostgreSQL；
- 规划起点：`main@e038e014`；当前工程基线：`57f6a263`；`origin/main` 是产品权威，`upstream` 只读；
- PF 用户功能尚未开放；长期规划、协作规则和工程基线已提交，v001 数据与解析契约正在阶段分支实现。

现有关键入口：

| 范围 | 关键位置 |
| --- | --- |
| 正式交易与账户 | `pkg/models/transaction.go`、`pkg/models/account.go`、`pkg/services/transactions.go` |
| 交易 API 与路由 | `pkg/api/transactions.go`、`cmd/webserver.go` |
| 现有导入 | `pkg/models/imported_transaction.go`、`pkg/converters`、`pkg/duplicatechecker` |
| 支付宝/微信解析 | `pkg/converters/alipay`、`pkg/converters/wechat` |
| 前端导入 | `src/views/desktop/transactions/import`、`src/core/import_transaction.ts` |
| 数据库启动 | `cmd/database.go` |

现有 `ClientSessionId` 只解决短期内存级重复提交；解析结果会进入普通交易批量创建，尚无原文件、批次、原始行、来源身份和证据关系。因此不得在旧 handler 中增加“同金额、同时间就跳过”的临时查询。

### 0.4 状态定义

- `未开始`：未分配或依赖未满足；
- `进行中`：已有负责人、分支和独立 worktree；
- `待验收`：任务分支已交付，等待复核、集成或完整门禁；
- `已完成`：已合入目标分支、通过规定验证、更新长期文档并完成安全清理；
- `阻塞`：存在已记录的外部依赖或用户决策。

### 0.5 总任务看板

| ID | 交付项 | 依赖 | 状态 | 负责人 / 分支 | 完成证据 |
| --- | --- | --- | --- | --- | --- |
| GOV-001 | 根级规则、需求与架构规划去重整合 | 无 | 已完成 | 主负责人 / `main` | `3d6d3dc4`，已推送 `origin/main` |
| BASE-001 | upstream、阶段分支、Go 工具链和三库基线 | GOV-001 | 已完成 | 主负责人 / `codex/pf-phase-1` | 前后端全门禁通过；`scripts/test-pf-db.sh all` 三库双次 Sync2 通过并自动清理 |
| SYNC-001 | 受控吸收 `upstream/main@fa6cda3b3d97` 的 15 个提交 | BASE-001 | 已完成 | 主负责人 / `main` | 合并提交 `57f6a263`；全门禁通过；临时分支/worktree 已清理；尚未推送 |
| DB-101 | PF 迁移框架、文件/来源账户/批次/原始行/来源身份模型 | BASE-001 | 已完成 | 主负责人 / `codex/pf-phase-1` | `c267346c`；v001、迁移租约/接管、三库精确验模、仓储 UID 隔离和导入契约均通过；全仓 `go test`、`go vet` 通过 |
| PARSE-101 | 支付宝证据解析器与金标夹具 | DB-101 契约冻结 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 执行提交 `6588bd7b`，集成提交 `63a795f0`；物理空行、定位与金标经复核，相关包、全仓及前端门禁通过；任务 worktree/分支已安全清理 |
| PARSE-102 | 微信证据解析器与金标夹具 | DB-101 契约冻结 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 执行提交 `e8c69aa7`，集成提交 `6a4bfda2`；UTF-8/UTF-16 严格解码和金标经复核，相关包、全仓及前端门禁通过；任务 worktree/分支已安全清理 |
| STORE-101 | 原文件存储补偿、上传和批次查询 API | DB-101 契约冻结 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 执行提交 `60a710ed`，集成提交 `f721f7f6`；补偿、UID 隔离、隐私、分页与 race 经复核，三库仓储契约及全门禁通过；任务 worktree/分支已安全清理 |
| DEDUP-101 | 版本化来源身份、精确重复、冲突态与并发测试 | DB-101 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 执行提交 `e4cd4dc8`，集成提交 `9c80e0bd`；精确重复/冲突/回滚/隐私及三库并发裁决通过；任务 worktree/分支已安全清理 |
| POST-101 | posting、证据关系、session 账本适配器和原子入账 | DEDUP-101 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 公共契约 `68267fed`，执行提交 `d45b7ff1`，集成提交 `b4d57849`；v002、原子入账/重放/复用/回滚、证据下钻、三库及全仓门禁通过；任务 worktree/分支已安全清理 |
| FLOW-101 | 原文件读取、解析器编排、来源账户映射与 reparse API | POST-101 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 公共契约 `fb49afab`，执行提交 `8e5e9012`，集成提交 `f1b14579`；聚焦测试、SQLite 纵切面与脱敏复核通过，任务 worktree/分支已安全清理 |
| UI-101 | 导入预览、历史、重复/冲突解释和证据下钻 | FLOW-101 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 公共契约 `da224137`、更正 `cb17709e`、执行 `551c105c`、集成 `a2cb4691`；聚焦状态测试、类型/lint 与生产构建通过，任务 worktree/分支已安全清理 |
| OPS-101 | 批次/原文件生命周期、用户清理与一致性运维 | UI-101 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 执行提交 `5155ae4f`、保护修正 `d66a0623`，集成提交 `db598600`、`536bc95b`；生命周期、清理、一致性、备份说明与聚焦验证通过，任务 worktree/分支已安全清理 |
| QA-101 | 三库、并发、隐私、迁移、恢复和上游同步门禁 | OPS-101 | 已完成 | 主负责人 / `codex/pf-phase-1-owner2` | 全仓 Go、前端和 `scripts/test-pf-db.sh all` 通过；三库隔离资源已清理；`upstream/main` 仍为 `fa6cda3b3d97`，阶段一完成证据见 0.17 节 |
| DEPLOY-101 | 本地产品主线、可追溯镜像与安全数据切换 | QA-101 | 已完成 | 主负责人 / `main` | 修复镜像 `ezbookkeeping-fork:2.0.0-pf1-37c27067` 已接管 8080；原账号与恢复数据验收通过，旧官方/开发/候选容器、旧镜像及空测试卷已清理，恢复卷与磁盘备份保留 |
| REC-201 | 阶段二：跨来源候选与人工对账 | 阶段 1 完成 | 已完成 | 主负责人 / `codex/pf-phase-2` | v003、通用银行 CSV、候选、人工决定/撤销、API、桌面/移动界面、全量三库验收与 8080 发布均完成；代码运行修复 `b61c42da`，镜像 `ezbookkeeping-fork:2.0.0-pf2-4c662305`，证据见 0.19～0.21 节 |
| REC-CONTRACT-201 | v003、候选、决策、账本效果与通用银行映射契约 | DEPLOY-101 | 已完成 | 主负责人 / `codex/pf-phase-2` | `0132d80a`；冻结契约见 0.19 节，阶段内任务不推送 |
| BANK-201 | 银行/信用卡通用 CSV 证据映射 | REC-CONTRACT-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 执行 `a4ac712d`、集成 `d22fb3e9`；显式 parser、映射摘要、来源账户强制映射及三包聚焦 test/vet 通过，前端留给 REC-UI-201 |
| REC-DB-201 | v003 对账模型、迁移与三库精确验模 | REC-CONTRACT-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 执行 `dac6bf5c`、集成 `01ce09b5`；五表、连续迁移、精确验模及 SQLite 协议通过，MySQL/PostgreSQL 留 REC-QA-201 一次收口 |
| REC-CAND-201 | 有界候选生成、稳定 case 与可解释原因 | BANK-201、REC-DB-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 执行 `0a6601a2`、集成 `38d92211`；有界扫描、稳定 case、并发唯一、刷新保护和零账本副作用经聚焦 test/vet 验证 |
| REC-DEC-201 | 人工决策、多来源证据关联与安全撤销 | REC-CAND-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 契约 `07341985`、实现 `4fc339c1` 已合入；集成修复 `b37c9733`、`c8259f0e` 补齐新建与既有事件的账户币种、金额和方向校验；幂等/CAS、零一多事件、转账/退款、独立/暂缓、安全撤销和 v002+活动 v003 证据读取经聚焦 test/vet 验证，任务已归档清理 |
| BANK-UI-201 | 通用银行 CSV 显式映射与来源账户交互 | BANK-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 执行 `a6deed72`、合并 `4544b800`；显式入口、三种金额模式、列号转换、账户映射及旧请求兼容经 12 个聚焦测试、类型和 lint 验证，任务已归档清理 |
| REC-API-CAND-201 | 候选生成 API 与路由 | REC-CAND-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 执行 `7adb7604`、合并 `96086ad9`；安全响应、严格原因码、权限和唯一路由经 pkg/api、cmd test/vet 验证，侧边栏任务与 worktree 已归档清理 |
| REC-API-DEC-201 | 对账 case/决定/撤销 API DTO 与处理器 | REC-DEC-201 契约检查点 | 已完成 | 主负责人 / `codex/pf-phase-2` | 执行 `4ac811e6`、合并 `37cb1ba8`、接线 `8032617a`；五个 handler、最终稳定码、安全 DTO、默认服务构造和路由经 pkg/api、cmd test/vet 验证，任务已归档清理 |
| REC-UI-201 | 桌面对账工作台与移动只读摘要 | REC-CAND-201、冻结 REST 契约 | 已完成 | 主负责人 / `codex/pf-phase-2` | 基础 UI `cb2a2192`、契约修正 `4d55db83`、决策编辑器 `0c39f07a`、状态入口修正 `3b2e91d1`，合并 `82939408`；桌面六端点、显式字段来源、零事件账本草稿与撤销影响闭环，移动端只读；12 项聚焦测试、类型和非修复式 lint 通过，任务已归档清理 |
| REC-QA-201 | 阶段二三库、并发、隐私、回滚与浏览器验收 | REC-UI-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 全仓 Go test/vet、前端 38,579 项测试、类型/lint/build 和 SQLite/MySQL/PostgreSQL 门禁通过；隔离迁移及桌面/移动浏览器冒烟通过，分块修复 `b61c42da`；两个只读任务已归档、worktree 已清理，证据见 0.20 节 |
| DEPLOY-201 | 阶段二镜像、备份迁移与本地发布 | REC-QA-201 | 已完成 | 主负责人 / `codex/pf-phase-2` | 镜像 `ezbookkeeping-fork:2.0.0-pf2-4c662305`（`sha256:9a2d5d0d…`）已接管 8080；v003、数据计数、一致性和桌面/移动浏览器均通过，迁移前备份与旧镜像保留，候选资源已清理，见 0.21 节 |
| LOAN-CONTRACT-301 | 阶段三贷款/分期模型与会计语义预评审 | 阶段 1 完成 | 已完成 | 主负责人 / `codex/pf-phase-2` | 侧边栏任务 `019ffb26-7b02-7e13-9645-6c20bce8ece6` 完成只读评审：形成五表候选、状态机、四种还款法、IRR/XIRR、正式交易关联与任务拆分建议；无代码/提交，公共契约未冻结，任务已归档并清理 |
| LOAN-301 | 贷款合同、计划、真实年化和实际交易分配 | 阶段 1 完成 | 进行中 | 第三任主负责人 / `codex/pf-phase-3` | 主负责人任务 `019ffb71-b3f8-7b20-a464-58b99b775752`，worktree `/Users/gaohongxiang/.codex/worktrees/d83f/ezbookkeeping`，基线 `main@a7eb68ff`；v004 与计算/账本/API 契约见 0.23 节 |
| LOAN-CALC-301 | 四种还款法、费用优惠、月度 IRR 与金标 | LOAN-301 契约冻结 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `7815a49a`、集成 `4a874bfc`；56 组矩阵、参考金标、包级 test/vet/race 和主负责人算法评审通过 |
| LOAN-DB-301 | v004 六表、运行模型、迁移与仓储基础 | LOAN-301 契约冻结 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `02e0828f`、稳定码修正 `15b60548`，集成 `8a2bc627`、`3ee544eb`；六表、UID 隔离、CAS/唯一裁决、事务回滚、SQLite 迁移协议及包级 test/vet 通过 |
| LOAN-UI-SHELL-301 | 贷款表单、结果、列表与移动只读前端骨架 | LOAN-301 REST/DTO 冻结 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `9cbf4be8`、集成 `e881f3d3`；11 个 feature 文件、8 项聚焦测试、类型检查和非修复式 lint 通过，公共接线与文案留 `LOAN-UI-301` |
| LOAN-SVC-301 | 合同/revision 生命周期与计划持久化 | LOAN-CALC-301、LOAN-DB-301 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `58a68215`、集成 `790b1e22`，stable patch-id 一致；持久幂等生命周期、追加 revision、计划进度、并发 CAS、回滚、跨 uid 与隐私经源任务 test/vet/race 及集成后包级 test/vet 验证；任务、worktree 和本地分支已清理 |
| LOAN-ALLOC-301 | 正式交易候选、分配、撤销与对账删除保护 | LOAN-SVC-301 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `68eb0f5f`、集成 `099b7fc7`，stable patch-id 一致；正式候选、到账/本金 transfer、利息/费用 expense、前置费、原子多组件、漂移拒绝、binding/contract CAS、只撤关系和 `loan_relation_present` 守卫经受影响四包 test/vet、关键 race 及集成后四包 test 验证 |
| LOAN-API-301 | 贷款计算、合同、计划、候选、结算与撤销 API | LOAN-SVC-301、LOAN-ALLOC-301 | 进行中 | 主负责人 / `codex/pf-phase-3` | 计算/合同 HTTP 切片已由 `LOAN-API-CONTRACT-301` 验收集成；settlement API、账本漂移 DTO、组合根和十二端点路由待 ALLOC 集成后收口 |
| LOAN-API-CONTRACT-301 | 贷款计算与合同生命周期薄 HTTP 边界 | LOAN-SVC-301 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `f9c62032`、集成 `617ca295`，stable patch-id 一致；八个 handler、严格 DTO、安全错误映射、客户端民用日期与脱敏响应经源任务 pkg/api test/vet 及集成后聚焦测试验证 |
| LOAN-UI-301 | 桌面完整贷款工作台与移动只读摘要 | LOAN-API-301、LOAN-UI-SHELL-301 | 进行中 | 执行任务 / `codex/loan-ui-301` | 前端 shell 与十二端点严格 HTTP 客户端已验收集成；当前由 0.6 节独立任务实现桌面完整闭环、移动只读入口、路由导航和中英文文案 |
| LOAN-UI-SERVICE-301 | 贷款前端类型契约与十二端点 HTTP 客户端 | LOAN-UI-SHELL-301、LOAN-301 REST/DTO 冻结 | 已完成 | 主负责人 / `codex/pf-phase-3` | 执行 `6d6fdcbd`、修正 `e2969d21`，集成 `96f9c0e0`、`84bbc9b0`；十二端点、显式 effectiveDate、int64 pptr、正交易快照与严格响应归一化经 21 项聚焦测试、类型和非修复式 lint 验证 |
| LOAN-QA-301 | 阶段三三库、并发、隐私、计算和浏览器验收 | LOAN-UI-301 | 未开始 | 待分配 | 阶段收口才运行一次全量/三库门禁 |
| DEPLOY-301 | 阶段三镜像、备份迁移与本地发布 | LOAN-QA-301 | 未开始 | 待分配 | 不复用开发卷；先在正式数据副本演练 v004 |
| DASH-401 | 带覆盖率和缺口说明的可信财务总览 | REC-201、LOAN-301 | 未开始 | 待分配 | 待补 |

推荐并行波次：先串行完成 `BASE-001` 和 `DB-101` 并冻结契约；随后 `PARSE-101`、`PARSE-102`、`STORE-101` 可在独立 worktree 并行；`UI-101` 只在 API/类型契约冻结后并行；`DEDUP-101`、`POST-101` 按身份与账本依赖顺序集成；最后由 `QA-101` 统一收口。

### 0.6 当前主负责人

- 状态：第二任主负责人已完成阶段二开发、验收、发布、主线推送和临时资源清理；第三任主负责人按 0.22 节正式接管阶段三后成为唯一主负责人；
- 阶段三集成分支：`codex/pf-phase-3`，目标工作区：`/Users/gaohongxiang/.codex/worktrees/d83f/ezbookkeeping`，冻结基线：`main@a7eb68ff5720e137d833ff8c217338a5a8831577`；
- 第三任主负责人任务：`019ffb71-b3f8-7b20-a464-58b99b775752`，GPT-5.6-sol / 最高；交接时 worktree 为洁净 detached `a7eb68ff`，收到本节所在交接提交哈希后先切换到 `codex/pf-phase-3` 并核对规划，再拥有任务拆分、契约冻结、集成和看板更新权；
- `LOAN-301` 公共契约已在 `75319c38` 冻结，并在 `08c162b0` 修正前置费用账本语义；计算、v004、合同服务、结算分配、合同 API 与前端 HTTP 客户端均已验收集成。`LOAN-UI-301` 已交付待依赖顺序集成，下一项只创建 settlement API、生产组合根与十二端点路由的单一后端任务；主负责人继续负责共享契约、全局看板、交付评审与集成；
- `origin/main@a7eb68ff` 已包含阶段二全部代码与发布证据。生产 8080 正在运行 `ezbookkeeping-fork:2.0.0-pf2-4c662305`，正式恢复卷和仓库外备份不得被阶段三开发或测试挂载；阶段三完整验收后再合入 `main` 并按用户授权推送。

阶段三活动执行任务如下；各任务冻结基线单列记录，阶段内不推送：

| 任务 | Codex 任务 | 模型 / 推理 | 分支 | worktree | 冻结基线 | 文件所有权 |
| --- | --- | --- | --- | --- | --- | --- |
| LOAN-UI-301 | `019ffbf2-9755-73c1-a2b8-ee24ddb7dae7` | GPT-5.6-sol / 高 | `codex/loan-ui-301` | `/Users/gaohongxiang/.codex/worktrees/cbf1/ezbookkeeping` | `2a2bc312` | `src/features/personal-finance/loans/**` 页面/controller/聚焦测试、桌面/移动路由与最小导航入口、`en`/`zh_Hans` 文案及其他 locale 校验兼容；不得修改后端、迁移、规划或共享服务契约 |

阶段三已完成执行任务：

| 任务 | Codex 任务 | 执行 / 集成提交 | 验证 | 资源清理 |
| --- | --- | --- | --- | --- |
| LOAN-CALC-301 | `019ffb81-af4a-7ef1-8eed-3c8183b23b92` | `7815a49a` / `4a874bfc` | 56 组四方式×两依据×费用/优惠矩阵、50,000/12×4,460.59 金标、包级 test/vet/race、diff 和算法评审通过 | 任务已归档，worktree 和本地任务分支已清理 |
| LOAN-DB-301 | `019ffb81-af4a-7ef1-8eed-3caaaadc392a` | `02e0828f`、`15b60548` / `8a2bc627`、`3ee544eb` | v004 六表与精确验模、v001/v003 升级和重复执行/漂移/失败恢复、UID 隔离、稳定分页、CAS、事务内聚合、并发唯一裁决、回滚及组合后包级 test/vet 通过；两组 patch-id 一致 | 任务已归档，worktree 和本地任务分支已清理 |
| LOAN-UI-SHELL-301 | `019ffb81-af4b-78f2-a15a-f49b645c2bb8` | `9cbf4be8` / `e881f3d3` | 聚焦状态测试 8/8、`vue-tsc --noEmit`、新增文件非修复式 ESLint、diff/所有权检查通过 | 任务已归档，worktree 和本地任务分支已清理 |
| LOAN-SVC-301 | `019ffbb3-c386-7da0-8d4e-8875349ce825` | `58a68215` / `790b1e22` | 持久幂等 create/revise/close/reopen/cancel、追加 revision、当前计划查询与进度派生、合同与 action 并发、事务回滚、跨 uid、账户复核和脱敏错误经源任务 test/vet/race 及集成后包级 test/vet 验证；stable patch-id 一致 | 任务已归档，worktree 和本地任务分支已清理 |
| LOAN-API-CONTRACT-301 | `019ffbda-8e67-7781-9ad0-a7bc8f5e5839` | `f9c62032` / `617ca295` | 八个 calculate/contracts handler、uid 注入、显式 effectiveDate、严格 pptr/ID、稳定游标、客户端时区、服务端 revise 身份复用、错误白名单与敏感字段排除经源任务 pkg/api test/vet、diff-check 及集成后聚焦测试验证；stable patch-id 一致 | 任务已归档，worktree 和本地任务分支已清理 |
| LOAN-UI-SERVICE-301 | `019ffbdb-c910-7451-9e55-cd936dd5113a` | `6d6fdcbd`、`e2969d21` / `96f9c0e0`、`84bbc9b0` | 十二端点 Axios 薄方法、严格响应投影、string ID、int64 pptr、显式 effectiveDate、transfer 双侧正快照、expense 无对端字段经 21/21 聚焦测试、`vue-tsc --noEmit`、受影响文件非修复式 ESLint 和 diff-check 验证；两组 stable patch-id 一致 | 任务已归档，worktree 和本地任务分支已清理；集成工作树未安装 node_modules，未重复源任务已通过的前端命令 |
| LOAN-ALLOC-301 | `019ffbd3-5f3e-7902-b1fa-afd64d6b7482` | `68eb0f5f` / `099b7fc7` | 候选脱敏/有界/跨 uid、正式事件复核、到账与本金双侧 binding、费用 expense、前置费、部分/多组件、持久幂等、超额 action-required、事务回滚、contract/binding/undo CAS、交易漂移不计进度、关系撤销留历史和 REC 删除守卫经源任务四包 test/vet、关键 race 与集成后四包 test 验证；stable patch-id `0ebcdbd9…` 一致 | 已集成，任务、worktree 和本地分支待主负责人完成证据提交后清理 |

阶段一和阶段二执行任务的历史审计记录如下；对应 worktree/分支均已在验收完成后清理：

| 任务 | Codex 任务 | 分支 | worktree | 冻结基线 | 文件所有权 |
| --- | --- | --- | --- | --- | --- |
| PARSE-101 | `019ff7cf-aa79-7203-a389-0dba53343985` | 已清理（原 `codex/parse-101-alipay`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/c300/ezbookkeeping`） | `c267346c` | `pkg/converters/alipay/**` |
| PARSE-102 | `019ff7cf-aa78-73a2-88c2-2ee3758c1b81` | 已清理（原 `codex/parse-102-wechat`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/8bc2/ezbookkeeping`） | `c267346c` | `pkg/converters/wechat/**` |
| STORE-101 | `019ff7cf-aa78-73a2-88c2-2f07ce536d5b` | 已清理（原 `codex/store-101-import-api`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/f05f/ezbookkeeping`） | `c267346c` | `pkg/personalfinance/importing` 的仓储/服务实现与测试、指定服务/API 文件和 `cmd/webserver.go` 最小注册；未改冻结模型与迁移 |
| DEDUP-101 | `019ff7d0-894f-7421-8c8e-2b7bc746ff3e` | 已清理（原 `codex/dedup-101-source-identity`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/d101/ezbookkeeping`） | `ab22f760` | 新增独立去重应用服务与持久层文件及测试；未改 `identity.go`、`status.go`、`model.go`、v001 migration、POST/API/UI |

阶段一历史后续任务：

| 任务 | 分支 | worktree | 冻结基线 | 文件所有权 |
| --- | --- | --- | --- | --- |
| POST-101 | 已清理（原 `codex/post-101-atomic-posting`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/post101/ezbookkeeping`） | `68267fed` | v002 migration、`pkg/personalfinance/importing` posting/批次 issue、核心 session 账本适配器及其必要 API/路由与测试；未修改 v001 冻结 bean/checksum |
| FLOW-101 | 已清理（原 `codex/flow-101-import-orchestration`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/flow101/ezbookkeeping`） | `fb49afab` | 导入解析编排、来源账户仓储/服务、受限存储读取、必要 API/路由与聚焦测试；未修改迁移、posting 核心或前端 |
| UI-101 | 已清理（原 `codex/ui-101-import-workbench`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/ui101/ezbookkeeping`） | `da224137`，更正 `cb17709e` | `src/features/personal-finance/**`、`src/lib/services.ts` 薄调用、桌面/移动路由与导航最小接入、`zh_Hans`/`en` 文案及相关前端测试；未修改后端生产代码 |
| OPS-101 | 已清理（原 `codex/ops-101-lifecycle`） | 已清理（原 `/Users/gaohongxiang/.codex/worktrees/ops101/ezbookkeeping`） | `c5f2bb44` | 0.16 节列明的 PF 运维服务/仓储、指定 API/数据管理接点、桌面工作台、`zh_Hans`/`en`、运维文档与聚焦测试；未修改迁移或实现通用撤销 |

交接核对已完成：`34a633b9` 以 fast-forward 进入当时的集成分支，阶段一全部执行任务均已验收、集成并清理。阶段一现已合入并推送到 `origin/main@67053c1e`；用户已授权今后每个完整阶段验收、合入 `main` 后继续安全同步，阶段内分支仍不推送。

当主负责人判断剩余上下文不足以安全完成下一集成单元时，必须在继续编码前更新本节和任务看板，提交已经验证的工作，列出全部活动分支/worktree、未提交文件、测试结果、阻塞与下一条准确命令，再由新的主负责人核对后接管。聊天摘要不能替代这份仓库内记录。

### 0.7 2026-08-13 工程基线记录

- 官方只读 remote：fetch `https://github.com/mayswind/ezbookkeeping.git`，push `DISABLED`；
- 本次上游基线：`upstream/main@fa6cda3b3d97`；同步前产品主线相对上游为自身 1 个提交、落后 15 个提交；
- 同步内容：1 个罗马尼亚央行汇率 URL 修复及 14 个账户/桌面样式/导入交互修复，无 schema 和核心交易模型迁移；
- 前端运行时：Node `v24.14.1`、npm `11.11.0`，`npm ci` 无已知漏洞；
- `npm test`：7 个测试文件、38,555 个用例通过；
- `npm run build`：通过，保留 Vite 配置、Sass `@import` 和大 chunk 既有警告；
- `npm exec vue-tsc -- --noEmit && npm exec eslint -- .`：通过且未修改文件；
- 后端：宿主机没有 Go；使用 `golang:1.26.4-alpine3.24` arm64 容器，与 Dockerfile 和 CI 版本一致；
- `BUILD_PIPELINE=1 CHECK_3RD_API=0 go test ./... && go vet ./...`：通过；第三方实时汇率探针不作为确定性门禁；
- 同步独立 worktree 全门禁通过后快进本地 `main`，阶段分支再快进到同一提交；同步分支和两个临时 worktree 已安全删除。
- 新增固定镜像 digest 的 `docker/compose.pf-test.yml`、Go runner 和 `scripts/test-pf-db.sh`；SQLite、MySQL 8.4、PostgreSQL 17 均在独立进程中连续两次执行核心 `database update` 通过；
- 三库测试使用随机 `ezbk-pfqa-*` Compose 项目、内部网络和 tmpfs，不发布端口、不挂载 `data/log/storage`；成功与失败路径均验证自动清理，现有 8080 容器保持运行且未被访问；
- BASE-001 只证明工具链、三库连接和现有核心 Sync2 可重复执行；PF migration、checksum、并发认领和恢复测试明确归 DB-101，不能用本结果替代。

### 0.8 2026-08-13 DB-101 完成与并行波次记录

- 冻结提交：`c267346c070bb4b3e8d248c5cd81d289f0e37a22`；PF schema version 为 v001，冻结 checksum 为 `71ca2be439df0fadab021905b4ffd2a21dcd814bc8903f47759287574d692193`；
- v001 只创建文件、来源账户、批次、原始行和来源身份五张业务表，PF 模型不进入核心常驻 Sync2；升级由独立 migration ledger、版本连续性、checksum 和精确 schema verifier 管理；
- SQLite、MySQL 8.4、PostgreSQL 17 的全新升级、重复执行、checksum/未知高版本拒绝、失败恢复、租约超时接管、并发认领、漂移拒绝和 UID 隔离测试均通过；MySQL 认领更新使用锁后数据库时间和 fenced update；
- `scripts/test-pf-db.sh all` 在最终提交源码上通过，所有临时 Compose 容器、网络、卷和 runner 镜像已清理；未复用真实 `data/log/storage`，也未访问现有运行容器；
- `BUILD_PIPELINE=1 CHECK_3RD_API=0 go test ./... && go vet ./...` 在无外网临时容器网络中通过；`git diff --check` 与 shell 语法门禁通过；
- DB-101 同时冻结 parser descriptor、canonical raw projection、来源账户证据、身份/核心摘要版本、状态与 issue code、文件级安全错误和仓储隐私会话契约。后续任务只能消费这些契约；需要变更时必须由主负责人串行决策并先更新本文；
- 回滚边界不变：旧应用可忽略新增 PF 表；不提供跨三库通用 down migration；生产变更前依赖备份恢复演练。
- 并行波次在冻结基线之上交付 `PARSE-101@6588bd7b`、`PARSE-102@e8c69aa7`、`STORE-101@60a710ed`，分别以 `63a795f0`、`6a4bfda2`、`f721f7f6` 集成；`b6cba135` 仅记录三个已验证执行提交的祖先关系，文件树未变化；
- 支付宝解析器保留数据区物理空行与精确 CSV 定位；微信解析器严格拒绝非法 UTF-8、奇数字节 UTF-16 和不成对 surrogate；两者均消费冻结证据契约，未修改公共模型、状态或迁移；
- 原文件服务在字符转码前按原始字节计算 SHA-256，通过不透明临时/最终 key、长度与摘要复验、补偿状态机和 `uid` 条件查询实现持久幂等；API 不返回摘要、对象 key 或创建 IP，普通日志不记录原文件名、内容或完整来源标识；
- `BUILD_PIPELINE=1 CHECK_3RD_API=0 go test ./...`、`go vet ./...`、受影响包 race、`scripts/test-pf-db.sh all` 全部通过；STORE 仓储契约另在 SQLite、MySQL 8.4、PostgreSQL 17 通过，隔离资源已清理；
- Node `v24.14.1`、npm `11.11.0` 下 `npm test` 的 7 个文件、38,555 个用例通过，Vue 类型检查、非修复式 ESLint 和生产构建通过；保留基线已有的 Vite、Sass `@import` 与大 chunk 警告；
- 三个执行 worktree 与本地任务分支均在 patch-id、洁净度和集成祖先关系核对后安全清理；本轮未推送远程。

### 0.9 2026-08-13 DEDUP-101 完成记录

- `DEDUP-101` 从 `ab22f760` 独立实现，执行提交为 `e4cd4dc85e257d095b17c71014871e086cba67c3`，以合并提交 `9c80e0bded7add803649fcf37d9ad87499dceb9f` 集成到 `codex/pf-phase-1-owner2`；
- 新增去重应用服务和独立持久层文件，复用 `c267346c` 冻结的 identity key、core digest、强指纹、状态矩阵、运行模型与 v001；`identity.go`、`status.go`、`model.go` 和 v001 migration 文件树未变；
- 每次显式解析/重解析都创建不可变批次，保存全部有效与无效行、解析/中心规则版本、解析选项摘要和映射快照；同一隐私事务内裁决 `new / exact_duplicate / identity_conflict / batch_local`、行处置和批次计数；
- repository 在写入前重建身份候选并校验完整持久边界，不信任调用方伪造的 key/core；已有 canonical identity 的主键、作用域、摘要、版本和时间不变量均再次校验，冲突证据不覆盖首次 core digest；
- SQLite/PostgreSQL 使用指定 `(uid, source_identity_key)` 的 `ON CONFLICT DO NOTHING`；MySQL 只识别普通 `INSERT` 的 1062 唯一冲突，不用 `INSERT IGNORE` 吞掉其他警告。候选 identity 主键碰撞、行插入失败和伪造摘要均验证整个批次回滚；
- `go test ./pkg/personalfinance/importing`、该包 `-race`、20 轮 SQLite 并发裁决、SQLite/MySQL 8.4/PostgreSQL 17 的 `pf_importing_db_integration` 均通过；三库测试覆盖同 core 并发、异 core 冲突和 identity 主键碰撞回滚；
- 执行提交源码上 `scripts/test-pf-db.sh all`、全仓 `go test ./...`、`go vet ./...` 通过；集成后受影响包、全仓 `go test ./...` 和 `go vet ./...` 再次通过。本任务无前端文件改动，延用当前基线已通过的 38,555 个前端测试、类型/lint 和生产构建证据；
- 原 DEDUP worktree 和本地任务分支已在洁净度、集成祖先关系与门禁核对后安全清理；临时 Compose 容器、网络、卷和本地 runner 镜像均已清理，本轮未推送远程；
- 架构复核记录的批次级 issue 缺口已在 0.10 节决策：v002 新增独立不可变 `pf_import_batch_issue`，不把文档问题偷塞进 `error_code/error_summary`、不伪造原始行，也不改写 v001。

### 0.10 2026-08-13 POST-101 公共契约冻结

- 冻结基线为 `ce570b71`；本节与 5.10、9.1～9.3 节共同冻结 POST-101 的 v002 schema、幂等命令、批次/行状态推进和 session 账本适配器。生产实现必须从本次规划提交建立独立分支/worktree；需要改变下述公共契约时先停工并由主负责人更新本文；
- v002 只新增 `pf_import_posting`、`pf_raw_row_transaction_link`、`pf_import_batch_issue` 三张业务表，不修改 v001 文件/checksum，也不向核心 `Transaction` 堆来源字段。`pf_import_batch_issue` 按 `uid + batch_id + issue_order` 唯一保存文档级 `code / severity / field`，与批次和原始行同一事务写入，解决 parser 已校验但此前未持久化的审计缺口；
- 客户端幂等键只接受受限非空技术字符串，数据库仅保存 `idempotency-key-v1` 的 SHA-256，不保存或记录原键；`posting-request-v1` 对排序后的命令、行 ID、草稿存在位、交易类型/时间/offset/账户/金额/分类/去重标签/隐藏金额/备注做长度前缀规范编码。命令顺序和标签顺序不改变摘要；同键不同摘要拒绝；
- 请求按来源身份分组，每组是一组同批次 `row_ids` 和一个可空的核心账本草稿。草稿为空表示“必须复用既有关系”；草稿非空表示“若既有关系存在则复用，否则创建”。同一身份不得拆成多组，任一行重复、跨 uid/批次、非 pending、无效、不可入账、未评估或身份冲突均整体拒绝；`batch_local` 和其他 `review_required` 只能通过显式草稿确认；
- 同一来源身份最多对应一个逻辑正式交易；普通交易建立一个 `primary` 链接，转账同时建立 `primary` 与 `transfer_counterpart` 链接。历史同身份若出现多个 primary、缺失/已删除交易或不完整转账对，视为证据不变量损坏并停止，不静默新建；人工修改后的既有交易仍可继续补证据链接，新链接保存当时的 `updated_unix_time` 快照；
- `created_transaction_count` / `reused_transaction_count` 统计逻辑账本事件，转账双侧合计一件；成功后选中行统一推进为 `non_postable + linked`，批次计数在事务内按全部行重新核算而非盲目加减，仍有 pending 行则回到 `partially_posted`，否则进入 `completed`；
- posting 初始 `ready` 记录先独立持久化，账本事务内通过条件更新认领 posting 和 `ready / partially_posted` 批次，随后创建/复用正式交易、写链接、更新行/批次和 posting，再一次提交。账本事务失败会整体回滚到原批次稳定状态，另以短隐私事务把 posting 标为 `failed`；同一幂等键的 `completed / failed` 都稳定重放，失败修复后使用新键，不把同一键偷偷执行第二次；
- 用户存在性、导入功能限制和交易编辑时间范围必须在进入账本事务前由注入的权限适配器强制校验；这是用户库与用户数据分片之间明确的跨库预检，不宣称跨库原子性。账本事务内仍重新验证 uid、账户、分类、标签、转账、余额和交易时间格式等核心不变量；
- 核心 `TransactionService` 只新增不依赖 PF 模型的窄 session 写入口：验证数据库/session 属于同一分片且已有活动事务，在调用方 session 中生成交易/标签索引 ID并复用现有 `doCreateTransaction`；返回 primary 和可选 transfer counterpart，绝不自行提交或回滚。现有 `CreateTransaction` 也复用同一准备逻辑，避免形成第二套账本规则；
- POST-101 同时冻结 `POST /api/v1/personal_finance/import_batches/post.json` 的薄 HTTP 边界和按正式交易反查证据的只读服务契约；HTTP 只负责鉴权、时区解析、绑定、稳定错误映射与响应组装，正文不接受内部 transaction ID、时间戳、删除标记、创建 IP、图片或地理位置；
- 门禁必须覆盖同键重放/异摘要、同批并发、崩溃可恢复 ready、失败整体回滚、多轮部分确认、同身份多行、精确重复补链接、batch-local 显式确认、冲突禁止、转账双侧/余额、人工修改后补证据、损坏/删除关系、uid 隔离、日志隐私和 SQLite/MySQL/PostgreSQL 一致性。

### 0.11 2026-08-13 POST-101 完成证据

- 执行提交 `d45b7ff1` 已由主负责人按冻结契约复核并集成为 `b4d57849`；新增 v002 三张表、持久幂等 posting 服务、来源身份复用/证据链接、核心 `TransactionService` 的调用方 session 窄入口、权限预检、`POST /api/v1/personal_finance/import_batches/post.json` 和正式交易证据反查服务；
- SQLite 聚焦用例覆盖同键并发重放、同键异摘要、分轮确认、跨批次同来源身份并发收敛、精确重复补链接、跨用户证据隔离及账本失败整体回滚；核心适配器用例验证调用方提交/回滚所有权、余额更新和非事务 session 拒绝；
- `scripts/test-pf-db.sh all` 在 SQLite、MySQL 8.4、PostgreSQL 17 上通过 v001→v002 迁移、导入并发/回滚集成测试和两次 database update，并自动清理隔离容器、网络、卷与临时 runner 镜像；
- 集成分支上的全仓 `go test ./...` 与 `go vet ./...` 通过，`git diff --check` 通过；本任务没有前端文件改动，不重复运行此前已通过且无相关变化的前端门禁；
- v001 冻结 bean/checksum 未改，核心账本不依赖 PF 模型；幂等键只保存摘要，PF 查询继续经隐私 session 且全程带 uid。本轮未推送远程。

### 0.12 2026-08-13 FLOW-101 解析编排契约冻结

- UI-101 开始前补齐一条最短可运行纵切面，不新增业务表、不修改 v001/v002：受限读取已验证的 available 原文件，依次调用已注册的支付宝 App/Web 与微信 CSV/XLSX parser，校验 probe 后只选择唯一最高置信度 parser，再调用现有 `DedupService` 生成不可变批次；无匹配或同级歧义时返回稳定格式错误，不猜测来源；
- `POST /api/v1/personal_finance/import_batches/reparse.json` 只接受 `fileId`、可空 `sourceAccountId`、`currency`、`timezoneUtcOffset` 和技术型 `reasonCode`。稳定完整账户证据按现有 `source-account-key-v1` 在同 uid 自动查找或创建来源账户；掩码、昵称或缺失证据不得作为稳定 key，未显式选择来源账户时返回脱敏 discovery 结果且不写批次；
- 新增 `GET /api/v1/personal_finance/source_accounts/list.json` 与 `POST /api/v1/personal_finance/source_accounts/save.json`。人工创建的来源账户使用服务端随机材料生成不可逆 key，后续更新只允许修改脱敏展示名、账本账户映射和启停状态，绝不改写 key/source type；所有写入继续由数据库唯一约束裁决并带 uid；
- 原文件读取适配器只接受受限最终对象 key，按数据库记录的长度上限读取并复核 SHA-256；原始字节、完整标识、对象 key 和 parser 原始错误不得进入普通日志或 HTTP 响应。`personalfinance/importing` 只依赖 parser 接口，具体支付宝/微信 parser 在 API 组合根注入，避免反向依赖；
- 新增 `GET /api/v1/personal_finance/transactions/evidence.json?transaction_id=...`，把 POST-101 的只读服务契约暴露为 uid 隔离的脱敏证据摘要；批次详情同时返回有序文档级 issue。HTTP 层仍只负责鉴权、绑定、稳定错误映射与响应组装；
- 聚焦验证只覆盖 parser 唯一选择、稳定/非稳定来源账户分支、重复 reparse、跨 uid 隔离、受限读取/哈希校验和证据响应脱敏。FLOW-101 不重复 POST-101 已通过且未触及的账本并发与三库全量门禁；数据查询变化通过受影响包与一次 SQLite 纵切面验证，完整门禁留到 UI/QA 集成节点。

### 0.13 2026-08-13 FLOW-101 完成证据

- 执行提交 `8e5e9012` 已由主负责人按冻结契约复核并以等价补丁集成为 `f1b14579`；接入四个既有 parser、唯一最高置信度选择、available 原文件受限读取与 SHA-256 复核、来源账户档案管理、显式 reparse、批次 issue 和正式交易脱敏证据 API；
- SQLite 纵切面验证稳定账户自动归属、重复 reparse 复用同一来源档案并生成两个不可变批次，以及跨 uid 文件隐藏；单元用例覆盖弱来源证据等待人工选择、同级 parser 歧义拒绝、来源 key/source type 更新不可变、账本映射清空、对象摘要不符拒绝和证据响应原始字段脱敏；
- 受影响的 importing、services、api 与 cmd 包测试通过，受影响 Go 包 vet 与 `git diff --check` 通过；未修改模型、v001/v002 migration、posting 核心和前端，因此按 0.12 与根级适度验证规则未重复三库、POST 并发、全仓及前端门禁；
- 临时 worktree 与分支已在确认任务/集成补丁 stable patch-id 相同后安全清理；集成分支工作树洁净，本轮未推送远程。

### 0.14 2026-08-13 UI-101 前端契约冻结

- UI-101 只消费 0.10～0.12 节已经冻结的 HTTP 契约，不修改 PF 模型、迁移、parser、去重、posting 或核心账本。前端类型、服务和状态收敛在 `src/features/personal-finance`，`src/lib/services.ts` 只增加薄 API 调用；桌面、移动路由和现有导航只做最小接入，旧交易导入入口保持不变；
- 桌面新增一个中文优先的个人财务导入工作台，单页承载上传、重复文件提示、显式 reparse、来源账户选择或创建、批次历史、批次摘要、稳定分页行预览、重复/冲突解释和确认入账。上传成功后才触发 reparse；重复上传可打开最新批次，也可由用户显式再次解析，不静默覆盖历史批次；
- 弱来源证据不自动建档或猜测账户。工作台展示脱敏 discovery，要求用户选择同来源档案，或创建只含来源类型、脱敏展示名、可空账本映射和启停状态的人工档案，再携带 `sourceAccountId` 重试 reparse；来源档案 key、完整标识和对象存储字段永不进入浏览器模型；
- 行预览默认不请求 `include_raw_snapshot`，只显示 API 已返回的标准字段、脱敏业务摘要和状态解释。`identity_conflict`、无效或 `non_postable` 行禁用确认；当前状态矩阵把尚未关联的 `exact_duplicate` 固定为 `review_required`，因此它与 `new`、`batch_local` 和其他 `review_required` 行一样必须显式提交账本草稿：若该来源身份已有完整正式交易关系，POST-101 复用既有交易，否则按草稿创建，前端不得猜测哪种结果；当前 UI 不暴露无草稿命令，因为没有可由行查询稳定证明其安全的用户场景；
- 阶段一采用单行逐次确认，而不是把不同来源行批量套用同一分类。确认对话框预填标准时间、金额、方向和批次账本映射，用户仍须确认交易类型、分类、账户及转账对端；每次提交生成新的客户端技术型幂等键，成功后刷新批次与当前页。界面不提供后端尚无契约的删除、撤销、冲突裁决或自动分类；
- 正式交易证据通过只读弹窗按 transaction ID 查询，展示脱敏来源、批次、文件扩展名、行号、标准金额/方向/时间、关系角色和创建方式，不显示原始字段、来源标识、备注、哈希或对象 key。现有交易详情只增加最小入口，不改变交易编辑保存流程；
- 移动端阶段一只在设置页增加个人财务导入历史入口，提供批次状态、来源、待处理/重复/冲突/已入账计数和只读摘要；不在移动端上传、建档或确认入账，避免两套交互状态机。新增用户可见文字走现有国际化体系，主动维护 `zh_Hans` 与 `en`，其他语言沿用现有 fallback，不扩展无关国家、税制、银行或币种规则；
- 验证按前端改动风险收敛：新增 feature 的类型/状态单元测试覆盖上传后分支、弱来源选择、可确认行判定、精确重复显式确认和幂等请求组装；再运行一次 Vue 类型检查、非修复式 ESLint 与生产构建。后端生产代码不变，不重复三库、posting 并发或全仓 Go 门禁；完整端到端、恢复与阶段隐私收口留给 QA-101。

### 0.15 2026-08-13 UI-101 完成证据与 QA 预检

- UI-101 执行提交 `551c105c` 已按文件所有权复核，并以 stable patch-id `87912fba7aee7f7be8de6fd4fc556a7dadab39ae` 等价集成为 `a2cb4691`；桌面新增上传→reparse→弱来源档案选择/创建→批次历史/预览→单行确认的完整工作台，移动端新增只读批次历史，现有交易详情增加脱敏来源证据入口；
- 状态与请求组装保持在 feature 内：身份冲突、无效、已关联或不可入账行不会进入确认；`new`、`batch_local` 和尚未关联的 `exact_duplicate` 均要求显式账本草稿，服务端再按 POST-101 不变量创建或复用；UTC offset 按解析证据保真，不受浏览器时区静默改写；
- 新增 5 个聚焦用例覆盖重复上传分支、来源档案按来源/启停过滤、新行/精确重复草稿要求、冲突/无效/已关联禁止和幂等请求组装；Vue 类型检查、受影响文件非修复式 ESLint、生产构建与集成后聚焦测试均通过，只保留仓库既有的 Vite 配置、Sass `@import` 和大 chunk 警告；
- UI-101 没有修改后端、迁移或三库查询，按 0.14 节未重复 Go、posting 并发和三库门禁；任务 worktree/分支已在确认任务/集成 stable patch-id 相同且两侧洁净后安全清理，本轮未推送远程；
- QA 预检发现阶段 1D 的验收对象尚不完整：现有代码只有上传补偿使用的文件内容状态，没有用户可调用的原文件内容删除、未入账批次废弃、PF 用户数据清理和孤立对象/批次一致性运维入口。为避免用测试掩盖缺功能，新增 `OPS-101` 先实现最小安全运维闭环，`QA-101` 改为依赖它；不把贷款、跨来源对账或总览混入该任务。

### 0.16 2026-08-13 OPS-101 最小运维契约

- 本任务不新增业务表或迁移，不物理删除批次、原始行、来源身份、posting 和证据关系，不实现已入账交易的一键撤销；修改范围限于 `personalfinance/importing` 运维服务与仓储、指定 API/路由、现有数据管理接点、个人财务工作台、`zh_Hans`/`en` 文案、运维说明和聚焦测试；
- `POST /api/v1/personal_finance/import_batches/discard.json` 接受 `{batchId}`。只有 `awaiting_source_account` 或 `ready` 且没有 linked 行、posting 记录和已入账计数的批次可原子转为 `discarded`；重复请求幂等返回已废弃批次，`posting`、`partially_posted`、`completed` 或任何存在账本影响的批次必须拒绝；
- `POST /api/v1/personal_finance/import_files/delete_content.json` 接受 `{fileId}`。服务按同一文件分片锁串行执行“幂等删除对象→条件更新为 `deleted` 并写入 `content_deleted_unix_time`”；`deleted` 重放成功，`pending` 拒绝，存储已删但数据库收口失败时允许同请求重试。删除仅移除原文件字节，文件元数据、批次、行、身份和证据关系保持可查，之后同哈希显式重传仍可恢复内容；
- `GET /api/v1/personal_finance/import_batches/undo_impact.json?batch_id=...` 只返回聚合影响：本批次关联交易总数、posting 创建/复用数量、已修改或缺失交易数量、跨批次共享数量以及不能自动撤销的原因码；前端明确展示“本阶段不自动撤销正式账本”，不得把影响预览变成通用撤销入口；
- 桌面工作台为允许状态提供“废弃批次”和“删除原文件”确认动作，确认前展示待处理/已入账数量及不可逆边界；移动端继续只读。服务端状态机是最终裁决，前端禁用只用于减少误操作；
- 现有数据统计新增 PF 原文件、批次和原始行数量。`ClearAllDataHandler` 必须先幂等删除当前 uid 的全部已登记 PF 对象，再在一个 privacy transaction 内按依赖逆序删除全部 `pf_` 用户数据，最后继续现有核心清理；任一步失败返回失败并允许安全重试，不删除 `pf_schema_migration`；
- 用户级一致性检查只返回脱敏聚合，核对批次计数可由行状态重算、PF 子记录/证据链接无悬空、正式交易链接仍存在且未删除，以及文件内容状态与对象校验一致。运维命令在停服条件下额外枚举 PF 存储，报告未登记最终对象和遗留临时对象；日志不得包含对象 key、原文件名、账户标识或原始字段；
- 备份口径是用户数据库与 PF 对象存储的一致性快照，CSV/TSV 交易导出不是完整备份。恢复后先运行 schema 升级，再运行一致性检查；旧二进制可忽略 PF 表，但已创建的正式交易不会随 PF 回滚自动消失；
- 最小验证覆盖：跨 uid 隔离、允许/拒绝/幂等废弃、对象删除失败与数据库收口重试、删除后证据可读、清空用户数据无已登记原文件、统计重算、影响聚合与敏感字段缺失；完成实现后只运行受影响包与前端聚焦检查，三库和全仓门禁留到 OPS 集成后的 `QA-101` 单次收口。

### 0.17 2026-08-13 OPS-101 与阶段一完成证据

- OPS-101 执行提交 `5155ae4f` 与保护修正 `d66a0623` 经主负责人按 0.16 节复核，并以 stable patch-id 等价集成为 `db598600`、`536bc95b`。实现包括未入账批次幂等废弃、原文件字节幂等删除、只读撤销影响、PF 数据统计和用户清理、脱敏一致性检查、停服对象清单以及《个人财务运维与备份》；没有新增表、迁移或第二套账本撤销逻辑；
- 主负责人额外收紧两个误操作入口：确认对话框引用缺失时破坏性动作直接退出；原文件内容不是 `available` 时，重解析按钮和方法入口均拒绝执行。桌面端承担确认动作，移动端保持只读；服务端状态机仍是最终裁决；
- OPS 聚焦验证覆盖跨 uid、允许/拒绝/幂等废弃、对象删除失败和数据库收口重试、证据保留、影响聚合、统计重算、用户清理与日志/响应隐私；Local filesystem 对象枚举与 WebDAV 明确不支持路径均有验证。MinIO 使用可选枚举能力，不改变既有 `ObjectStorage` 公共契约；
- QA-101 在最终集成源码上一次性通过：Go `1.26.4` 容器中的 `BUILD_PIPELINE=1 CHECK_3RD_API=0 go test ./...` 与 `go vet ./...`；Node `v24.14.1`、npm `11.11.0` 下 8 个前端测试文件共 38,561 个用例、Vue 类型检查、全仓非修复式 ESLint 和生产构建。构建只保留既有 Vite 配置、Sass `@import` 和大 chunk 警告；
- `scripts/test-pf-db.sh all` 在 SQLite、MySQL 8.4、PostgreSQL 17 上通过 PF 单元测试、v001→v002 migration、导入并发/事务回滚集成测试和两次 `database update`；随机命名的容器、网络、卷和 runner 镜像均自动清理，未挂载或访问真实 `data`、`log`、`storage`；
- 恢复边界已形成可重复停服流程：数据库与 `personal_finance/import_files` 必须作为一致性快照恢复，随后执行 `database update` 和 `database check-personal-finance --uid <UID>`；旧应用可忽略 PF 表，但备份回滚不会自动撤销恢复点之后的正式交易。阶段收口没有触碰用户现有官方容器或真实数据；
- 2026-08-13 收口时只读 fetch 官方主线，`upstream/main` 与既有基线同为 `fa6cda3b3d9768214a1a9aff273192e239f39421`，没有待评估的新提交。最终 `git diff --check`、工作树洁净和任务 stable patch-id 核对通过；OPS 临时 worktree/本地分支已安全清理，本阶段未推送远程。

### 0.18 2026-08-13 DEPLOY-101 安全切换记录

- 本地 `main` 已从 `57f6a263` fast-forward 到阶段一收口提交 `2b635c14`，不产生额外合并提交；仍不推送远程。正式镜像必须同时向后端和前端注入同一 Git 提交哈希，普通 clone 继续从 Git 读取，worktree Docker 构建使用显式 `BUILD_COMMIT_HASH`，禁止发布空提交哈希镜像；
- 切换前的官方容器仍在运行但其 `/ezbookkeeping/{conf,data,log,storage}` bind mount 已指向被替换的旧宿主目录 inode；应用日志从 2026-08-12 起持续报告 SQLite `operation not permitted`，宿主现有目录为空，进程也没有打开的数据库文件句柄；
- 初次从终端搜索用户目录、OrbStack/Docker 卷、回收目录与本机快照时，只找到二开开发实例的新库和一个 0 字节恢复文件；随后通过 Finder 垃圾桶接口定位到原运行目录 `ezbookkeeping 下午8.35.28`。用户将其按复制方式恢复到仓库外目录，垃圾桶原件仍保留，并另存原样基线与迁移副本；
- 恢复数据库大小 212,992 字节，修改时间与旧容器记录完全一致，三份副本 SHA-256 一致；SQLite `quick_check` 和 `integrity_check` 均为 `ok`。迁移前包含 1 个用户、6 个账户、7 笔交易和 81 个分类，对象存储为空；没有输出账号、邮箱或交易内容；
- 初始候选镜像 `ezbookkeeping-fork:2.0.0-pf1-01dd0020` 完成数据库升级，但浏览器实测发现 PF 共享模块被错误拆成循环依赖分块，登录页触发 `TypeError` 白屏；`37c27067` 将跨桌面/移动共享的 PF 顶层模块归入共同分块，消除 `common`、`store`、`vendor-common` 的循环初始化，不改变业务或数据契约；
- 最终镜像 `ezbookkeeping-fork:2.0.0-pf1-37c27067`（镜像 `sha256:3bb40058…`）重新通过 Docker 发布流程内置的 Go 全仓测试、前端 38,561 项测试、类型/lint 与生产构建；一次性 8085 实例、恢复 8083 实例和正式 8080 实例均实际显示中文登录页、提交 `37c2706` 且没有新的浏览器异常；
- 恢复副本已导入 `ezbookkeeping-fork-recovery-*` 三个独立命名卷并复用原配置中的 `secret_key`；PF v001、v002 成功落账，迁移后 SQLite 完整性和用户、账户、交易、分类数量保持不变。用户在 8083 使用原账号登录并确认历史数据存在；
- 正式实例 `ezbookkeeping` 已使用上述恢复命名卷、仓库外稳定只读配置 `/Users/gaohongxiang/ezbookkeeping-production/conf/ezbookkeeping.ini` 和 `unless-stopped` 策略接管 8080，生产数据库、日志和对象存储不依赖 Git worktree；
- 用户确认正式切换后，旧官方回滚容器、8081 开发实例、8082 空候选、官方 `1.6.1` 镜像、白屏候选镜像和对应空测试卷均已删除；正式恢复卷、垃圾桶复制件、原样基线和迁移副本继续保留。最终 8080 HTTP 与浏览器验证通过；用户随后授权按阶段同步，`origin/main` 已安全快进至 `67053c1e`。

### 0.19 2026-08-13 阶段二启动与 REC-201 冻结契约

- 阶段二基线为 `main@67053c1e`，集成分支为 `codex/pf-phase-2`。阶段一生产实例继续独立运行，阶段二开发不得挂载或迁移正式恢复卷；只有 `REC-QA-201` 通过后才进入备份副本演练和 `DEPLOY-201`；
- 当前只有支付宝、微信证据来源，不能据此宣称已满足“支付宝 + 信用卡流水”或“银行卡还信用卡”验收。`BANK-201` 先增加 `bank` 来源和显式选择的通用 CSV 映射：自动探测永不抢占支付宝/微信 parser；用户必须选择来源账户、编码、分隔符、表头行、时间/金额模式及列索引。第一版支持 UTF-8、GB18030/GBK，逗号/制表符，单金额加方向、带符号金额或收入/支出双列；时间格式使用受限预设，不接受任意执行型表达式；
- 通用银行映射只生成与现有契约一致的原始字段、稳定编号候选和标准化证据，列映射经规范编码进入 `parse_options_digest`，不把账号明文或映射内容写入普通日志。来源账户必须由用户显式建立并映射账本账户；第一版不内置任何银行、卡组织、税制或国家专属规则；
- v003 只新增五张表，不修改已发布 v001/v002。`pf_reconciliation_case` 保存 `case_id/uid/case_key/case_key_version/status/version/member_count/suggested_relation_type/candidate_score/candidate_rule_version/explanation_version/reason_codes_json/current_decision_id?/created/last_evaluated/updated`；`uid+case_key` 唯一，按 `uid+status+updated+case_id` 分页。case key 只由排序后的稳定成员 token 构成，不包含候选算法版本；
- `pf_reconciliation_case_member` 保存 `member_id/uid/case_id/member_order/member_kind/member_ref_id/member_role/created`。优先以 `source_identity` 为成员，只有 `batch_local` 才使用 `raw_row`；批次重叠或规则升级因此复用同一 case。`uid+case_id+member_order` 和 `uid+case_id+member_kind+member_ref_id` 唯一，并提供反查成员索引；第一版一个 case 固定两个不同来源账户的成员；
- `pf_reconciliation_decision` 是追加式持久幂等命令，保存 `decision_id/uid/case_id/expected_case_version/applied_case_version/decision_type/previous_decision_id?/idempotency_key_digest+version/request_digest+version/status/field_selection_json/reason_codes_json/error_code/created/started?/completed?/failed?/updated`；原始幂等键不落库，`uid+idempotency_key_digest` 唯一。状态为 `ready/applying/applied/action_required/deferred/failed`，决定类型为 `same_event/internal_transfer/refund_reversal/independent/defer/reopen`；同键异摘要拒绝，重放返回既有结果；
- `pf_reconciliation_transaction_link` 保存当前或历史决定产生的多来源证据关系：`link_id/uid/decision_id/row_id/transaction_id/relation_role/creation_method/rule_version/transaction_updated_unix_time/created`。角色为 `primary/transfer_counterpart/refund_original/refund_transaction`，创建方式为 `attached_existing/reconciliation_created`；活动关系必须通过 `case.current_decision_id` 读取，历史 revision 永不覆盖。阶段一 `pf_raw_row_transaction_link.posting_id` 语义不变，禁止伪造 posting；
- `pf_reconciliation_ledger_effect` 保存可撤销账本效果：`effect_id/uid/decision_id/transaction_id/effect_type/transaction_updated_unix_time/transaction_deleted_unix_time/created`，效果为 `created/soft_deleted/restored`；每个决定、交易和效果唯一。所有表均使用跨三库基础类型、非空唯一成员和 `uid` 隔离，不使用外键、数据库 JSON、部分索引、生成列或触发器；v003 继续采用冻结 bean、canonical checksum、可重入命名步骤、租约 fencing、取消传播和精确最终验模；
- case 状态为 `open/resolved/action_required/deferred`。候选生成先按 uid、金额、币种和有界时间窗口收窄，再以账本账户/支付方式、订单材料、方向/经济效果和规范化文本相似度解释；API 只返回稳定原因码和脱敏摘要。金额相同加时间接近可以贡献候选分，但绝不能单独产生自动账本效果；候选插入以唯一 case key 裁决，只刷新没有人工决定的 open case，不覆盖 resolved/deferred/action_required；
- 决策执行使用 privacy transaction、持久幂等记录和 `case.version` 条件更新。成员都指向同一完整正式事件时只补证据；恰有一个完整事件时校验 uid、未删除、转账双侧和 `updated_unix_time` 后附着；没有事件时可通过复用的 PF 账本草稿校验与 session 写入口创建一件正式事件；若成员已指向多个不同正式事件，则保存/返回 `action_required`，第一版绝不静默选择或删除其中一笔；
- `same_event` 和 `internal_transfer` 只允许附着一个既有事件或原子创建一个事件；`refund_reversal` 保留原交易和退款两件正式事件并建立角色关系，不按重复消费删除；`independent` 将参与行收口为已忽略并避免重叠导入重复询问；`defer` 不改变账本和原始行处理状态；所有结果都重新计算受影响批次的 pending/linked/ignored 状态，不把未确认数据写入正式统计；
- 安全撤销以新 `reopen` revision 留痕。附着既有交易的决定只撤销活动证据关系；只有 `reconciliation_created`、自快照后未修改/删除、没有其他活动 case、批次或未来贷款依赖且转账双侧完整的交易，才允许在同一账本事务内软删除并恢复原始行状态。任何共享、修改、缺失或多事件冲突都只返回影响和 `action_required`，不得自动改余额；核心账本只增加不依赖 PF 模型的窄 session 删除入口；
- API 固定为 `POST /personal_finance/reconciliation/candidates/generate.json`、`GET /cases/list.json`、`GET /cases/get.json`、`POST /cases/decide.json`、`GET /cases/undo_impact.json`、`POST /cases/undo.json`。桌面提供候选列表、原因、成员证据、决定和撤销影响；移动端阶段二只读。完成门禁覆盖 case 防重、人工决定不被规则刷新覆盖、跨 uid、同键重放/异摘要、并发 CAS、一个/零个/多个既有事件、转账双侧、退款不合并、独立/暂缓、撤销共享与修改拒绝、日志隐私和 SQLite/MySQL/PostgreSQL 一致性；全仓和三库只在 `REC-QA-201` 收口时运行一次。

### 0.20 2026-08-13 REC-QA-201 完成证据

- 后端只读验收基于 `82939408` 各执行一次：宿主 Go 1.26.5 下 `BUILD_PIPELINE=1 CHECK_3RD_API=0 go test ./...` 与 `go vet ./...` 通过；固定 Go 1.26.4 runner 下 `scripts/test-pf-db.sh all` 通过，SQLite、MySQL 8.4、PostgreSQL 17 均完成 v001→v003、v002→v003、精确验模和两次 `database update`。隔离 Compose 容器、网络、卷和 runner 镜像均已自动清理，正式 8080 容器全程未重启或替换；
- 前端只读验收在独立 worktree 完成 `npm ci`，锁文件未变化且审计为 0 个漏洞；`npm test` 共 9 个文件、38,579 项通过，`vue-tsc --noEmit`、全量非修复式 ESLint 和生产构建通过，`zh_Hans` / `en` locale 结构一致；
- 主负责人使用正式数据库和对象存储的只读复制件，在临时目录执行 v003 升级；SQLite `quick_check` 为 `ok`，用户、账户、交易和分类数量在升级前后保持不变。临时服务只监听 `127.0.0.1:8083`，数据库与对象存储均指向副本；
- 首次浏览器运行验收发现嵌套对账 TypeScript 模块未归入共享分块，随后过宽正则又误匹配 Vue SFC 的 `?lang.ts` 虚拟模块，都会导致登录页白屏。`b61c42da` 仅把真实 PF `.ts/.js` 源文件纳入 `common` 并排除 SFC 虚拟脚本；配置 lint、生产构建及桌面/移动登录与个人财务路由冒烟均通过，控制台无错误；
- DB/UI 两个只读验收任务均无代码提交，任务已归档，detached worktree 已在确认洁净后通过 `git worktree remove` 清理。阶段二业务边界未扩大：移动对账仍只读，金额加近时不自动合并，多个既有正式事件仍进入人工处理；
- 剩余风险明确：三库 tagged 集成覆盖 migration/importing，reconciliation 的并发、CAS 和回滚由 SQLite 业务集成测试覆盖；跨库结构、迁移和基础导入一致性已经三库验证。该边界不阻塞个人自托管阶段二发布，后续若增加数据库专有对账 SQL，必须补对应三库业务用例。

### 0.21 2026-08-13 DEPLOY-201 安全发布记录

- 从洁净 `4c662305` 构建 `ezbookkeeping-fork:2.0.0-pf2-4c662305`，镜像 digest 为 `sha256:9a2d5d0dd27e558c2d975e3fdbc4dc984881fa1d48f4f4caab996e6673a5069f`；镜像构建流程内再次通过 Go test/vet、9 个前端文件 38,579 项测试、类型/lint 和生产构建，镜像内前后端版本均为 `4c66230`；
- 先把正式 v002 数据/对象存储复制到独立候选卷，在候选卷执行 v003 后 SQLite `quick_check=ok`、PF 表 14 张，用户/账户/正式交易/分类数量保持 `1/6/7/81`；8083 候选镜像桌面和移动端均正常渲染、版本匹配、控制台零错误；
- 正式切换前停止阶段一容器，将数据、对象存储和只读配置完整复制到仓库外 `/Users/gaohongxiang/ezbookkeeping-production/backups/20260813-pf2-4c662305`；迁移前数据库 SHA-256 为 `1b6918f8615ad8e434726934db418cad8cc844708870fca7d6e1966879a449a8`，`quick_check=ok`、PF schema=v002、核心计数同上；
- 仅在备份完成后使用阶段二镜像把正式恢复数据卷推进到 v003。迁移后再次验证 `quick_check=ok`、PF schema=v003、14 张 PF 表和 `1/6/7/81` 核心计数；停服一致性命令报告文件、批次、行、孤立关系、缺失交易、内容异常、未登记最终对象和临时对象均为 0；
- 新容器 `ezbookkeeping` 复用原三个正式恢复命名卷、仓库外只读配置和 `unless-stopped`，以阶段二镜像接管 8080；HTTP 200，桌面登录及移动个人财务路由均正常渲染 `4c66230` 且控制台零错误，验收过程不读取或输出账务内容；
- 8083 候选容器及三个候选卷已删除，两个临时数据库核对目录移入系统垃圾桶；已停止的阶段一容器记录已删除，避免误启动旧二进制读取 v003。阶段一镜像 `ezbookkeeping-fork:2.0.0-pf1-37c27067` 与正式迁移前备份继续保留，构成明确回滚材料；正式恢复卷和阶段二镜像不得在阶段三开发中挂载。

### 0.22 2026-08-13 阶段二完成与阶段三正式交接

- 阶段二收口 `a7eb68ff5720e137d833ff8c217338a5a8831577` 已 fast-forward 到本地 `main` 并推送 `origin/main`；原 `codex/pf-phase-2` 已在确认合入后通过 `git branch -d` 清理。BANK/REC-DB/REC-CAND 的三个无 worktree 本地执行分支因采用等价集成提交而不是祖先关系，仅保留为已完成恢复引用，不是活动任务，也不得继续开发或推送；
- 第二任主负责人任务为 `019ff7d0-894f-7421-8c8e-2b7bc746ff3e`，工作区 `/Users/gaohongxiang/.codex/worktrees/3fa6/ezbookkeeping`；完成本节提交并释放 `codex/pf-phase-3` 后归档，不再修改阶段三文件或看板；
- 第三任主负责人任务为 `019ffb71-b3f8-7b20-a464-58b99b775752`，独立 worktree `/Users/gaohongxiang/.codex/worktrees/d83f/ezbookkeeping`，创建时为 detached `a7eb68ff`。正式接管命令是先确认旧负责人已经释放分支，再在该 worktree 执行 `git switch codex/pf-phase-3`、`git status --short --branch`、`git log -3 --oneline --decorate` 和 `git worktree list --porcelain`；本节所在提交即完整交接状态，不依赖聊天摘要；
- 交接时没有未提交代码、待合并执行提交、活动 QA 容器或候选卷；生产、备份、测试证据和唯一剩余任务都已写入 0.5、0.20、0.21 与本节。新主负责人不得重复阶段二实现或全量门禁，直接从贷款需求与预评审开始冻结阶段三公共契约；
- 协作策略保持不变：主负责人 GPT-5.6-sol / 最高；执行任务只用 GPT-5.6-sol 的中等、高或最高，不用极高/ultra；可并行的持续实现使用侧边栏独立任务、分支和 worktree，完成即归档；按风险运行最小充分验证，不做过度审计，但主负责人必须从全局架构最优角度评审和集成。

### 0.23 2026-08-13 LOAN-301 公共契约冻结

本节由第三任主负责人在 `main@a7eb68ff` 之上串行冻结，是阶段三 v004、纯计算、合同状态、正式交易分配和 REST/DTO 的共同实现边界。`LOAN-CONTRACT-301` 的只读预评审与参考仓库只作为输入；实现与验收以本节为准。需要改变表、状态、现金流符号、算法口径或正式账本语义时，执行任务必须停工，由主负责人先更新本文。

#### 0.23.1 首版范围与明确边界

- 首版支持 `credit_card_installment / bank_loan / consumer_loan / personal_loan`，类型只影响展示和资金取得方式，不用于产品推荐或风险评价；
- 计算只支持每月一期、`flat / equal_payment / equal_principal / interest_only` 四种标准还款法，以及 `rate / repayment` 两种输入依据；报价口径为 `annual / monthly / daily / installment`，`installment` 只允许 `flat`；
- 支持一次性必付费用、每期必付费用、利息折扣、每期减免和总额减免。金额始终使用 ezBookkeeping 最小货币单位整数，所有持久化比例使用 pptr 定点整数，`1.0 = 10^12`；
- 首版不做自定义计划、不规则还款日期、XIRR、提前还款重排、浮动利率、自动代扣或综合交易自动替换。额外本金超过当前计划待还时返回 `action_required`，不得静默重排未来计划；未来 XIRR 必须使用独立命名口径，不能替换本节月度 IRR；
- 合同创建不产生实际余额。`Transaction`、`Account`、分类和标签继续是余额、消费和现金流唯一权威；贷款域只保存合同、不可变计算 revision、逐期计划、动作审计与正式交易关系；
- 负债账户数据库余额沿用核心账本符号，实际未偿负债展示为该负债账户余额的相反数。合同计划剩余本金与账户实际负债必须分别展示；同一负债账户可承载信用卡其他消费或多份分期，不能用单份合同覆盖账户余额。

#### 0.23.2 v004 六表冻结结构

v004 只新增以下六张表，不修改已发布 v001～v003 文件、字段、索引或 checksum。所有用户表都带 `uid`；不使用外键、触发器、数据库 JSON、部分索引、生成列、`UNSIGNED` 或浮点字段。ID、金额、计数、版本和 Unix 时间使用 `BIGINT`，民用日期使用严格 `YYYY-MM-DD` 的 `CHAR(10)`，比例使用 `BIGINT` pptr。唯一索引的全部成员非空，所有索引名保持 ASCII 且不超过 63 字符。

`pf_loan_contract` 保存合同身份和生命周期，不复制正式账户余额：

| 字段 | 冻结语义 |
| --- | --- |
| `contract_id`、`uid` | `BIGINT NOT NULL`；`contract_id` 为 PK |
| `name`、`lender_name` | `VARCHAR(128) NOT NULL` |
| `contract_type`、`status`、`close_reason_code` | `VARCHAR(32) NOT NULL`；无关闭原因使用空字符串 |
| `liability_account_id` | `BIGINT NOT NULL`；必须是当前 uid、未删除、单一且非隐藏的信用卡或债务账户 |
| `default_payment_account_id?` | `BIGINT NULL`；存在时必须是当前 uid、未删除、单一且非隐藏的资产账户 |
| `currency` | `CHAR(3) NOT NULL`；与上述账户币种一致 |
| `note` | `VARCHAR(255) NOT NULL` |
| `version`、`current_revision_id` | `BIGINT NOT NULL`；所有写操作使用 `version` CAS |
| `created_unix_time`、`updated_unix_time` | `BIGINT NOT NULL` |
| `closed_unix_time?` | `BIGINT NULL` |

索引固定为 `IDX_pf_loan_contract_uid_status_updated(uid,status,updated_unix_time,contract_id)` 和 `IDX_pf_loan_contract_uid_liability(uid,liability_account_id,status,contract_id)`。

`pf_loan_contract_revision` 追加式保存输入、算法版本和结果；旧 revision 永不覆盖：

- 身份与链：`revision_id PK`、`uid`、`contract_id`、`revision_number`、`previous_revision_id?`、`action_id`；
- 日期与方式：`effective_date`、`contract_date`、`first_due_date`、`funding_type`、`input_mode`、`repayment_method`、`rate_quote_type`、`frequency_type`、`frequency_interval`；首版 frequency 固定 `monthly / 1`，资金类型固定 `cash_disbursement / purchase_installment`；`rate` 模式必须使用四个稳定报价码之一，`repayment` 模式没有报价口径并以非 NULL 空字符串持久化 `rate_quote_type`；
- 输入金额：`principal_amount`、`actual_disbursement_amount`、`upfront_fee_amount`、`per_period_fee_amount`、`payment_basis_amount?`、`term_count`；`actual_disbursement_amount` 固定表示扣除全部起始必付费用后的 t0 净流入，并强制 `actual_disbursement_amount = principal_amount - upfront_fee_amount > 0`，IRR 不得再次扣一次 upfront fee；
- 利率与优惠：`quoted_rate_pptr?`、`discount_type`、`discount_rate_pptr?`、`discount_amount`。`discount_type` 稳定码固定为 `none / interest_rate / per_period / total`；利息折扣使用 `discount_rate_pptr`，每期/总额减免使用 `discount_amount`，其他组合必须为 NULL/0；
- 版本与摘要：`calculation_version`、`rounding_version`、`irr_version` 均 `VARCHAR(32) NOT NULL`，`schedule_digest CHAR(64) NOT NULL`；
- 汇总结果：`pre_discount_total_payment_amount`、`pre_discount_total_cost_amount`、`total_payment_amount`、`total_interest_amount`、`total_fee_amount`、`total_discount_amount`、`total_cost_amount`、`cost_ratio_pptr` 均 `BIGINT NOT NULL`；`total_payment_amount` 包含 upfront fee 和全部逐期实付，`total_cost_amount = total_interest_amount + total_fee_amount`；
- IRR：`irr_status VARCHAR(32) NOT NULL`，`monthly_irr_pptr? / simple_apr_pptr? / effective_apr_pptr? BIGINT NULL`；无解不得写 0；
- `created_unix_time BIGINT NOT NULL`。

唯一索引固定为 `UQE_pf_loan_revision_uid_number(uid,contract_id,revision_number)` 与 `UQE_pf_loan_revision_uid_action(uid,action_id)`；普通索引为 `IDX_pf_loan_revision_uid_contract_created(uid,contract_id,created_unix_time,revision_id)`。

`pf_loan_installment` 保存某一 revision 的不可变逐期结果：

- `installment_id PK`、`uid`、`contract_id`、`revision_id`、`installment_number`、`due_date`、`created_unix_time`；
- `beginning_principal_amount`、`principal_amount`、`interest_amount`、`fee_amount`、`discount_amount`、`payment_amount`、`ending_principal_amount`；
- `pre_discount_interest_amount`、`pre_discount_fee_amount`、`pre_discount_payment_amount`；
- 唯一索引 `UQE_pf_loan_installment_uid_revision_number(uid,revision_id,installment_number)`；普通索引 `IDX_pf_loan_installment_uid_contract_due(uid,contract_id,due_date,installment_id)` 和 `IDX_pf_loan_installment_uid_revision_number(uid,revision_id,installment_number,installment_id)`。

有效计划只由 `contract.current_revision_id` 选择，不在 installment 行中维护 `active/superseded` 双重事实。首版只在合同没有任何活动正式交易分配时允许创建新计算 revision；已有分配必须先显式撤销，因此不需要跨 revision 猜测稳定期次或改写已结算历史。

`pf_loan_action` 保存持久幂等命令：

- `action_id PK`、`uid`、`contract_id`、`expected_contract_version`、`applied_contract_version`、`action_type`、`previous_action_id?`；
- `idempotency_key_digest CHAR(64)`、`idempotency_key_version VARCHAR(32)`、`request_digest CHAR(64)`、`request_digest_version VARCHAR(32)`；原始幂等键不落库；
- `status VARCHAR(32)`、`reason_codes_json TEXT`、`error_code VARCHAR(64)`；JSON 只允许稳定码数组，不保存名称、机构、备注、账户或账单内容；
- `created_unix_time`、`updated_unix_time` 非空，`started_unix_time? / completed_unix_time? / failed_unix_time?` 可空；
- 唯一索引 `UQE_pf_loan_action_uid_key(uid,idempotency_key_digest)`；普通索引 `IDX_pf_loan_action_uid_contract_created(uid,contract_id,created_unix_time,action_id)` 和 `IDX_pf_loan_action_uid_status_updated(uid,status,updated_unix_time,action_id)`。

`pf_loan_transaction_binding` 是正式交易并发唯一锚点：

- `binding_id PK`、`uid`、`transaction_id`、`current_allocation_id?`、`version`、`created_unix_time`、`updated_unix_time`；
- `UQE_pf_loan_binding_uid_transaction(uid,transaction_id)` 由数据库最终裁决同一正式交易行不能同时属于两份活动贷款分配；
- `IDX_pf_loan_binding_uid_current(uid,current_allocation_id,binding_id)` 支持依赖检查。转账的主侧和对端各有一个 binding，并通过同一 allocation 绑定，防止从另一侧重复分配。

`pf_loan_transaction_allocation` 保存当前和历史分配；撤销后旧行保留：

- `allocation_id PK`、`uid`、`contract_id`、`installment_id?`、`primary_binding_id`、`counterpart_binding_id?`；
- `component_type VARCHAR(32)`：`disbursement / principal / interest / fee`；`disbursement` 的 installment 必须 NULL；`fee` 在 installment 为 NULL 时唯一表示当前 revision 的一次性 upfront fee，在 installment 非空时表示该期费用；`principal / interest` 必须绑定当前 revision 的具体 installment；
- `allocated_amount BIGINT`、`creation_method VARCHAR(32)`：`attached_existing / loan_created`；
- `status VARCHAR(32)`：`active / reversed / action_required`；
- `transaction_updated_unix_time`、`counterpart_updated_unix_time?` 保存校验快照，不替代正式交易字段；
- `created_action_id`、`last_action_id`、`created_unix_time`、`updated_unix_time`；
- 普通索引为 `IDX_pf_loan_allocation_uid_contract_status(uid,contract_id,status,updated_unix_time,allocation_id)`、`IDX_pf_loan_allocation_uid_installment(uid,contract_id,installment_id,status,allocation_id)` 和 `IDX_pf_loan_allocation_uid_action(uid,created_action_id,allocation_id)`。

#### 0.23.3 状态、进度与并发

- 合同状态固定为 `active / closed / cancelled`。创建即 active；无任何分配时可 cancel；close 原因为 `paid_off / manual_close / written_off`，后两者必须返回剩余计划和账本差异；closed 可通过显式 reopen 恢复 active；
- action 类型固定为 `create_contract / revise_contract / apply_settlement / reverse_settlement / close_contract / reopen_contract / cancel_contract`，状态为 `ready / applying / applied / action_required / failed`；同键同摘要稳定重放，同键异摘要拒绝；
- installment 的结算状态由活动 allocation 聚合派生：零分配为 `unpaid`，存在分配且仍有应还为 `partial`，本金/利息/费用全部结清为 `paid`；`overdue = due_date < as_of_date && outstanding_payment > 0`，不得靠定时任务永久写入；
- allocation 通过 contract version CAS、binding version/当前指针条件更新和唯一约束共同裁决。非空 installment 的一次 settlement action 可原子处理同一期的本金、利息和费用多个组件；NULL installment 的 action 在 `cash_disbursement` 下只允许到账本金和/或 upfront fee，在 `purchase_installment` 下只允许 upfront fee；任一组件失败，正式交易、binding、allocation、action、合同 version 全部回滚；
- 同一期允许多笔正式交易形成部分还款；一笔逻辑正式事件首版只能承担一个组件并全额分配，不能虚拟拆分金额。各组件活动分配总额不得超过计划金额；超过本金或任何额外本金一律 `action_required`；
- 撤销 settlement 只撤销活动关系并保留 action/allocation 历史，不自动删除正式交易。贷款服务创建但需要删除的交易仍由用户在明确影响后使用核心账本流程处理；
- 查询、聚合和条件更新全部同时限定 `uid`。合同名称、机构、金额、计划、幂等摘要和交易快照只经 privacy session；普通日志只允许技术 ID、稳定错误码和脱敏计数。

#### 0.23.4 四种还款法、优惠、舍入与 IRR

冻结版本名为 `loan-calculation-v1 / loan-rounding-half-up-v1 / periodic-irr-v1`。实现必须在本仓库重建纯 Go 计算内核和脱敏金标，不建立参考仓库运行时依赖。

- 报价周期率：annual 除以 12，monthly 原值，daily 使用 360 天报价口径后除以 12，installment 原值且只适用于 flat；
- `flat`：每期本金按期数分摊，固定总利息按本金×周期率×期数计算；按还款额输入时，总利息为每期应还×期数－本金；
- `equal_payment`：按周期率使用年金公式；按还款额输入时，对 `[principal, -payment…]` 求月 IRR 并以用户输入作为非末期应还；
- `equal_principal`：每期本金均分，利息按期初剩余本金；按还款额输入时以首期应还减首期本金反推周期率；
- `interest_only`：非末期本金为零，末期归还全部本金；按还款额输入时该值表示每期利息/费用并反推周期率；
- 所有中间比例用 `math/big` 的整数/有理数实现，禁止二进制浮点进入金额或持久结果。正金额以 half-up 舍入到最小货币单位；非末期按公式舍入，末期收口剩余本金和固定总利息。必须满足本金守恒、每期 `payment = principal + interest + fee`、末期余额为零；
- 利息折扣是周期率乘折扣因子并重新生成实付计划，equal_payment 必须重新摊还；每期/总额减免只减少费用与利息，先减 fee 再减 interest，绝不减少本金。总额减免按每期优惠前成本比例分配，末期吸收分币尾差；
- IRR 现金流固定为 `CF0 = actual_disbursement_amount`，后续为每期实付的负数；upfront fee 已体现在净到账，不再重复扣除。只接受一个正流入、其后非正的传统现金流；
- 月 IRR 在 pptr 整数域 `[0, 2×10^12]` 二分，使用固定上界和有理数 NPV 符号，直到相邻 pptr；零成本为 `solved_zero` 且写 0，正常根为 `solved`，无非负根、现金流不足或超范围分别为 `no_nonnegative_root / insufficient_cashflows / out_of_range` 且三个率字段为 NULL；
- `simple_apr = monthly_irr × 12`，`effective_apr = (1 + monthly_irr)^12 - 1`，最终 half-up 到 pptr。金标至少覆盖 50,000、12 期、equal_payment 每期 4,460.59 的简单年化约 12.78%、有效年化约 13.55%，以及四方式×两依据×费用/三优惠矩阵；
- 月度日期从显式 first_due_date 按原始日锚点逐月计算，目标月不足该日时取月末；使用民用日期，不受服务器时区或 DST 影响。

#### 0.23.5 正式账本语义与安全依赖

- `cash_disbursement` 到账必须关联或创建“合同负债账户 → 到账资产账户”的完整 transfer，不是 income；transfer 金额固定为 `principal_amount`，使负债增加合同本金。`actual_disbursement_amount` 只表示 IRR 的 t0 净流入；当 `upfront_fee_amount > 0` 时另以 installment 为 NULL 的 `fee` expense 关联或创建，因此全部资产净增加为实际到账。`purchase_installment`（信用卡原消费转分期）不创建到账交易，也不新增第二笔消费，但存在 upfront fee 时仍可只建立该笔独立费用关系；
- principal 必须关联或创建“还款资产账户 → 合同负债账户”的完整 transfer；interest、每期 fee 和 upfront fee 必须各自关联或创建 expense。分类、标签、账户、币种和金额继续实时读取正式交易；
- 综合扣款若当前只有一笔 expense，不能仅在 PF 表中虚拟拆成本金/利息/费用。首版要求用户先拆成语义正确的正式事件，或由 settlement action 在没有重叠既有事件时原子创建多个组件事件；不自动软删或替换既有综合交易；
- 对既有事件分配前校验 uid、未删除、类型、账户、币种、金额、`updated_unix_time` 和转账双侧；贷款创建事件复用现有 caller-owned privacy transaction 账本入口，不在核心模型增加 PF 字段；
- `REC-201` 自动撤销检查必须把任何 `current_allocation_id IS NOT NULL` 的贷款 binding 视为活动依赖并返回稳定原因 `loan_relation_present`。既有 posting/reconciliation 关系不得因贷款分配被伪造或覆盖；
- 活动 allocation 所指正式交易被修改、删除或转账缺侧时，读取返回 `action_required` 和稳定原因，不把计划自动标为已还，也不自动修复余额。

#### 0.23.6 REST/DTO 冻结边界

固定端点：

```text
POST /api/v1/personal_finance/loans/calculate.json
GET  /api/v1/personal_finance/loans/contracts/list.json
GET  /api/v1/personal_finance/loans/contracts/get.json
POST /api/v1/personal_finance/loans/contracts/create.json
POST /api/v1/personal_finance/loans/contracts/revise.json
POST /api/v1/personal_finance/loans/contracts/close.json
POST /api/v1/personal_finance/loans/contracts/reopen.json
POST /api/v1/personal_finance/loans/contracts/cancel.json
GET  /api/v1/personal_finance/loans/settlements/candidates.json
POST /api/v1/personal_finance/loans/settlements/apply.json
GET  /api/v1/personal_finance/loans/settlements/undo_impact.json
POST /api/v1/personal_finance/loans/settlements/undo.json
```

- calculate/create/revise 共享同一规范计算输入，业务日期显式包含 `effectiveDate / contractDate / firstDueDate`；`effectiveDate` 表示实际放款或分期生效日，不得由 API 静默替换为合同日。calculate 不持久化，create/revise 只接受业务输入、expected version 和技术幂等键，不接受内部 ID、摘要、状态、汇总或算法版本；
- list 使用 `status + updated_unix_time + contract_id` 稳定游标；get 返回合同、当前 revision、逐期计划、活动分配聚合、账本负债与合同计划差异；未确认计划不进入正式现金流统计；
- candidates 只在固定有界日期/金额/账户条件内返回当前 uid 的正式交易脱敏摘要、稳定原因、主交易 `updatedUnixTime` 及 transfer 可空对端快照时间，不自动分配；
- settlement apply 接受同一结算上下文的组件数组：非空 installment 只允许该期 `principal / interest / fee`，NULL installment 按资金类型只允许到账本金与 upfront fee 的冻结组合；每个组件必须二选一提供既有事件引用或受限 ledger draft。既有事件引用固定包含 `existingTransactionId + expectedUpdatedUnixTime + expectedCounterpartUpdatedUnixTime?`，所有 ledger draft（包括 transfer）都显式包含当前 uid 的 `categoryId`，不得任意选择默认分类。服务端在同一隐私事务中重新验证 component、账户、类别、金额和主/对端快照，不信任前端候选；
- undo impact 只返回聚合计数、能否撤关系和稳定原因，不返回其他合同/批次/对账细节；undo 以追加 action 和 contract/binding CAS 撤销；
- JSON ID 使用字符串；金额保持最小货币单位整数；pptr 比例使用十进制字符串避免 JavaScript 精度损失；日期使用 `YYYY-MM-DD`。API 不返回 uid、内部摘要、原始幂等键、交易备注、机构原文以外的账户敏感信息或 action 内部错误；
- 桌面端完成计算、合同、计划、候选、分配与撤销闭环；移动端阶段三只读合同/进度/下一期摘要。所有新增文案维护 `zh_Hans` 与 `en`，服务端状态机和会计校验是最终裁决。

#### 0.23.7 迁移、拆分与最小门禁

- v004 每表一个命名、可重入步骤，复用现有 migration ledger、租约 fencing、MySQL 锁后数据库时间、取消传播、冻结 bean、canonical checksum 与精确最终验模；旧二进制可忽略 v004，正式交易不会随贷款表回退自动撤销；
- 契约冻结后可并行 `LOAN-CALC-301`、`LOAN-DB-301` 和 `LOAN-UI-SHELL-301`，三者文件所有权不得重叠；服务、分配、API、完整 UI、QA 与发布按 0.5 节依赖顺序推进；只有主负责人修改全局看板、共享契约和集成分支；
- CALC 最小门禁是参考金标与纯函数不变量；DB 最小门禁是迁移/模型/仓储聚焦测试和一次 SQLite 协议；UI shell 最小门禁是 feature 状态测试、类型检查和受影响文件非修复式 lint。不得提前重复阶段二全仓或三库门禁；
- 最终 QA 一次覆盖 v001→v004、v003→v004、重复升级、漂移拒绝、三库精确结构、同键重放/异摘要、contract/binding 并发、事务整体回滚、跨 uid、日志隐私、正式交易修改/删除、转账缺侧、REC 撤销贷款依赖、本金不计消费、利息/费用计支出、信用卡分期不重复消费、桌面完整与移动只读浏览器流程；
- DEPLOY-301 只使用正式数据库和对象存储的一致性副本演练 v004，阶段三开发与测试绝不挂载正式 8080 恢复卷。阶段完整验收、合入 `main`、工作树洁净后再按用户授权安全推送 `origin/main` 并发布，不推送阶段内分支。

## 1. 根本目标

本项目不再以“尽可能维持一个接近官方、可随时丢弃的补丁集”为目标，而采用长期受控 fork：

- `origin/main` 是产品、数据结构和部署的权威主线；
- 官方 ezBookkeeping 的 `upstream/main` 是持续供给源，不是本项目的产品主线；
- 安全修复、基础能力和有价值的上游改进经过评估后吸收；
- 不为追求与上游零差异而牺牲个人财务领域模型的正确性；
- 仍通过清晰的模块边界和少量接入点，把长期同步成本控制在可维护范围内。

目标关系：

~~~mermaid
flowchart LR
    U["官方 upstream/main"] -->|"评估与筛选"| S["上游同步分支"]
    S -->|"冲突处理、测试、迁移演练"| M["origin/main 产品主线"]
    M --> R["版本标签与自托管部署"]
    M --> F["短期功能分支"]
    F -->|"评审与验收"| M
~~~

## 2. 架构决策摘要

### 2.1 采用模块化单体

继续保持一个 Go 后端、一个 Vue/PWA 前端、一套用户数据库和一个部署单元，不拆分微服务。

理由：

- 确认入账必须让正式交易、账户余额、原始证据关联和批次状态处于同一数据库事务；
- 个人自托管不需要承担分布式事务、服务发现和多套部署的成本；
- 现有认证、权限、配置、数据库兼容和对象存储可以继续复用；
- 模块边界可以在单体内部实现，不需要依靠网络边界。

### 2.2 采用命名隔离的个人财务域

新增能力统一放入 `personalfinance` 代码命名空间、`pf_` 数据表前缀和独立前端 feature，避免与未来上游同名功能碰撞。

### 2.3 核心账本保持权威

现有 `Transaction`、`Account`、分类和标签继续作为实际余额、消费、收入和现金流的唯一口径。

个人财务域负责保存证据、计划和关系，不复制一套正式账本，也不把几十个来源字段塞入 `Transaction`。

### 2.4 依赖方向单向

个人财务域可以通过窄适配器调用现有账本；现有核心账本不得导入或理解个人财务域模型。

### 2.5 数据演进独立版本化

个人财务表从第一版建立迁移版本和校验记录。XORM Sync2 可以辅助全新安装，但不能成为后续重命名、回填和约束变更的唯一机制。

### 2.6 来源账户与账本账户分离

支付宝、微信等“账单属于谁”是来源身份；银行卡、信用卡、余额账户等“钱记到哪里”是账本账户。二者不能混成同一个字段。

阶段 1 即持久化来源账户档案。它为跨文件去重和账期覆盖提供稳定边界；到正式入账时，再通过默认映射和逐行覆盖选择账本账户。修改账本映射不得改变历史来源身份。

## 3. 逻辑架构

~~~mermaid
flowchart TB
    subgraph FE["Vue / PWA"]
        IUI["导入工作台与历史"]
        RUI["对账工作台"]
        LUI["贷款与分期"]
        DUI["可信财务总览"]
    end

    subgraph PF["Personal Finance 扩展域"]
        API["个人财务 API"]
        IMP["导入应用服务"]
        PARSER["来源证据解析器"]
        DEDUP["文件与来源身份去重"]
        POST["确认入账编排"]
        REC["跨来源人工对账"]
        LOAN["贷款合同与计划"]
        DASH["覆盖率与总览查询"]
        MIG["PF 版本化迁移"]
    end

    subgraph ADAPTER["边界适配器"]
        LEDGER["账本写入适配器"]
        FILES["原文件存储适配器"]
    end

    subgraph CORE["ezBookkeeping 上游核心"]
        AUTH["用户与权限"]
        TX["Transaction / Account"]
        META["Category / Tag"]
        DB["Datastore / XORM"]
        STORAGE["对象存储"]
    end

    IUI --> API
    RUI --> API
    LUI --> API
    DUI --> API
    API --> IMP
    API --> REC
    API --> LOAN
    API --> DASH
    IMP --> PARSER
    IMP --> DEDUP
    IMP --> POST
    POST --> LEDGER
    IMP --> FILES
    LEDGER --> TX
    FILES --> STORAGE
    PF --> DB
    API --> AUTH
    PF --> META
~~~

禁止出现的反向依赖：

```text
pkg/services/transactions.go  -> personalfinance/importing
pkg/models/transaction.go     -> PFImportBatch 或 PFRawImportRow
通用报表核心                  -> 原始账单表
```

允许的依赖：

```text
personalfinance/importing -> 窄账本接口 -> 现有 TransactionService
personalfinance/importing -> 对象存储接口 -> 现有 storage.Container
personalfinance/querying  -> 只读账本接口 -> 现有账本查询
```

接口由个人财务应用层定义，适配器由组合根注入。适配器可以依赖现有账本服务，个人财务应用层不能反向导入适配器实现，从而避免包循环并让领域测试使用内存替身。

## 4. 建议物理结构

后端：

```text
pkg/personalfinance/
  importing/
    model.go
    status.go
    identity.go
    fingerprint.go
    parser.go
    service.go
    posting.go
    repository.go
  reconciliation/
  loans/
  dashboard/
  migrations/

pkg/services/
  personal_finance_ledger_adapter.go
  personal_finance_storage_adapter.go

pkg/api/
  personal_finance_imports.go
  personal_finance_reconciliation.go
  personal_finance_loans.go
```

来源解析代码保留在对应来源包内，以便旧导入器与新证据解析器共享低层提取逻辑：

```text
pkg/converters/alipay/
  ...现有文件
  alipay_import_evidence_parser.go

pkg/converters/wechat/
  ...现有文件
  wechat_import_evidence_parser.go
```

`PARSE-101` 与 `PARSE-102` 并行时分别只拥有 `alipay/**` 与 `wechat/**` 及各自测试夹具。现有公共 CSV/XLSX reader 会跳过空行或 trim 单元格，不能直接满足证据契约；两个执行会话不得同时修改公共 reader。首批各自在来源目录实现薄适配，确需抽取公共物理行读取器时，由主负责人串行冻结后再共享。

前端：

```text
src/features/personal-finance/
  models/
  services/
  stores/
  import/
  reconciliation/
  loans/
  dashboard/
```

上游公共文件只保留薄接入点：

- `cmd/database.go`：调用一次 PF 迁移入口；
- `cmd/webserver.go`：调用一次 PF 路由注册入口；
- 前端 router/menu：增加 feature 入口；
- 交易服务：提供一个可参与外部现有事务的窄写入能力；
- locale：个人财务独立命名空间。

## 5. 阶段 1 数据架构

### 5.1 关系模型

~~~mermaid
erDiagram
    PF_IMPORT_FILE ||--o{ PF_IMPORT_BATCH : parsed_as
    PF_SOURCE_ACCOUNT ||--o{ PF_IMPORT_BATCH : scopes
    PF_SOURCE_ACCOUNT ||--o{ PF_SOURCE_IDENTITY : scopes
    PF_IMPORT_BATCH ||--o{ PF_RAW_IMPORT_ROW : contains
    PF_SOURCE_IDENTITY o|--o{ PF_RAW_IMPORT_ROW : identifies
    PF_IMPORT_BATCH ||--o{ PF_IMPORT_POSTING : posts_with
    PF_RAW_IMPORT_ROW ||--o{ PF_RAW_ROW_TRANSACTION_LINK : evidences
    TRANSACTION ||--o{ PF_RAW_ROW_TRANSACTION_LINK : supported_by
~~~

### 5.2 `pf_import_file`

职责：代表当前用户上传的一份唯一文件内容。

关键字段：

- `file_id`、`uid`；
- 原文件名、文件大小、SHA-256；
- MIME/扩展名和内容存储状态；
- 对象存储中的不透明路径；
- 上传时间、内容删除时间；
- 创建 IP 仅按现有安全规则保留，不记录文件内容。

约束：

- `uid + file_sha256` 唯一；
- 文件名不是身份；
- 删除原文件内容不删除哈希、批次和原始行；
- 对象路径不得包含原始文件名、账号或卡号。

### 5.3 `pf_source_account`

职责：代表当前用户在某一外部来源中的稳定账户档案，是同来源去重和账期覆盖的作用域；它不是正式账本账户。

关键字段：

- `source_account_id`、`uid`、`source_type`；
- 稳定且不含明文敏感信息的 `source_account_key` 及 `source_account_key_version`；
- 用户可识别的脱敏名称；
- 可选默认 `ledger_account_id`；
- 来源标识发现方式、状态、创建和更新时间。

约束：

- `uid + source_type + source_account_key` 唯一；
- 文件提供可靠账户标识时，使用 `source-account-key-v1 + source_type + 规范化标识` 的长度前缀编码后再做 SHA-256；文件不提供时，由用户选择或创建持久档案，不能只凭昵称或尾号静默合并；
- 掩码账号、尾号和微信昵称只能进入脱敏展示候选，公共判别校验器禁止据此计算跨文件 key；
- 修改名称或默认账本映射不改变 `source_account_key`；
- 一个来源账户可以按原始行覆盖默认账本账户；
- 不保存完整卡号、手机号或其他不必要的账户明文。

### 5.4 `pf_import_batch`

职责：代表一次解析运行；同一文件可以显式产生多个解析批次。

关键字段：

- `batch_id`、`uid`、`file_id`、可空 `source_account_id`；
- 来源类型快照和可选账本账户候选；
- parser 名称、parserVersion、normalizationVersion；
- 解析选项摘要和显式重解析原因；
- 账期开始、结束；
- 总行数、有效数、无效数、精确重复数、身份冲突数、待确认数、已入账数；
- 状态、错误码和脱敏错误摘要；
- 创建、开始、完成和更新时间。

同一文件普通重传返回已有文件及最近批次；只有用户显式选择重新解析才新增批次。
当账单只给掩码账号、展示昵称或完全缺少来源账户证据时，批次先进入 `awaiting_source_account`，用户选择持久来源账户后才进行身份计算。

### 5.5 `pf_source_identity`

职责：代表“同一个来源账户中的一个稳定原始交易身份”，是同来源并发去重的数据库锚点。

关键字段：

- `identity_id`、`uid`、`source_account_id`；
- `identity_kind`：来源交易号、订单组合、强指纹或批次局部；
- `source_identity_key`；
- 来源语义不变量的 `source_core_digest`；
- `identity_key_version`、`core_digest_version`、`fingerprint_version`；
- 首次和最近出现时间。

约束：

- `source_identity_key` 始终非空；
- `uid + source_identity_key` 唯一；
- 不依赖可空复合唯一索引；
- 只有身份相同且来源语义不变量兼容才是精确重复；相同身份出现不兼容金额、币种或方向时必须进入冲突态；
- 来源身份不受用户后来修改账本账户映射的影响。

### 5.6 `pf_raw_import_row`

职责：保存每次解析看到的每一条原始证据，包括无效行和精确重复行。

关键字段：

- `row_id`、`uid`、`batch_id`、原始行号；
- 可选 `identity_id`；
- 来源交易号、订单号、商户单号；
- 原始时间、金额、方向、状态、商户、商品、支付方式；
- 标准化 Unix 秒、UTC offset 分钟、非负金额、币种、方向、来源交易类型、经济效果和账户候选；
- 本行观察到的来源 identity key/core digest，身份冲突时也不丢失候选证据；
- 版本化有序原始字段 JSON 与受限问题数组 JSON；
- parser、normalization、identity key、core digest、fingerprint、raw snapshot 六类版本；
- 解析状态、中心语义资格、去重状态、最终处置、入账状态和主问题码；
- 创建时间。

约束：

- `uid + batch_id + row_number` 唯一；
- 原始快照写入后不可覆盖；
- 无效行使用 `identity_state=not_evaluated`，不伪造来源身份；
- 服务层限制单行和整批快照大小；
- 快照和完整标识不得写入普通日志。

### 5.7 `pf_import_posting`

职责：持久化一次确认入账命令、幂等结果和审计信息。

关键字段：

- `posting_id`、`uid`、`batch_id`；
- 客户端幂等键摘要、摘要版本及请求内容摘要/版本；原始幂等键不持久化；
- 状态、选中行数、创建交易数、复用交易数；
- 开始、完成、失败时间和错误码。

约束：

- `uid + idempotency_key_digest` 唯一；
- 同一个键但请求摘要不同必须拒绝；
- `ClientSessionId` 可继续防双击，但不能替代该表。

### 5.8 `pf_raw_row_transaction_link`

职责：建立原始证据与正式交易之间的多对多关系。

关键字段：

- `link_id`、`uid`、`row_id`、`transaction_id`；
- 关系角色和创建方式；
- 创建该关系的 posting、规则版本和时间；
- 创建正式交易时的 `updated_unix_time` 快照，用于安全撤销判断。

约束：

- `uid + row_id + transaction_id + relation_role` 唯一；
- 所有读取和写入同时限定 `uid`；
- 不依赖数据库级跨分片外键，服务层必须验证双方归属。

### 5.9 DB-101 的 v001 冻结契约

DB-101 只创建阶段 1A 所需的五张业务表；`pf_import_posting` 和 `pf_raw_row_transaction_link` 留到 POST-101 的后续迁移。所有 ID、金额、大小、计数和 Unix 时间均使用有符号 `BIGINT`，时区偏移使用 `SMALLINT`，状态使用小写 `VARCHAR(32)`，保证始终为完整 SHA-256 的字段使用小写 `CHAR(64)`，允许空字符串哨兵的币种和 observed digest 使用 `VARCHAR`，原始快照使用 `TEXT`。不使用外键、级联、触发器、`UNSIGNED`、数据库 JSON 类型、部分索引或生成列。

字段末尾 `?` 表示数据库允许 NULL；其余字段必须 `NOT NULL`。唯一索引的所有成员始终非空。

| 表 | v001 字段 |
| --- | --- |
| `pf_import_file` | `file_id`、`uid`、`original_file_name`、`file_size`、`file_sha256`、`mime_type`、`file_extension`、`content_state`、`storage_object_key`、`created_ip`、`created_unix_time`、`updated_unix_time`、`content_deleted_unix_time?` |
| `pf_source_account` | `source_account_id`、`uid`、`source_type`、`source_account_key`、`source_account_key_version`、`masked_display_name`、`ledger_account_id?`、`discovery_method`、`status`、`created_unix_time`、`updated_unix_time` |
| `pf_import_batch` | `batch_id`、`uid`、`file_id`、`source_account_id?`、`status`、`source_type_snapshot`、`ledger_account_id?`、`parser_name`、`parser_version`、`normalization_version`、`identity_key_version`、`core_digest_version`、`fingerprint_version`、`raw_snapshot_version`、`parse_options_digest`、`reparse_reason_code`、`statement_start_unix_time?`、`statement_end_unix_time?`、`statement_timezone_utc_offset?`、七类行数、`error_code`、`error_summary`、`created_unix_time`、`started_unix_time?`、`completed_unix_time?`、`updated_unix_time` |
| `pf_source_identity` | `identity_id`、`uid`、`source_account_id`、`identity_kind`、`source_identity_key`、`source_core_digest`、`identity_key_version`、`core_digest_version`、`fingerprint_version`、`first_seen_unix_time`、`last_seen_unix_time` |
| `pf_raw_import_row` | `row_id`、`uid`、`batch_id`、`row_number`、`source_locator`、`identity_id?`、来源交易/订单/商户号、原始时间/金额/方向/状态/类型/对手方/商品/支付方式/备注、`normalized_unix_time?`、`normalized_timezone_utc_offset?`、`normalized_amount?`、`currency`、`normalized_direction`、`normalized_transaction_type`、`economic_effect`、`ledger_account_id?`、`observed_source_identity_key`、`observed_source_core_digest`、`raw_fields_json`、`issues_json`、`primary_issue_code`、六类规则版本、`semantic_eligibility`、`parse_state`、`identity_state`、`disposition`、`processing_state`、`created_unix_time` |

`pf_import_batch` 的七类行数固定为 `total_row_count`、`valid_row_count`、`invalid_row_count`、`exact_duplicate_row_count`、`identity_conflict_row_count`、`pending_row_count`、`posted_row_count`。计数由服务层在同一事务维护，不依赖触发器。

批次可以在尚未解析出可靠来源账户、等待用户选择时先持久化，因此 `source_account_id` 可空并进入 `awaiting_source_account`；进入身份计算前必须补齐。无效行的标准化可空字段保持 NULL，`currency`、观察到的 identity key/core digest 和各非空原始字符串使用空字符串哨兵，并以 `identity_state=not_evaluated` 明确表示未计算身份。`row_number` 是批次内从 1 开始、跨 XLSX sheet 全局递增的逻辑序号；物理 CSV 行或 XLSX sheet/行只写入版本化 `source_locator`。

v001 索引名、唯一性和有序列固定如下，名称均为不超过 63 字符的 ASCII：

| 表 | 唯一索引 | 普通索引 |
| --- | --- | --- |
| `pf_import_file` | `UQE_pf_import_file_uid_sha256(uid,file_sha256)` | `IDX_pf_import_file_uid_created(uid,created_unix_time,file_id)`；`IDX_pf_import_file_uid_content_updated(uid,content_state,updated_unix_time)` |
| `pf_source_account` | `UQE_pf_source_account_uid_type_key(uid,source_type,source_account_key)` | `IDX_pf_source_account_uid_status_updated(uid,status,updated_unix_time)`；`IDX_pf_source_account_uid_ledger(uid,ledger_account_id)` |
| `pf_import_batch` | 无，显式重解析必须允许多批次 | `IDX_pf_import_batch_uid_file_created(uid,file_id,created_unix_time,batch_id)`；`IDX_pf_import_batch_uid_source_created(uid,source_account_id,created_unix_time,batch_id)`；`IDX_pf_import_batch_uid_status_updated(uid,status,updated_unix_time,batch_id)` |
| `pf_source_identity` | `UQE_pf_source_identity_uid_key(uid,source_identity_key)` | `IDX_pf_source_identity_uid_source_seen(uid,source_account_id,last_seen_unix_time,identity_id)` |
| `pf_raw_import_row` | `UQE_pf_raw_row_uid_batch_number(uid,batch_id,row_number)` | `IDX_pf_raw_row_uid_batch_states_number(uid,batch_id,parse_state,identity_state,processing_state,row_number)`；`IDX_pf_raw_row_uid_identity(uid,identity_id,row_id)` |

`raw_fields_json` 必须是按原始列顺序保存的数组，而不是以列名为 key 的 map；这样重复表头、空表头和原始顺序都不会丢失。服务层统一限制单行快照低于 MySQL `TEXT` 的 64 KiB 边界，并限制整批大小；SQLite 的 `VARCHAR(n)` 不执行长度约束，所以三库都必须依赖同一服务校验。

已发布 v001 的唯一真实结构是 `pkg/personalfinance/migrations/schema_v001.go` 及 canonical checksum `71ca2be439df0fadab021905b4ffd2a21dcd814bc8903f47759287574d692193`。反射测试逐字段校验运行模型与冻结 bean；后续模型演进不得修改 v001，只能新增 v002 迁移与新的最终 schema 验证器。

### 5.10 POST-101 的 v002 冻结契约

v002 以独立冻结 bean 和 checksum 新增下表；迁移 preflight 接受 v001 精确结构加“整张 v002 表不存在或完整兼容”，最终验证 v001+v002 全部表精确一致。每张新表单独一个可重入命名步骤，仍执行租约 fencing、取消传播、InnoDB 和三库方言校验。

| 表 | 冻结字段 | 唯一索引 | 普通索引 |
| --- | --- | --- | --- |
| `pf_import_posting` | `posting_id`、`uid`、`batch_id`、`idempotency_key_digest`、`idempotency_key_version`、`request_digest`、`request_digest_version`、`status`、`selected_row_count`、`created_transaction_count`、`reused_transaction_count`、`error_code`、`created_unix_time`、`started_unix_time?`、`completed_unix_time?`、`failed_unix_time?`、`updated_unix_time` | `UQE_pf_import_posting_uid_key(uid,idempotency_key_digest)` | `IDX_pf_import_posting_uid_batch_created(uid,batch_id,created_unix_time,posting_id)`；`IDX_pf_import_posting_uid_status_updated(uid,status,updated_unix_time,posting_id)` |
| `pf_raw_row_transaction_link` | `link_id`、`uid`、`row_id`、`transaction_id`、`relation_role`、`creation_method`、`posting_id`、`rule_version`、`transaction_updated_unix_time`、`created_unix_time` | `UQE_pf_raw_row_tx_link_uid_relation(uid,row_id,transaction_id,relation_role)` | `IDX_pf_raw_row_tx_link_uid_transaction(uid,transaction_id,link_id)`；`IDX_pf_raw_row_tx_link_uid_posting(uid,posting_id,link_id)` |
| `pf_import_batch_issue` | `issue_id`、`uid`、`batch_id`、`issue_order`、`code`、`severity`、`field`、`created_unix_time` | `UQE_pf_import_batch_issue_uid_order(uid,batch_id,issue_order)` | 无；该唯一索引同时支持批次内顺序读取 |

状态固定为 posting `ready / posting / completed / failed`，关系角色固定为 `primary / transfer_counterpart`，创建方式固定为 `posting_created / exact_identity_reused`，规则版本固定为 `idempotency-key-v1 / posting-request-v1 / posting-link-v1`。所有字段 `NOT NULL`，仅三个结束时间可空；不使用外键、触发器、数据库 JSON 或专有锁语义。

## 6. ID 策略

现有内部 UUID 类型字段只有 4 bit，类型总数上限为 16。个人财务域不能为每张表长期消耗一个 UUID 类型。

阶段 1 采用一个共享的 `UUID_TYPE_PERSONAL_FINANCE`，所有 PF 实体共用；实体类型由表本身区分。增加新 UUID 类型前必须检查剩余容量。

这比每表占一个类型更适合后续对账、贷款和总览扩展。

## 7. 证据解析器契约

不修改所有旧解析器共同实现的 `TransactionDataImporter` 接口。新增只由首批来源实现的证据解析契约：

```go
type ImportEvidenceParser interface {
    Descriptor() ParserDescriptor
    Probe(ctx context.Context, file EvidenceFile) ProbeResult
    Parse(ctx context.Context, file EvidenceFile, opts ResolvedParseOptions) (*EvidenceDocument, error)
}

type EvidenceDocument struct {
    Metadata DocumentMetadata
    Rows     []EvidenceRow
    Issues   []EvidenceIssue
}

type EvidenceRow struct {
    RowNumber            int64
    Locator              SourceLocator
    RawFields            []RawField // 有序，允许重复列名
    Raw                   CanonicalRawEvidence
    Identifiers          SourceIdentifiers
    Normalized           NormalizedEvidence
    FingerprintMaterials StrongFingerprintMaterials
    ParseStatus          ParseState
    Issues               []EvidenceIssue
}
```

要求：

- 使用标准库 `context.Context`；解析器是纯转换组件，不依赖 User、账本映射、数据库或对象存储；
- 文件服务在字符转码或解包前对原始上传字节计算哈希；解析器接收同一份不可变内容；
- CSV 返回物理行定位，XLSX 返回 sheet 与行定位；返回每一逻辑行而不仅是有效交易；
- `row_number` 是跨 sheet 的全局逻辑序号，CSV/XLSX 物理行号从 1 开始，`sheet_index` 从 0 开始；
- `RawFields` 保留解码后、trim 前的完整有序单元格；`CanonicalRawEvidence` 同时把原始时间、金额、方向、状态、类型、对手方、商品、支付方式和备注投影到固定字段，缺失项用空字符串；
- 语义错误行也返回原始列快照、标识材料和标准化草稿；只有文件级不可读才让整次 Parse 失败；
- `ResolvedParseOptions` 在进入 parser 前已校验：币种是大写 ISO 4217 三字母代码，UTC offset 是 `-720..840` 的分钟数；不存在 nil 默认语义；
- 标准化时间是 Unix 秒，金额始终是非负最小货币单位；退款只由 `economic_effect` 表达，不能复用旧导入器的“负数支出”投影；
- `ParseStatus` 只描述结构、时间、金额等核心字段能否可靠解析；未知方向、来源类型或状态语义仍是 `valid`，由中心规则降为 `review_required`；closed/failed 为 `valid + non_postable`，核心可靠的 refund 可入账；
- parser 不返回 `semantic_eligibility`。中心必须调用 `ValidateEvidenceDocument` 绑定 descriptor、文档来源、来源问题码命名空间和连续逻辑行；该入口再逐行校验定位、快照大小和字段上限，并用 `ResolveSemanticEligibility` 推导资格。最终 `disposition` 和 `processing_state` 再结合去重结果生成；
- 错误使用稳定错误码，原始值不进入日志；公共码由中心维护，来源专属码必须使用 `alipay_` 或 `wechat_` 前缀及小写 ASCII/数字/下划线，两个解析器不得为增加来源码争改公共枚举；
- 上层必须用 descriptor-aware 的 `ProbeResult.Validate` 校验探测结果，并用 `NormalizeEvidenceParseError` 把文件级失败归一为稳定安全码；禁止直接记录 parser 返回的 `err.Error()`，未知错误统一收敛为通用文件格式错误；
- parser 只拥有 parser/normalization 两类版本；中心包唯一拥有 source-account-key、identity-key、core-digest、fingerprint、raw-snapshot 版本，并在批次/账户/原始行中注入相应快照，解析器不得覆盖；
- 解析器只返回稳定编号候选、标准化证据和强指纹材料；身份优先级、`identity_kind`、key/core 编码与 SHA-256 全部由中心 identity 包负责；
- 支付宝、微信旧导入入口与新入口共享底层提取和标准化函数；
- 第一阶段只接支付宝 App/Web CSV、微信 CSV/XLSX；其他来源继续使用旧流程。

来源账户识别也遵守证据强度：稳定标识只接受完整未掩码邮箱，或含可选 `+` 的 8–15 位完整电话；更短/更长数字、尾号、连续 `xx`、星号等证据只能作为掩码展示并要求用户选择档案。稳定标识候选不得携带 parser 自填的展示名，中心必须用 `SafeSourceAccountDisplayName` 生成脱敏显示；掩码展示最多保留末四位，不能因为文本中存在星号或“尾号”就把其余明文原样保存。微信昵称只能作为二次脱敏后的显示候选，不能静默生成跨文件身份。公共 `SourceAccountCandidate.Validate`、`ComputeSourceAccountKey` 与 `SafeSourceAccountDisplayName` 共同强制该判别规则。账单中的支付方式只是账本账户候选，永远不是来源账户身份。

## 8. 同来源精确去重

### 8.1 文件级

上传字节在任何字符转码或解析之前计算 SHA-256：

```text
fileIdentity = SHA256(originalUploadBytes)
```

### 8.2 来源级

`sourceIdentityKey` 使用无歧义的版本化编码后再计算 SHA-256，不使用简单字符串拼接。

`identity-key-v1` 的所有字符串先做 NFKC、去 BOM、trim，并把空值及 `- / n/a / null / none` 哨兵归一为空；编码使用 UTF-8 字节长度前缀。key 的公共前缀固定为 `identityKeyVersion + sourceType + sourceAccountKey + identityKind`，然后按以下优先级追加材料：

1. 非空来源交易号；
2. 同时非空的订单号 + 商户订单号，单独一个不构成订单组合身份；
3. `fingerprint-v1` 强指纹；
4. 信息不足时追加 batch ID + 批次内逻辑 row number，生成批次局部身份，不跨文件自动去重。

`fingerprint-v1` 只有在规范化对手方非空，且商品或支付方式至少一项非空时才成立；摘要材料固定为：

- fingerprintVersion；
- 标准化时间；
- 最小货币单位金额；
- 币种和方向；
- `economic_effect`；
- 规范化对手方、商品、支付方式。

parser 只决定哪些来源字段可以安全放入上述材料，不能修改中心优先级或自行哈希。金额相同、时间接近不能单独形成精确身份。

来源账户档案是上述身份计算的稳定作用域。账本账户映射、显示名称或用户后续分类不参与来源身份计算。

### 8.3 身份一致性

命中相同 `sourceIdentityKey` 后还要比较来源语义不变量。每个解析器只负责把来源字段映射为中心规定的标准化语义，并明确：

- 哪些字段属于不可变核心，例如金额、币种和收支方向；
- 哪些变化是允许的证据演进，例如状态更新或补充商户文本；
- 哪些来源状态和方向映射为 `normal / refund / closed / failed / unknown` 及 `income / expense / neutral / unknown`。

中心 identity 包唯一生成版本化 `sourceCoreDigest`。v001 固定包含：非负最小货币单位金额、币种、来源方向和 `economic_effect`。`economic_effect` 只允许 `normal / refund / closed / failed / unknown`；来源原始状态先由解析器映射为该语义，原始状态文本本身不直接进入 digest。`neutral` 或任何 `unknown` 语义都必须进入人工复核，不能直接入账。标准化来源交易类型仍持久化供审计，但支付宝 App/Web 或不同导出版本可能对同一交易给出不同分类，因此不进入 `core-digest-v1`。

同一 `economic_effect` 内的状态细化、来源分类或商户文字变化允许证据演进；金额、币种、方向或 effect 改变属于核心冲突。身份相同且 core digest 相同才标记 `exact_duplicate`。若核心不兼容，当前行自己的 observed key/core digest 与标准化语义仍然保存，但标记 `identity_conflict`，禁止自动复用正式交易或确认入账，直到用户明确解决。解析器升级导致的不一致也走这一安全路径，不能用新值覆盖旧证据。

### 8.4 并发策略

应用层先计算身份，数据库唯一约束负责最终裁决：

1. 尝试插入 `pf_source_identity`；
2. 唯一冲突时读取同 uid 的既有身份；
3. 比较来源语义不变量，决定精确重复或身份冲突；
4. 当前原始行无论结果如何都写入；
5. 只有精确重复且既有身份已关联正式交易时，才复用这些交易关系；
6. 身份冲突进入人工处理，不删除、不覆盖任何原始行。

## 9. 确认入账与事务边界

### 9.1 数据库内原子操作

一次确认必须在同一个用户数据数据库事务内完成：

1. 按 uid 读取批次、posting 和选中原始行；
2. 通过条件更新认领 posting/batch；
3. 校验账户、分类、标签和交易编辑时间；
4. 对每个来源身份决定创建正式交易或复用既有交易；
5. 调用账本适配器，在当前 XORM session 中创建交易并更新余额；
6. 写入所有证据关系；
7. 更新原始行、posting 和批次统计；
8. 一次提交。

任一步失败必须整体回滚。

### 9.2 不依赖数据库专有锁

批次认领采用条件更新，而不是把正确性完全建立在 `SELECT FOR UPDATE` 上。`partially_posted` 允许同一批次分多轮确认，但同一时刻只允许一个 posting 持有批次：

```sql
UPDATE pf_import_posting
SET status = 'posting'
WHERE uid = ? AND posting_id = ? AND status = 'ready'

UPDATE pf_import_batch
SET status = 'posting'
WHERE uid = ? AND batch_id = ?
  AND status IN ('ready', 'partially_posted')
```

只有更新成功的请求继续执行。唯一索引和最终状态读取共同保证 SQLite、MySQL、PostgreSQL 下的持久幂等。

### 9.3 账本适配器

现有交易创建逻辑需要提供“在调用方已有 session 中写入”的窄入口。适配器负责：

- 生成交易和标签索引 ID；
- 复用现有账户、分类、标签和余额校验；
- 处理转账双侧交易；
- 不提交或回滚调用方事务；
- 不依赖任何 PF 模型。

个人财务服务负责编排；核心交易服务只负责账本不变量。

## 10. 对象存储一致性

对象存储与数据库无法共享事务，采用状态机和补偿，不假装存在跨资源原子提交。

推荐流程：

```text
上传临时对象
  -> 计算并验证哈希
  -> 创建/读取 pf_import_file
  -> 把对象移动或复制到不透明最终路径
  -> 标记 content_state=available
  -> 创建解析批次
```

失败处理：

- 数据库未提交：清理临时对象；
- 数据库已提交但对象落位失败：文件状态标为 missing/failed，可重试修复；
- 原文件被用户删除：标为 deleted，保留哈希、批次、行和关联；
- 周期性检查孤立临时对象和缺失对象，只记录 ID，不记录原文件名或内容。

## 11. 状态机

批次：

~~~mermaid
stateDiagram-v2
    [*] --> receiving
    receiving --> parsing
    receiving --> failed
    parsing --> awaiting_source_account: 缺少稳定来源账户
    awaiting_source_account --> ready: 用户选择来源账户并完成身份计算
    awaiting_source_account --> discarded
    parsing --> ready
    parsing --> failed
    ready --> posting
    ready --> discarded
    posting --> completed: 无待处理有效行
    posting --> partially_posted: 仍有待处理有效行
    posting --> failed
    partially_posted --> posting: 下一轮确认
~~~

posting 完成后仍有待确认行时，批次进入 `partially_posted`；全部有效行已关联或被明确忽略后才进入 `completed`。

原始行采用正交状态，避免一个巨大枚举：

- 解析 `parse_state`：`valid / invalid`；
- 身份 `identity_state`：`not_evaluated / new / exact_duplicate / identity_conflict / batch_local`；
- 中心语义 `semantic_eligibility`：`postable / review_required / non_postable`；
- 最终处置 `disposition`：`postable / review_required / non_postable`；
- 处理 `processing_state`：`pending / linked / ignored / failed`；

固定状态矩阵：无效行是 `invalid + not_evaluated + non_postable + ignored`；来源语义不可入账但 core 完整的有效行仍执行身份计算，最终为 `non_postable + ignored`；身份冲突和 batch-local 有效行一律 `review_required + pending`；已有正式交易关系的精确重复为 `non_postable + linked`，尚无关系的精确重复为 `review_required + pending`。只有仍处于 pending 的有效行会阻止批次进入 completed。

posting：

```text
ready -> posting -> completed
             |
             v
           failed
```

失败状态必须区分可安全重试与需要人工处理。

## 12. 数据库迁移策略

### 12.1 独立迁移账本

新增 `pf_schema_migration`，每个用户数据数据库实例按以下精确 schema 记录；字段均 `NOT NULL`，只有 `applied_unix_time` 可空：

| 字段 | 类型与语义 |
| --- | --- |
| `version` | `BIGINT PK`，严格从 1 连续递增 |
| `name` | `VARCHAR(128)` |
| `checksum` | `CHAR(64)`，冻结 migration 表、业务表和 raw snapshot 规则的 canonical SHA-256 |
| `application_version` / `application_commit` | `VARCHAR(64)`，本次认领者的应用信息 |
| `runner_id` | `CHAR(32)`，一次 Upgrade 调用的随机实例标识 |
| `claim_token` | `CHAR(32)`，每次首次认领或接管都重新生成的随机 fencing token |
| `first_started_unix_time` | `BIGINT`，首次尝试时间，重试不覆盖 |
| `started_unix_time` / `updated_unix_time` / `lease_expires_unix_time` | `BIGINT`，本次认领与租约时间 |
| `applied_unix_time?` | `BIGINT`，成功后写入 |
| `success` | `BOOLEAN` |
| `failure_code` | `VARCHAR(64)`，空字符串表示正在执行，非空表示可重新认领的稳定失败码 |

迁移注册表在运行前校验版本严格从 1 连续递增、无跳号且 checksum 固定。检测到同版本 checksum 改变、数据库版本高于当前二进制或存在不能安全接管的失败迁移时拒绝升级，禁止静默修改已发布迁移。

`pf_schema_migration` 是数据库级结构元数据，不包含用户业务数据，因此不带 uid；这是“所有用户数据模型必须带 uid”的明确且唯一例外。其他所有 `pf_` 业务表必须带 uid。

MySQL 的 DDL 不承诺事务原子性，因此每个迁移必须可重入。runner 使用数据库时间认领 `success=false` 的记录，心跳、失败释放、超时接管和最终成功都必须匹配 `version + checksum + success=false + claim_token + failure_code='' + 有效租约` 并检查 `RowsAffected()==1`；接管还要匹配刚读取的旧租约、更新时间和失败码。迁移主体拆成有名称、可重入且预期短于一个租约周期的步骤；每步前完成 fenced 续租，成功后才能进入步骤，步骤后再次校验。SQLite/PostgreSQL 通过同语句数据库时钟做条件更新；MySQL 的无参 `UNIX_TIMESTAMP()` 固定在语句开始时，因此续租、失败释放和最终成功必须在短事务内先 `SELECT ... FOR UPDATE` 锁定 ledger 行，取得锁后另行读取数据库时间，再校验 token、有效租约、旧租约和更新时间并条件更新。该行锁只保护 migration ledger，不扩散到业务表。旧 runner 丢失 token 或租约后不得进入下一步，也不能再续租、标失败或标成功。heartbeat、DDL、Sync2、schema 元数据和方言目录查询都传播取消信号，heartbeat 停止过程有界且幂等。SQLite、MySQL、PostgreSQL 使用同一状态语义。

bootstrap `CREATE TABLE IF NOT EXISTS` 后先精确验证 migration 表自身。v001 执行前只接受“整张业务表不存在，或已有表列/PK 完整兼容且至多缺少预期索引”；缺列、缺 PK 的半表在 Sync2 修改前直接拒绝。未知 `pf_` 表、未知列/索引和已有字段的不兼容类型、nullable、PK、自增或索引顺序同样拒绝。冻结 bean Sync2 后再精确验证五张业务表，完全匹配才允许 `success=true`。除 XORM 元数据比较外，方言目录还必须拒绝 partial/expression/prefix 索引、无效索引、触发器、外键和生成列，防止同名唯一索引被静默弱化；MySQL 的 migration 表与所有 PF 表必须是 InnoDB。SQLite 按其可表达的类型亲和性归一化，MySQL/PostgreSQL 仍精确比较 `CHAR/VARCHAR` 长度；PostgreSQL 连接与目录校验统一固定到 `public` schema，不能受角色 `search_path` 漂移影响。

### 12.2 与 Sync2 的关系

- 上游核心表继续沿用现有 Sync2，不在本阶段重写其迁移机制；
- 实时 PF 运行模型永远不加入 `cmd/database.go` 的常驻 Sync2 列表；
- `pf_schema_migration` 只通过一段最小、三库通用且可重入的 bootstrap DDL 创建；
- v001 可在迁移包内部使用冻结的 v001 schema beans 对单个 `Database` 执行 Sync2；这些 beans 不能被运行模型替代或随模型演进修改；
- v002 起的加字段、回填、重命名和约束变化全部写成显式迁移；
- 迁移入口遍历 `UserDataStore.Count()/Get()`，每个数据库实例独立读取和推进版本；
- 不使用 JSON 查询、部分索引、数据库专有生成列等三库不一致能力；
- 三种数据库的 DDL 原子性不同，生产迁移默认 forward-only，并要求事前备份。

个人财务 repository 的全部查询和事务使用 privacy session，把 XORM `SessionShowSQLKey` 固定为 false；因此即使管理员打开全局 `log_query`，原始快照、完整标识及其他绑定参数也不会进入 SQL 日志。普通核心查询日志行为保持不变。

为保持上游接触面最小，只修改三个组合点：UUID 类型表新增一个 PF 共享类型；`datastore.Database` 提供单实例 SyncStructs、schema 元数据以及可关闭的独立测试连接；核心 Sync2 全部成功后，`cmd/database.go` 调用一次 PF Upgrade。`cmd/webserver.go` 已复用数据库更新入口，无需再加第二个接触点。

### 12.3 回滚原则

- 第一阶段只新增表和索引，不修改或删除上游核心列；
- 应用版本回滚后，旧版本忽略 PF 表；
- 已写入的正式交易仍属于正式账本，不能靠删 PF 表撤销；
- 数据迁移回滚以恢复备份为主，不承诺所有 DDL 自动逆向；
- 发布说明必须写明最低 schema version 和可回滚边界。

## 13. API 与前端边界

API 使用独立命名空间：

```text
POST /api/v1/personal_finance/import_files/upload.json
GET  /api/v1/personal_finance/source_accounts/list.json
POST /api/v1/personal_finance/source_accounts/save.json
POST /api/v1/personal_finance/import_batches/reparse.json
GET  /api/v1/personal_finance/import_batches/list.json
GET  /api/v1/personal_finance/import_batches/get.json
GET  /api/v1/personal_finance/import_batches/rows.json
POST /api/v1/personal_finance/import_batches/post.json
POST /api/v1/personal_finance/import_batches/discard.json
```

约束：

- 保留全部现有 `/transactions/parse_import.json` 和 `/transactions/import.json`；
- 新旧流程不共享持久幂等语义；
- 新接口所有 ID 查询同时限定 uid；
- 列表默认不返回原始 JSON 快照；详情按需返回并受大小限制；
- API 层只负责绑定、鉴权、错误映射和响应组装。

桌面端第一阶段完成完整工作台；移动端先提供历史、状态和待确认数量的只读视图。

## 14. 安全撤销边界

第一阶段即保存撤销所需审计信息，但撤销实现不得物理删除证据。

正式交易只有同时满足下列条件才可由批次安全撤销：

- 确实由该 posting 自动创建；
- 创建后未被人工修改；
- 没有其他批次、人工录入、贷款计划或对账决定依赖；
- 相关账户仍允许在该时间点修改；
- 整个影响集合能够在一个账本事务中撤销。

否则只展示影响并要求人工处理。

## 15. 上游同步治理

### 15.1 Remote 与分支职责

```text
origin/main                       产品权威、稳定、可部署
upstream/main                     官方只读参考
codex/sync-upstream-YYYYMMDD      每次上游整合
codex/<feature>                   短期功能开发
```

禁止：

- 把 `origin/main` reset 到 `upstream/main`；
- force push 产品主线；
- 让自动依赖更新或上游合并直接触发生产部署；
- 在长期共享分支上通过 rebase 改写已经发布的历史。

### 15.2 更新分级

| 上游变化 | 默认策略 |
| --- | --- |
| 安全、认证、数据损坏、数据库兼容修复 | 优先评估和同步 |
| 普通 Bug、性能和依赖升级 | 按收益和风险同步 |
| UI 大改、导入重构、与 PF 领域冲突的新功能 | 可延后、拆取或拒绝 |
| 与本产品方向相反的语义变化 | 保留自身实现并记录差异 |

### 15.3 每次同步流程

1. 获取 `origin` 和 `upstream` 最新引用；
2. 从最新 `origin/main` 创建同步分支；
3. 记录本次目标 upstream commit；
4. 合并 `upstream/main`，不直接操作产品主线；
5. 重点审查架构接入点和数据库变化；
6. 在数据库备份副本上做迁移和启动演练；
7. 运行后端、前端、三库和 PF 验收测试；
8. 通过 PR 合入 `origin/main`；
9. 创建产品版本标签并记录 upstream 基线；
10. 备份生产数据库与原文件存储后部署。

### 15.4 冲突预算

维护一份“上游接触面”清单。每新增一个对上游核心文件的修改，都必须回答：

- 能否改为新增文件？
- 能否放入适配器或注册函数？
- 是否真的需要核心知道 PF 的存在？
- 能否将通用改进贡献回上游？

若连续多次同步都在同一处产生高成本冲突，应重构边界，而不是长期重复手工解决。

## 16. 分阶段实施路线

### 阶段 0：长期 fork 与工程底座

目标：先让自己的产品主线可长期演进和安全吸收上游。

任务：

1. 配置只读 `upstream` 并记录官方基线；
2. 建立同步分支、版本标签和发布规则；
3. 固化本架构决策与上游接触面；
4. 建立 PF 包、表和 API 命名约定；
5. 定位 Go 1.26 工具链并跑确定性前后端基线；
6. 准备隔离的 SQLite、MySQL、PostgreSQL 测试方式；
7. 在 DB-101 中建立 PF 版本化迁移框架和 v001，而不是混入 BASE-001。

BASE-001 完成门槛：

- 记录 origin、upstream、阶段分支和准确工具链；
- 基线测试结果可复现；
- 三库连接和现有核心 Sync2 连续执行两次通过，且测试环境不会接触现有容器或真实数据目录。

DB-101 完成门槛：

- 能明确说出部署版本对应的 upstream commit 和 PF schema version；
- 全新库升级到 v001、重复执行、checksum 拒绝、未知高版本拒绝、失败接管和并发认领在三库通过；
- “回滚”指旧应用能忽略新增 PF 表，以及备份恢复后可重跑迁移；不承诺跨三库通用 down migration。

### 阶段 1A：文件、批次和原始证据

任务：

1. 新增 ImportFile、SourceAccount、ImportBatch、SourceIdentity、RawImportRow；
2. 接入原文件对象存储和补偿状态；
3. 实现支付宝、微信证据解析；
4. 保存有效、无效和跳过行；
5. 提供上传、重复文件响应、批次和行查询。

完成门槛：

- 同文件重复上传命中同一 `pf_import_file`；
- 显式重解析产生新批次且保留旧批次；
- 原始行能够追溯到文件、解析器和规则版本；
- 来源账户映射变化不改变已有来源身份；
- 日志不出现原始账单内容或完整账户标识。

### 阶段 1B：同来源精确去重与预览

任务：

1. 实现版本化 sourceIdentityKey；
2. 处理批次内和跨批次精确重复；
3. 检测相同身份下的核心字段冲突；
4. 给出重复或冲突原因以及既有证据/交易；
5. 前端展示批次、覆盖账期、无效行、重复和冲突状态。

完成门槛：

- 重叠账期中的相同来源订单只有一个来源身份；
- 每次出现的原始行仍然保留；
- 同日同额但交易号不同的两笔消费均保留；
- 相同来源 ID 但核心金额或方向冲突时禁止自动入账；
- 信息不足的行不跨文件自动合并。

### 阶段 1C：持久幂等确认入账

任务：

1. 新增 ImportPosting 和 RawRowTransactionLink；
2. 建立 session 级账本适配器；
3. 实现可多轮部分确认、原子确认、并发认领和失败回滚；
4. 对已入账精确重复只补证据关系；
5. 提供正式交易到原始证据的下钻。

完成门槛：

- 并发确认同一请求只产生一组正式交易；
- 同一个幂等键提交不同请求摘要会被拒绝；
- 任意一行失败不会留下半批正式交易；
- 用户 A 无法读取、确认或关联用户 B 的数据。

### 阶段 1D：导入历史与安全运维

任务：

1. 完成桌面导入历史和批次详情；
2. 支持废弃未入账批次和删除原文件内容；
3. 展示安全撤销影响；
4. 将 PF 数据纳入清空数据、统计、备份和隐私说明；
5. 建立孤立对象检查和批次一致性检查。

完成门槛：

- 批次统计可由行和关联重新计算并核对；
- 删除原文件不影响审计链；
- 清空用户数据不会遗留可识别的原文件；
- 回滚和恢复流程有自动测试或可重复演练。

### 阶段 2：跨来源人工对账

仅在阶段 1 稳定后开始：候选生成、解释原因、人工决策持久化、同一事件/转账两侧/退款/不匹配关系。第一版不做跨来源静默自动合并。

### 阶段 3：贷款与分期

建立纯计算内核、合同、逐期计划和实际交易分配；本金不计消费，利息和必要费用才计支出。

实施时优先读取参考仓库中的 `docs/PRODUCT.md`、`miniprogram/domain/loan-model.js`、`repayment-plans.js`、`discount-engine.js`、`calculation-engine.js` 及对应测试。先提取四种还款方式、两种输入模式、费用和优惠的金标，再在本仓库建立后端权威实现。金额使用最小货币单位整数；如新增真实日期 XIRR，必须作为新口径命名，不能静默改变旧 APR。

### 阶段 4：可信财务总览

基于正式交易、贷款计划和导入覆盖率展示资产、负债、净资产、现金流、待还和数据缺口。未确认原始行不进入正式统计。

## 17. 持续验收矩阵

### 17.1 后端正确性

- 文件哈希重复；
- 显式重解析；
- 来源交易号重复；
- 来源账户档案选择与映射变更；
- 相同来源身份的核心字段冲突；
- 强指纹重复与不足降级；
- 同额同日独立交易；
- 无效行保存；
- 跨用户隔离；
- 并发上传和并发确认；
- 确认中途失败整体回滚；
- 转账双侧和账户余额一致；
- 原始快照大小限制；
- 日志隐私检查。

### 17.2 数据库兼容

- SQLite 为每次开发的快速验证；
- MySQL 和 PostgreSQL 至少进入 CI/发布门禁；
- 唯一约束、条件更新、事务回滚和迁移均在三库验证；
- 不以某一数据库的 NULL 唯一性、JSON 查询或锁语义作为正确性前提。

### 17.3 前端与接口

- TypeScript 类型检查；
- store 和服务单测；
- 批次状态、重复解释和错误显示；
- 大批次分页和按需原始快照；
- 以 `zh_Hans` 为个人使用的主验收语种，新增 PF 功能仍保留 `en` 国际化骨架；其他既有语言不作为阶段 1 的主动翻译范围，不删减多币种与时区基础能力；
- 构建与桌面关键流程验证。

### 17.4 上游同步门禁

- 当前上游基线记录；
- 上游接触面 diff 审查；
- PF schema 从生产版本副本升级；
- 旧版本回滚可启动；
- 数据库和对象存储备份恢复抽查；
- 正式交易数量、账户余额和证据链接一致性核对。

## 18. 明确不做

- 不拆微服务或独立账本数据库；
- 不重写现有认证、账户和交易系统；
- 不为了跟随上游而放弃 PF 正确的数据模型；
- 不完全停止关注上游安全和基础修复；
- 不修改所有旧导入器的公共接口；
- 不用金额和时间的弱条件自动删除或合并；
- 不物理删除作为去重手段；
- 不在阶段 1 混入跨来源自动合并、贷款或总览实现。

## 19. 当前下一步

阶段一、阶段二代码与本地正式发布均已完成，当前准备进入阶段三：

1. `DB-101` 至 `QA-101` 的模型、解析、存储、去重、入账、编排、界面与安全运维均已完成复核、集成、规定门禁和安全清理；准确提交、隐私结论与验证证据以 0.5、0.8～0.17 节为准；
2. 当前分支可以完成“上传支付宝/微信账单→显式解析→人工选择来源账户→逐行确认入账→查看脱敏证据→安全废弃未入账批次/删除原文件→统计、清理与一致性检查”的阶段一闭环；正式 `Transaction`、账户、分类和标签仍是唯一账本权威；
3. `REC-201` 已完成：0.19 节共享契约、v003、通用银行映射、候选、决定、API 和页面全部集成；全量、三库、生产副本迁移和浏览器证据见 0.20 节，正式 8080 发布与回滚材料见 0.21 节；
4. `DEPLOY-101` 的阶段一恢复卷和备份策略已由 `DEPLOY-201` 原样继承；阶段一镜像已从 8080 安全退出并保留为回滚材料，当前 8080 运行阶段二镜像 `ezbookkeeping-fork:2.0.0-pf2-4c662305`；
5. 阶段二已同步到 `origin/main@a7eb68ff`；第三任主负责人已按 0.22 节接管阶段三，并在 0.23 节冻结 `LOAN-301` 的 v004、计算、状态、账本和 REST/DTO 契约。计算、数据、合同服务、结算分配、合同 API 与前端客户端已集成，完整 UI 已交付待依赖顺序合入；当前只剩 settlement API/生产组合/十二路由接线、阶段三统一 QA 与发布，`DASH-401` 不提前实现。

任何后续实现若偏离本计划的依赖方向、数据口径或上游治理策略，必须先更新架构决策并说明迁移影响。
