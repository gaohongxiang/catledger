# 招财猫记账本实施规划

> 状态：当前阶段、任务、冻结契约和完成证据的唯一事实源
>
> 产品主线：`origin/main`
>
> Web 归档：`origin/catledger-web` / `web-v2-final`

## 快速阅读

- 只想知道当前做到哪里：看 0.3、0.5、0.6 和第 19 章；
- 准备领取下一项任务：从 0.5 看任务边界，再读第 5 章方向契约和第 19 章；
- 核对历史为什么这样设计：按需查看第 1～4 章，不必每次从头阅读；
- 产品范围、业务对错和技术实现分别以用户需求、业务规则与验收、架构设计为准。

## 0. 文档使用与当前看板

### 0.1 唯一事实源

- 根目录 `AGENTS.md`：长期协作、安全、分支、数据和验证硬约束；
- [招财猫记账本用户需求.md](./招财猫记账本用户需求.md)：产品定位、范围和体验；
- [招财猫记账本业务规则与验收.md](./招财猫记账本业务规则与验收.md)：会计、隔离、导入和隐私的对错标准；
- [招财猫记账本架构设计.md](./招财猫记账本架构设计.md)：小程序、云函数、MySQL 与存储边界；
- 本文：当前状态、冻结契约、任务与下一步；
- `现行说明/`：当前代码已具备行为；
- 代码、迁移与测试：已落地行为的最终证据。

### 0.2 仓库与产品边界

- `origin/main` 是微信小程序产品权威主线；
- `origin/catledger-web` 和 `web-v2-final` 完整保留迁移前 Web 版；
- 新产品不维护桌面端、PWA 或移动 Web，不从旧 Web 代码机械移植页面；
- 首发目标是中国用户、多用户隔离和完整批量账单闭环。

### 0.3 当前工程与生产基线

| 范围 | 当前事实 |
| --- | --- |
| 客户端 | 微信原生小程序，WXML/WXSS/JavaScript |
| 小程序 AppID | `wx958ebc33591b2bab`；AppID 可公开，AppSecret 和云密钥禁止入库 |
| 后端 | `catledger-api` 开发版已部署并通过真实微信身份完成账户与手工交易闭环；`catledger-import` 仅保留目录，尚无可部署入口 |
| 数据库 | 开发云环境 MySQL 已部署 `0001`～`0003`；七张 `catledger_*` 表、受限应用账号及合成开发验证数据已验收 |
| 文件 | CloudBase 临时云存储，尚未接入 |
| 正式发布 | 尚未创建可审核的小程序版本 |
| Web 版 | 完整冻结于 `catledger-web` 与 `web-v2-final`；本轮不更新 8080/8082 |

### 0.4 状态定义

- `未开始`：未领取或依赖未满足；
- `进行中`：已有负责人、分支和工作区；
- `待验收`：候选已交付，等待复核或集成；
- `已完成`：已集成、验证、更新文档并安全清理；
- `阻塞`：存在已记录且无法在授权内解决的外部依赖或用户决策。

### 0.5 当前任务看板

| ID | 目标 | 依赖 | 状态 | 负责人 / 分支 | 验收与边界 |
| --- | --- | --- | --- | --- | --- |
| MINI-1901 | 固化 Web 并建立小程序干净基线 | 用户确认主线切换 | 已完成 | 当前主负责人 / `main` | Web 分支与标签均指向 `39a0ea55`；契约 `37be5cff`、骨架 `75b00ecf`；旧运行代码通过普通提交删除，配置/JSON/JS/WXML/云函数加载检查通过；未改本地 Web 数据和运行容器 |
| MINI-1902 | 配置 AppID、跑通开发者工具并收敛分支治理 | MINI-1901 | 已完成 | 当前主负责人 / `main` | 任务提交 `12ff0519` 已集成；开发者工具使用真实 AppID 完成首次编译；远端只保留 `main` 与 `catledger-web`；旧同步分支已归档为标签并删除；未创建云资源或业务数据 |
| MINI-1903 | 微信身份、用户初始化与默认分类 | MINI-1902 | 已完成 | 当前主负责人 / `54dc5901`、`8b21dd73` 已合入 `main` | 服务端可信身份、幂等初始化、首版 MySQL 迁移、用户隔离、云函数开发版与真实微信身份闭环均已验收；旧宠物业务资源已按授权清理 |
| MINI-1904 | 账户、手动记账与交易列表 | MINI-1903 | 已完成 | 当前主负责人 / `daa43fd6` 已合入 `main` | `0002` 与当前函数已部署；真实微信身份基础业务闭环、具体日期、首页近半年收支趋势、完整统计页、六套本机主题、模拟器视觉走查与真机预览推送均通过；任务 worktree 与本地分支已安全清理 |
| MINI-1904A | 核心账本收口 | MINI-1904 | 已完成 | 当前主负责人 / `82ab2216` 已合入 `main` | 分类增改停用恢复与排序、关联退款和净额统计、受保护页面统一登录门禁、移除“我的”重复账本入口均已集成；MySQL 8 集成测试 48/48、无数据库单元测试 32/32、根目录静态测试 8/8 通过；`0003` 与 `catledger-api` 已部署到开发环境且函数为 Active/Available，VPC 与私密变量保持不变；正常 AppID 项目窗口真实微信身份回归通过，`bootstrap` 与账户列表均返回 200；正式上传仍需处理 SDK 传递依赖告警 |
| MINI-1904B | 小程序视觉与连接状态收口 | MINI-1904A | 已完成 | 当前主负责人 / `1b72399f` 已合入 `main` | 暖橘编辑式视觉令牌、底栏、一级页、子页和共享面板已收口，六主题统一轻量层级、收入绿和支出红；首页与“我的”共用头像回退规则；客户端区分身份连接和局部同步，只读瞬时错误最多短重试一次且写动作不自动重放；根目录 26/26、云函数单元测试 32/32、JSON/JavaScript 语法检查通过，17 项数据库集成测试因本轮未配置测试库按设计跳过；用户确认集成，真机验证保留为正式上传前门禁 |
| MINI-1904C | 核心服务分层与一致性读取 | MINI-1904B | 待验收 | 当前主负责人 / `codex/mini-1904c-core-layering` | 公共 API、schema 与界面未变；交易服务拆为薄门面、领域校验、正式命令、明细查询和报表统计，明细摘要、首页与完整统计使用只读一致性快照；根目录 29/29、云函数非数据库测试 38/38 通过，17 项数据库集成测试因未配置测试库按设计跳过；未部署或推送 |
| MINI-1905 | 支付宝/微信单文件可信导入 | MINI-1904C | 未开始 | 待领取 | 上传、类型识别、来源与数据日期、解析、原始证据、来源身份、账户映射、经济事件候选、待确认项和单文件安全入账闭环 |
| MINI-1906 | 多文件整理、去重与失败隔离 | MINI-1905 | 未开始 | 待领取 | 通用跨来源经济事件归组、转账/信用卡还款/退款语义、并发、幂等、冲突、整批原子入账、文件级失败隔离和隐私门禁 |
| MINI-1907 | 财务状态、导出、注销与上线准备 | MINI-1906 | 未开始 | 待领取 | 同一快照的净值/可立即使用资金/负债、数据日期与完整度、导出、数据生命周期、注销、隐私指引、真机与审核清单 |
| MINI-1908 | 未来付款承诺、贷款/分期与安全线 | MINI-1907 | 未开始 | 待领取 | 沿用经济事件主流程扩展贷款账户、期次与本金/利息/费用拆分，未来承诺独立于正式交易、实际付款幂等关联、用户设置最低保留资金、未来 30/90 天确定支出口径 |
| MINI-1909 | 大额支出确定性情景测算 | MINI-1908 | 未开始 | 待领取 | 立即付清/分三次/推迟 90 天/暂不支付同快照比较，输出安全线、受影响支出、来源、假设和缺失资料且不产生业务写入 |
| MINI-1910 | 可选 AI 解读与周期变化摘要 | MINI-1909、隐私与合规评估 | 未开始 | 待领取 | 只读解释确定性结果；最小化脱敏输入、模型失败降级、无账本写权和无资金操作能力；不作为前序阶段上线条件 |

### 0.6 当前主负责人

- 当前阶段：MINI-1904C 本地候选待验收；
- 主负责人：当前主会话；
- 基线：小程序主线 `b3448204`；Web 最终提交 `39a0ea55`；
- 分支/worktree：`codex/mini-1904c-core-layering` / `/Users/gaohongxiang/projects/catledger-worktrees/mini-1904c-core-layering`；
- 文件所有权：`cloudfunctions/catledger-api/`、`shared/`、`test/`、本任务 spec 与相关说明；
- 不触碰主工作树中的用户未跟踪文件，不改本地 Web 容器、卷或 8080/8082 服务。

## 1. MINI-1901 冻结契约

1. 用普通提交删除旧 Web/Go 运行代码，保留连续 Git 历史，不使用 orphan 分支、reset 或 force push。
2. `catledger-web` 与 `web-v2-final` 必须先指向 `39a0ea55`，再改变 `main`。
3. 新基线只提供可导入的原生小程序外壳、可信微信身份的云函数入口和后续目录，不伪装数据库或账单导入已经完成。
4. 仓库默认不含真实 AppID、AppSecret、云环境 ID、数据库凭据或业务数据。
5. 旧 Web 代码需要参考时从归档分支按模块重写或移植，经新契约验收后进入 `main`，不整树合并。

## 2. MINI-1901 完成证据与边界

- 远端 `origin/catledger-web` 与标签 `web-v2-final` 均解析到 `39a0ea55`；
- `main` 保持连续历史，通过 `37be5cff` 重建文档契约，通过 `75b00ecf` 移除旧 Go/Vue/Docker 运行代码并加入原生小程序基础；
- `project.config.json`、全部应用 JSON、JavaScript 入口和 WXML 结构检查通过；`catledger-api` 在本地加载并正确拒绝未开放 action；
- 微信开发者工具 CLI 能读取项目路径，但仓库有意不保存 AppID，因此实际编译需在首次导入时选择测试号或配置个人 AppID；
- `wx-server-sdk@4.0.2` 是实现可信微信上下文所需的当前依赖，`npm audit` 报告其传递依赖存在 1 个中危、5 个高危且没有无破坏修复。当前函数不接收 URL、文件或数据库输入；MINI-1902 上线真实业务前必须重新核对官方 SDK 更新和实际可达攻击面；
- 未创建 MySQL schema、云存储桶或正式云环境，未上传真实账单，未更新或停止 8080/8082。

## 3. MINI-1902 冻结契约

1. 远端长期分支只有 `main` 和 `catledger-web`：前者是小程序产品主线，后者是冻结的 Web 归档；不建立 `develop`、`mini` 或其他长期集成分支。
2. 开发任务使用 `codex/<task-id>-<short-name>` 和独立 worktree，任务分支不推送；验收后快进或普通合并进入 `main`，随即清理本地分支与 worktree。
3. `web-v2-final` 固定迁移前 Web 产品；旧 `ezbookkeeping-sync` 的独有历史先保存为 `archive/ezbookkeeping-sync-final`，再删除远端分支。
4. AppID `wx958ebc33591b2bab` 是公开项目标识，可进入 `project.config.json`；AppSecret、上传私钥、云环境密钥和数据库凭据只能保存在平台或本地私有配置中。
5. 本任务只验证开发者工具项目、编译和分支结构，不创建 CloudBase 环境、MySQL schema、云存储对象或业务数据。

完成证据：任务提交 `12ff0519` 已快进集成到 `main`；微信开发者工具 `36.6.0` 已登录并使用真实 AppID 打开项目，CLI 预览编译通过，主包 `89,030` 字节；全部 JSON 可解析，JavaScript 语法、WXML 和 `git diff --check` 通过；`archive/ezbookkeeping-sync-final` 保存旧分支节点 `50784eda`，远端旧分支已删除；任务 worktree 和本地分支已安全清理。未执行上传或发布。

## 4. MINI-1903 冻结契约

1. `bootstrap` 是用户主动确认微信登录后的首个业务动作。未登录浏览不调用个人业务接口；客户端只发送动作名，云函数必须从 `cloud.getWXContext().OPENID` 取得身份，请求中出现 `uid`、`openid` 或同义身份字段时直接拒绝。
2. 数据库不保存明文 OpenID。服务端将 `wechat-mini:<OPENID>` 计算为 SHA-256 身份摘要，通过 `catledger_user_identities(provider, subject_hash)` 唯一约束映射到内部 UUID；响应、日志和客户端缓存均不返回内部 `uid` 或身份摘要。
3. 首版迁移只建立 `catledger_schema_migrations`、`catledger_users`、`catledger_user_identities` 和 `catledger_categories`。除迁移表外，每张业务表均带 `uid`；表、索引和环境变量全部使用 `catledger_` / `CATLEDGER_` 命名空间。
4. 初始化在单个 MySQL 事务中创建用户、身份映射和默认分类；重复、并发请求只能产生一个用户和一套默认分类，任一步失败必须整体回滚。迁移编号连续、forward-only、可重入并校验 SHA-256 checksum。
5. 默认分类只覆盖支出和收入；转账由交易类型表达，不伪装成分类。系统键稳定，中文名称可在后续版本中由用户调整；退款不作为普通收入默认分类。
6. 数据库使用官方推荐的 `mysql2` 连接池，通过云函数私有环境变量和 VPC 内网连接。仓库不保存数据库地址、账号、密码或密钥；允许像 AppID 一样保存非敏感的开发环境 ID，个人或生产环境配置不得提交。
7. 当前 AppID 下的旧宠物业务函数与 NoSQL 集合已按用户授权清理；云环境、AppID 绑定、认证配置和平台静态文件继续保留。本任务只新增 `catledger-api` 和 `catledger_*` 表；用户确认部署后，函数开发版已绑定 VPC 和受限 MySQL 应用账号，函数权限默认拒绝并只允许非匿名登录身份调用。

候选验证：`catledger-api` 的 11 个身份、隐私、请求契约和事务重试单元测试及 5 个 MySQL 5.7 集成测试通过；覆盖迁移重跑与 checksum、重复初始化、8 路并发唯一裁决、死锁与锁等待有限重试、双用户隔离、明文 OpenID 不落库和失败整体回滚。数据库集成测试另连续压测 30 轮通过。用户确认部署后，`catledger-api` 已以 Node.js 18 事件函数发布开发版，绑定 VPC、私有数据库变量和最小权限 MySQL 账号；函数状态为 `Active / Available`，无可信身份的管控调用正确返回 `AUTH_REQUIRED`。微信开发者工具已显式命中开发环境，连续两次 `bootstrap` 均返回 HTTP 200，页面显示“账本已准备好”和 16 个常用分类；第二次结果 `isNewUser=false`。云端聚合核对为 1 个用户、1 条 64 位十六进制摘要身份和 16 个分类（10 支出、6 收入），未保存明文 OpenID。`wx-server-sdk@4.0.2` 的 `npm audit` 仍报告 1 个中危与 5 个高危 CloudBase 传递依赖，只有降级到破坏性版本的修复建议；当前入口不接收 URL、代理配置或任意对象合并参数，暂不执行 `--force` 降级。

## 5. MINI-1905～MINI-1910 方向契约

1. MINI-1905/1906 先把账单转化为可追溯的正式事实：来源文件、数据日期、原始行、规范化记录、来源身份、经济事件、账户映射、整理决定和正式交易保持独立并可关联；不为后续“智能分析”牺牲证据、幂等或人工确认边界。
2. 所有导入业务统一采用“来源证据 → 经济事件候选 → 整理决定 → 正式交易”主流程。一个经济事件允许多份来源证据和多笔正式交易；贷款只是需要本金、利息和费用拆分的复杂类型，不另建一套导入主链路。
3. 跨来源关联与同来源去重分开处理：共享并核验通过的稳定关联号或已冻结官方强关系可以自动确认，仅金额、时间、文本、对方或账户尾号接近只能生成建议。无法确认的账户、用途、身份或关系进入待确认。
4. 消费、收入、本人转账、信用卡还款、退款和借款还款必须使用各自业务语义；同一事件的多来源证据不得重复入账。贷款缺少明细时不得猜测本金、利息和费用，有明细时组成项必须与实际还款总额守恒。
5. MINI-1907 的财务状态以正式账本同一数据快照为基础，分别表达净值、可立即使用资金和负债。账户流动性必须可解释并由用户确认；数据日期、覆盖范围、过期账户和待确认项属于结果的一部分，未知不得按零处理。
6. MINI-1908 的未来付款承诺、贷款和分期计划独立于已发生交易；计划不得提前进入余额或月度收支，实际付款必须通过用户范围内的幂等关系与经济事件、正式交易关联。最低保留资金由用户设置，系统不得根据画像自动决定。
7. MINI-1909 的情景测算是只读确定性能力：金额使用分单位，日期使用明确本地日期，四种方案使用同一数据快照、规则版本和用户输入。测算必须列出付款后可用资金、未来 30/90 天安全线、受影响支出、来源、假设和缺失资料，且不得创建或修改正式业务数据。
8. MINI-1910 的 AI 只解释已经计算完成的结构化结果，不负责解析权威事实或计算金额。模型默认不接收原始账单全文，不拥有数据库写权或付款能力；模型失败时用户仍能查看完整计算表和来源。该阶段必须先通过隐私、合规、成本和降级评估，并且不阻塞 MINI-1907～MINI-1909 的确定性能力上线。
9. 首轮仍只支持人民币，不承诺证券实时价格、房产估值、万能银行 PDF、多币种换算或投资建议；这些能力如需进入产品，必须另行冻结数据来源、估值日期、汇率和风险口径。

## 19. 当前下一步

0. MINI-1904C 已形成独立分支本地候选：公共 action、请求响应、schema 与界面不变；交易服务拆为领域校验、正式命令、明细查询和报表统计，明细摘要、首页与完整统计改为一次只读一致性快照；新增服务端、客户端和共享契约漂移测试。根目录 29/29、云函数非数据库测试 38/38 通过，17 项数据库集成测试因未配置测试库按设计跳过；未部署或推送，状态为待验收。
1. MINI-1904A/1904B 已按用户确认集成并推送至 `origin/main` 的 `b3448204`：核心账本补齐分类管理、关联退款和统一登录门禁；视觉系统与六主题统一轻量层级和财务颜色语义；客户端连接状态能区分身份连接与局部同步，只读瞬时错误只短重试一次，写动作不自动重放。开发云 `0003` 与 `catledger-api` 已先行部署并通过真实微信身份调用；真机验证保留为正式上传前门禁，尚未上传小程序。
2. MINI-1904 已以 `daa43fd6` 集成到 `main`，完成自动测试、开发云部署、真实微信身份闭环、六主题走查和真机预览推送；未上传审核前只称为“开发版已验证”。下一阶段是 MINI-1905 支付宝/微信单文件可信导入，领取前先冻结上传、类型识别、来源与数据日期、解析、来源证据、来源身份、经济事件候选、账户映射、待确认项和单文件原子入账契约；MINI-1906 在同一主流程上完成跨来源归组，不为贷款另建一套导入链路。不得提前把情景测算或 AI 引入解析权威链路。
3. 底部导航按用户确认收敛为“首页 / 明细 / 中央记账 / 账本 / 我的”；账本以统一点击行分别进入“账户管理”和“分类管理”，当前微信身份连接、隐私与应用信息归入“我的”。首页承担基础财务总览，不设置独立的账户、分类或统计底栏入口。
4. MINI-1904 默认界面沿用招财猫记账本品牌的暖米白、Logo 同源暖橘和轻巧现代层级；新增本机主题系统后，“我的”可切换暖橘手账、青绿清单、红印编辑、石墨秩序、墨绿留白和票据凭证。六套方案共享语义令牌与信息架构，不改变业务数据，不复制第三方 AGPL 代码、素材或页面结构。
5. 明细页支持具体日期选择；`transactions.list` 的可选 `date` 字段使用 `YYYY-MM-DD`，必须属于 `month`，日期进入筛选摘要和游标摘要，列表与收支摘要都按交易本地日期过滤。首页同时恢复迁移前产品的收入/支出趋势语义，`dashboard.get.cashFlowTrend` 返回截至当月的近半年聚合；`statistics.get` 提供完整月度收支、每日支出和收支分类排行。当前函数已更新到开发云，微信开发者工具以真实微信身份验证首页趋势、完整统计页和具体日期筛选均成功，函数私有变量与 VPC 配置保持不变。
6. 正式上传前重新评估 `wx-server-sdk` 传递依赖；账单导入闭环完成前不上传真实账单。
7. 产品登录采用按需触发：导入、记一笔、账户/分类管理和个人数据入口打开同一暖色登录面板，面板使用微信现行 `chooseAvatar` 与 `nickname` 能力提供可选头像昵称，两项留空也可登录；用户点击“登录”后才执行 `bootstrap`，成功后回到触发入口继续原操作，不统一跳转“我的”。选择的资料首版仅存当前设备；首页与“我的”只有在已登录且用户主动选择头像后才显示该头像，其余情况统一回退品牌 Logo，已登录但未填写昵称时显示“微信用户”；可信身份仍完全来自服务端微信上下文。
8. 本机主题任务已完成：只影响小程序本机视觉配置、页面语义令牌和主题选择页，不改变云函数、MySQL、身份或账本契约；六套主题已通过注册表测试、逐文件编译、开发者工具实际切换与跨页持久化走查，纳入 MINI-1904 最终验收范围。
9. 后续产品按“可信导入与正式账本（MINI-1905/1906）→ 财务状态与上线闭环（MINI-1907）→ 未来承诺和安全线（MINI-1908）→ 确定性情景测算（MINI-1909）→ 可选 AI 解读（MINI-1910）”推进。前一层数据和验收未闭环时，不用后一层展示掩盖缺失事实。
