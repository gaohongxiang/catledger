# 当前任务与验收状态

> 本文件是唯一现行状态入口，不再兼作完整架构、业务规则和逐日修复日志。流程见[项目逻辑说明](招财猫记账本入账逻辑说明.md)，完整历史记录见[整理前固定快照](https://github.com/gaohongxiang/catledger/blob/b804bfc405d3cabff5ff277732d657d3020efb47/docs/招财猫记账本实施规划.md)。本次整理不把任何原待验收任务改成已完成。

## 1. 当前优先事项

| 任务 | 状态 | 已知事实 | 剩余与边界 |
| --- | --- | --- | --- |
| DOCS-20260926 文档系统整理 | 待验收 | 基于 `b804bfc4` 整理逻辑主文档、规则、架构、专题、开发入口与历史索引；仅在 `codex/docs-20260926-reorganization` 交付 | 用户审阅后才合并 main；不修改业务逻辑，不部署，不改备份 |
| MINI-1915 一期减复杂度与模块化 | 待验收 | 主体和异步补修已进入整理基线的 main；原阶段规格及验证材料保留 | 手机完整业务走查未因代码/CI 通过而自动完成 |
| MINI-1915 异步补修 | 待验收 | 入账/刷新令牌、首次恢复意图有修复和真实 Page 回归，已合入 | 三项竞态的原生完整业务时序及手机操作待验收 |
| MINI-1910～1914 账户、全选、贷款、银行、分期 | 保留各原待验收状态 | 本轮不回退已合入实现，也不覆盖旧任务成果 | 各项剩余证据和已被后续方案替代的入口，按各规格及历史记录核对 |

本轮负责人为文档整理会话；Astra 的既有实现和发布记录作为历史事实保留。本轮授权是整理、检查、提交审阅分支；过去任务中的“合并、部署”授权不能自动套用到本次。

## 2. 文档整理基线与保全

开始时远端 `main` 与用户新建的 `backup-docs` 均为：

`b804bfc405d3cabff5ff277732d657d3020efb47`

本次不更新这两个引用。审阅分支以该提交为祖先，旧内容仍可按 SHA 读取；[归档清单](归档/README.md)逐项给出旧文档入口和原始 blob。38 个既有 specs 子目录不移动、不删改，新增总导航区分现行手册与阶段历史。

本会话通过 GitHub 读取与提交，不声称访问或同步了用户 `/Users/gaohongxiang/projects/catledger` 的本机工作区。已记录的旧 worktree 未提交成果必须保留，文档整理不授权清理它们。

## 3. 最近一次既有交付记录

以下是整理前仓库记录，不是本轮重新部署或真机验证：

| 项目 | 整理前已记录事实 | 本轮如何处理 |
| --- | --- | --- |
| MINI-1915 主体 | 主体交付 `4097a01f` 已进入 main | 保留实现与证据 |
| 异步补修 | `4901d845` 和 `b99d4874` 修复；后续记录至 `b804bfc4` | 在逻辑及恢复专题原位说明当前行为，不重复追加补丁长文 |
| 自动化 | 历史 main `72712a62` 的 CI `36030053183` 记录为成功 | 不借作本轮新提交的 CI 结果 |
| 客户端 | 记录已上传开发版 0.4.71；并有首页预览 | 不等于本会话确认手机验收通过 |
| 云端 | 记录两函数 Active/Available；仅前端补修未重复部署 | 本轮没有调用云端发布或迁移 |
| 分支清理 | 旧两个 MINI-1915 远端任务分支已清理 | 用户新备份另行保留，不再清理 |
| 本机旧工作区 | 历史记录明确另有未提交修改 | 本轮没有访问、覆盖、重置或删除 |

详证：[原始状态与交付全文](https://github.com/gaohongxiang/catledger/blob/b804bfc405d3cabff5ff277732d657d3020efb47/docs/招财猫记账本实施规划.md)、[阶段规格导航](../specs/README.md)。

## 4. 本轮文档验收清单

- [ ] 用户能只读逻辑主文档，讲清项目架构、核心对象及完整导入流程。
- [ ] 每个主要步骤都有输入、判断条件、结果和失败/重试边界，且能定位代码。
- [ ] 当前页面与保留后台接口分开，历史与未来计划不冒充当前能力。
- [ ] 需求、规则、架构、专题、开发与状态各有职责，没有第二份现行任务表。
- [ ] 原文和阶段资料可通过归档/固定提交找回，main 与 backup-docs 未被修改。
- [ ] 新提交的文档检查和 CI 结果已核实；不将未运行的本机、数据库或真机项目计为通过。
- [ ] 用户明确批准本次文档合并；此前保持审阅分支或草稿 PR。

文件间链接与锚点由轻量检查约束；它不能自动证明文字与实现完全一致，仍需上述内容验收。精确审阅提交和 CI 在 PR/交付结果记录，避免为在文档写入自身 SHA 而不断追加自引用提交。

## 5. 后续范围与状态维护方式

本轮不启动 Jev/AI 接入，不增加缓存体系，不继续扩展 MINI-1915 重构，也不实施复式分录、影子双写或财务预测。历史候选方案原文仍保留；是否启动另行明确收益、边界与授权。

以后只在本文件更新“当前任务、剩余阻塞、最后一次交付指针”。完成记录进入对应阶段 README 或 Git 提交，不在这里粘贴完整测试输出。状态使用未开始、进行中、待验收、已完成、阻塞；缺少集成、规定验证、必要文档或用户验收时，不标已完成。
