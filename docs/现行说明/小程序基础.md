# 招财猫记账本小程序现行说明

## 当前行为

### 工程与身份

- 仓库根目录可被微信开发者工具识别为原生小程序项目，项目使用 AppID `wx958ebc33591b2bab`；
- 小程序未登录时可以浏览首页、明细、账本和“我的”的公开结构，不调用 `catledger-api.bootstrap` 或其他个人业务接口，也不以零值或演示数据伪装个人账本；
- `catledger-api` 只从微信云函数可信上下文取得 OpenID，将其摘要映射到内部用户，`bootstrap` 向当前用户返回已有 uid，页面统一称 ID；不返回 OpenID 或摘要，普通日志不记录 uid；
- `catledger-api` 使用公开动作注册表和薄交易门面组织模块：交易领域校验、正式写入、明细查询和报表统计相互分离。写动作使用用户范围的幂等事务，明细摘要、首页和完整统计分别在同一 MySQL `REPEATABLE READ` 只读快照内完成；`accounts.createBatch` 仍是普通账本兼容动作，但多账单导入不再调用它提前创建账户；
- 首次初始化在一个事务中创建用户、身份映射和 16 个常用收入/支出分类，重复或并发调用复用同一套数据；

### 正式账本

- MINI-1904A 开发后端已在既有账户与手工交易底座上增加分类新增、改名、排序、停用、恢复和关联原支出的手动退款；退款作为到账流入并冲减支出、趋势及原分类统计，不计为收入，累计金额受原支出约束；开发环境已应用 `0003` 并更新 `catledger-api`，正常 AppID 项目窗口的真实微信身份回归已经通过；

### 页面与视觉

- 本地页面包含暖橘色首页、明细、账本、我的、记一笔、账户管理、分类管理和多账单统一导入工作台；底栏仍使用“首页 / 明细 / 中央记账 / 账本 / 我的”。中央“导入账单”入口完成按需登录后进入独立工作台；
- 多账单工作台本地候选一次最多选择 5 个支付宝 App/Web CSV、微信 CSV/XLSX；单文件不超过 5 MiB、规范化记录不超过 5000 条。服务端用一次 `imports.prepareMany` 签发全部用户隔离随机路径，客户端最多 5 条“直传→逐文件解析”流水线并行执行，瞬时网络或上传超时会沿用同一路径自动补传；单次上传没有完成回调时主动中止，页面收口为可独立重试的中文错误，不透传平台底层信息。云函数按内容探测并独立持久化文件摘要、原始行、解析版本和错误；单文件失败可重试或移除，不污染其他来源。文件内容相同不直接等于已入账：明确重新上传时，上传和正文解析仍执行，持久化前按内容摘要裁决；未提交来源复用原不可变解析批次，若它仍在未入账更新中则保留审计并把旧 FinanceUpdate 标为 `abandoned`，旧资金方向、账户草稿和待整理决定不进入新更新。重复裁决由系统内部处理，未入账文件仍以普通“解析完成”收口，不向用户展示 FinanceUpdate、批次复用或作废术语；只有已随正式交易提交的来源才提示“已经入账”。用户只是返回同一工作台且仍持有 `updateId` 时才恢复原 FinanceUpdate，不能与明确重新上传混用。首次进入账户步骤只调用一次 `financeUpdates.prepare`，服务端在一个幂等事务中建立 FinanceUpdate 并完成首次整理，不再暴露 create / organize 两段半流程。页面只展示打开的 ReviewIssue，不再展示逐行“将入账 / 不新建”；账户归属单项确认先持久化并标记本地已核对；后台合并调用 `reviewIssues.resolveAccountMappings` 保存全部待提交组，每个请求最多 50 组并使用一个 MySQL 事务，超出时在同一次等待中顺序分批保存，Update 只锁一次、批内共用当前映射索引，全部决定完成后仅重算受影响事件、计数一次并返回一次完整视图。新建账户和支付工具映射在此阶段只保存为 FinanceUpdate 草稿，正式账户和规则不变。所有阻塞问题清零后只有一个“整批入账”动作，一笔 MySQL 事务物化新账户、提升映射并写完全部正式交易和链接，失败整体回滚。入账前可常驻入口放弃整批，成功后清空工作台并释放来源，正式账户、映射、交易、余额和统计不变。退款、转账、还款保留各自经济语义，不再默认跳过；
- 页面采用“暖橘编辑式轻账本”视觉基线：统一 `32rpx` 页边距、主要操作 `88rpx` 触控区、固定字阶和安全区；普通内容以留白、金额对齐和细分隔线建立层级，不使用格纸背景，也不堆叠阴影卡片。净值区保留主题色与原有高度，本月摘要、趋势、账户和账目改为连续信息流；六套主题共享中等字重、细边界和柔和浮层阴影，中央加号与底部面板保留必要的强层级；

### 登录与连接

- 导入、手动记账、账户管理、分类管理和个人数据入口统一按需打开暖色登录面板。用户点击“登录”后才调用 `bootstrap`；登录成功后留在触发页面并继续原操作，不统一跳转到“我的”，头像昵称不再前置阻塞登录；
- 登录面板已抽为共享原生组件；账户、分类、记账和统计子页面被直接打开时也先走同一产品许可门禁，未许可不会调用个人 API；
- 登录面板首次打开按字段保留已有资料，缺少时提供品牌猫头像与随机昵称（从24个完整名字选择，不拼数字），立即写入本机资料缓存；关闭重开或重启复用，清空昵称提交时复用已生成昵称。用户可通过 `chooseAvatar` 与 `nickname` 修改，包内品牌头像不调用 `wx.saveFile`。默认与自选资料仅在当前设备用于展示，随机昵称不保证唯一、不用于身份判断，生成资料不授予登录许可、不调用云端业务接口；首页未登录时头像位置显示登录按钮；已登录首页与“我的”在存在自选头像时展示该头像，否则回退品牌猫头像，历史已登录但未填写昵称时显示“微信用户”；
- “我的”提供退出登录，确认后清除本机登录许可、头像昵称和临时编辑状态，不删除或修改云端账本；退出后仍可公开浏览，受保护功能会重新打开登录面板；
- “我的”只保留主题、数据与隐私、关于和退出登录，不再重复提供账本设置；账户与分类统一从“账本”进入；
- 客户端云调用统一保留稳定的公开错误码：只读动作遇到网络、超时或云端明确瞬时错误时自动短重试一次；只有公共契约已声明为幂等且携带有效 `requestId` 的写动作才允许自动恢复一次，其他写动作绝不盲目重放。两个云函数对连接重置、超时、死锁和锁等待使用新连接有限重试，并把可恢复故障映射为 `SERVICE_TEMPORARY_UNAVAILABLE`；失败日志只记录 action、平台追踪 ID、稳定错误类别和耗时，不记录请求 data、账单正文、身份或 SQL 绑定值。开发者工具缺失 AppID 时返回可识别的项目上下文错误；客户端命中新动作而开发云函数仍是旧版本时明确提示“导入服务版本过旧，请更新云函数后重试”，不再误报“操作尚未开放”；"我的"只以 `bootstrap` 判断个人账本连接，局部账户摘要失败时保持已连接并提示重新同步，不重复显示同一连接错误；

### 主题、首页与明细

- “我的”新增无需登录的“主题”入口；主题页提供暖橘手账、青绿清单、红印编辑、石墨秩序、墨绿留白和票据凭证六套原创方案。选择只写入本机 `catledger_theme_v1`，即时同步页面、微信导航栏和自定义底栏，不调用云函数、不修改账目；主题只改变品牌色和受控圆角，不改变字阶、间距、触控尺寸或信息架构，六套主题统一使用收入绿 `#477153` 与支出红 `#B54738`，无效存储值回退暖橘手账；
- 首页只展示净值、本月收入、支出、收支结余、已有账户摘要和最近 3 条账目，不另设统计底栏；更多交易统一从“全部”进入明细。既有用户回访直接调用一次 `dashboard.get`，只有服务端明确返回 `INITIALIZATION_REQUIRED` 才初始化后重试；服务端从近半年趋势结果复用当月汇总，不重复扫描当月交易。无账户时不显示创建卡，无交易时使用轻量文字空状态。首次加载失败时保留概览结构并以“—”表示未知，不使用零值或演示数据冒充真实账本；客户端屏蔽 `cloud.callFunction` 等平台原始错误；
- 首页账户摘要最多展示 3 个活动账户，按现金、银行卡、平台钱包、信用/负债和其他类型显示本地图标，不再使用占空间的横向大卡片，也不展示已停用账户；现金账户历史负数保留负号便于纠正，服务端不允许新的手工交易或整批导入新增/加深现金负余额，不用界面取绝对值或归零掩盖；
- 首页的“统计分析”沿用迁移前产品的收入/支出趋势语义，以轻量双柱图展示截至当月的近半年；它与“账户余额”、“最近账目”共用缩小后的标题字体和右侧“全部”入口。完整页展示月度收支、每日支出趋势和收支分类排行；数据由服务端从正式、未删除交易聚合，不使用客户端分页列表估算；
- 明细页删除顶部“记一笔”和重复大标题，月份、横向收支摘要、账户/分类/备注筛选和明细列表紧凑排列；点击中间日期可选到具体日期，本地代码会将日期交给服务端按交易本地日期筛选；

### 工程配置与验证

- `app.json` 已启用 `requiredComponents` 组件按需注入，避免启动时注入未使用组件；
- 小程序通过 `miniprogram/config/cloudbase.js` 显式选择公开的开发环境配置，避免误调用同一 AppID 下的旧默认环境；
- MINI-1903～1906 的开发云基础闭环已验证；2026-09-05 开发云迁移推进至 `0010`，应用账号权限已核验，两个函数已更新为 MINI-1906V2/A1 开发候选（`organizer-plan-v26`）。真实微信身份已完成八月两文件重新解析、整理、账户来源及已入账记录只读查看；正式账目保持不变。当前尚未提交微信审核或正式发布，真机完整流程及真实语义差异仍待验收；详细证据见实施规划 21.4。

V2 本地候选的语义分层、覆盖口径、迁移 0009 与验证命令见[账单语义与工程验证](./账单语义与工程验证.md)。该说明不改变上述开发云部署事实。

## 数据流

常规账本流为“未登录公开浏览 → 用户在受保护动作中打开登录面板 → 可选头像昵称 → 点击登录 → `bootstrap` → 云函数可信微信身份 → SHA-256 身份摘要 → MySQL 身份映射 → 初始化或读取账本 → 原动作继续 → 账户/交易事务 → 服务端余额与统计 → 返回公开字段”。导入流为“`prepareMany` 一次签发随机对象路径 → 最多五条私有上传/解析流水线并行 → 各文件独立内容探测、规范化和证据持久化 → `financeUpdates.prepare` 单事务建立并首次整理 FinanceUpdate → `resolveAccountMappings` 单事务保存账户/映射草稿并定向重算受影响事件 → 其他 ReviewIssue 决定与服务端 postability → 整批 posting 单事务物化账户、提升映射、写正式交易和链接 → 尽力逐来源清理临时文件”。入账前放弃则关闭 FinanceUpdate、释放来源并清空工作台，不跨越正式账本写屏障。客户端不能传入 uid、OpenID 或事件状态，数据库连接只读取云函数私有环境变量。

## 扩展点

- 常规业务进入 `cloudfunctions/catledger-api/`；
- 账单文件处理进入 `cloudfunctions/catledger-import/`；
- 公共请求/响应契约进入 `shared/`；
- 服务端公开动作、客户端静态调用与 `shared/catledger-api.json`、`shared/catledger-import.json` 由仓库测试检查漂移；
- MySQL 迁移进入 `migrations/`。
- 小程序视觉主题进入 `miniprogram/theme/`：`registry.js` 只保存纯主题元数据、语义令牌和序列化函数，`service.js` 独占本机存储与微信系统栏适配；业务页面不判断主题 ID。

开发环境部署读取根目录 `cloudbaserc.json`：`catledger-api` 与 `catledger-import` 均随包上传本地依赖并关闭云端重复安装，部署前需分别在函数目录执行 `npm ci --omit=dev`；配置文件不保存数据库地址、账号、密码或其他私密环境变量，部署必须保留云端既有 VPC 与私有变量。`catledger-import` 同时需要从 VPC 访问 MySQL、从私有云存储读取上传对象，因此保留公网出站能力，但不配置 HTTP 触发器。每次迁移或 SQL 写路径变化后都须以 `npm run check:db-permissions` 复核受限应用账号的运行权限，不再只核对表级 GRANT；账户归属批处理对 `catledger_review_issue_members` 只持有 `UPDATE(object_version)` 列级权限，其他字段不可更新。

`wx-server-sdk@4.0.2` 保持当前正式版本，`axios` 与 `@cloudbase/database` 通过兼容版本覆盖消除了历史传递依赖告警；两个事件函数的 `npm run audit:prod` 当前均为 0 个已知漏洞。事件函数不接收任意 URL、代理参数、请求头或 SDK 配置对象，客户端公开数据限制为 1000 个对象节点并拒绝身份字段，响应与日志不回传事件、上下文、环境变量或数据库绑定值。正式上传前仍须重跑依赖审计并复核上游 SDK 是否已吸收安全版本，届时应移除临时覆盖。

旧宠物项目的业务函数与 NoSQL 集合已按用户授权清理；云环境、AppID 绑定、认证配置和平台静态文件继续保留。当前开发云环境的 MySQL、19 张 `catledger_*` 表、迁移 checksum、唯一索引、外键和受限应用账号均已验收。开发版保留内部合成验收数据，包括本轮单文件导入创建的 1 条合成交易及其证据链接；没有保存明文 OpenID。用户本次真实账单仍处于逐笔确认阶段，原文件按私有临时文件策略保留，只有提交、取消或生命周期清理后才删除。

## 版本规则

开发阶段只维护一个当前 `catledger-api`、一个当前 `catledger-import` 及各自的 `shared/` 契约，直接覆盖更新，不建立并行接口。小程序正式上传时再使用语义化版本号；数据库迁移按四位编号递增且已登记文件不可修改。开发版未经过微信审核和发布时，不视为正式可用版本。


### MINI-1906UI：字阶、主题和导入导航

全 11 页与底栏、登录弹层使用正文 26rpx、辅助 22–24rpx、区块 30rpx、标题 34rpx，装饰角标 20rpx、首页辅助文字 18rpx；关键金额单独放大。页面使用共同左右留白、可换行账户名和主题变量，保留 Logo 与用户头像回退。六套主题保留原主色与 hero 配色，首页净资产、记账金额区和主题缩略图使用有辨识度的主色块；问题卡片保留柔和主题底色。收入绿、支出红保持语义。

导入第三步分交易核对与分类，核对内含待核对、按原因展开的已排除、可对照来源的重复记录。分类使用已有 ReviewIssue 冻结分组和决定接口；计数标为待分类笔数／组数，搜索摘要，完整成员在处理弹层查看。浏览证据不修改账目，分类仅写分类字段。所有阻塞问题与分类门槛仍在整批入账前校验。


用户补充后的主题色阶：暖橘（纸白／浅橘／暖灰）、青绿（薄荷／青绿／深绿）、红印（浅粉／砖红／深红）、石墨（浅灰／石墨／深灰）、墨绿（浅苔／苔绿／墨绿）、票据（奶油／麦金／赭棕）。普通边线、标签、说明和按钮使用同主题深浅色；财务收入／支出与危险操作单独保留语义色。主色摘要与浅色列表同时存在，避免所有页面统一淡化。部分深色 hero 稍加深，确保浅色数字与说明可读。


密度以完整信息优先：首页主卡保留原有 166rpx 最小高度与 22／26rpx 内边距，账户行 80rpx、最近交易行 92rpx，常用手机尺寸优先同时露出三条最近账目。关键金额 48rpx，月度与列表金额 26–28rpx，首页区块标题 30rpx／600，条目名称 24rpx／400，时间和余额标签 18rpx；其他页面共享字阶但不一律放大行高。特别长名称和系统字体放大时允许自然滚动，不截断正式金额。


所有页面采用两条横向基线：卡片外沿距屏幕 32rpx，卡片外的标题／说明／列表文字再内收 20rpx；卡片已有的内部留白保持。通过公共 content-inset 类统一首页、明细、账本、我的、账户、分类、统计、主题、记账、导入与维护页的相关区块，右侧金额和操作同样内收，避免文字贴着圆角外框显得突出。

首页返回切页时保留已有余额和账目，刷新状态在“账户余额”标题后的固定宽度按钮中旋转，失败在同一位置提供重试，不插入状态行。统计绘图区增至 168rpx，柱形区 136rpx；柱形以绘图区百分比呈现，避免放大后比例或顶边溢出。

六主题的卡片、筛选器、按钮和边线统一由 theme/registry.js 提供。暖橘使用中性纸白底、浅橘问题卡头和暖灰说明文字，红印浅层使用浅红；导航、账户与管理图标使用各主题的本地 SVG，图标色板变更后运行 node scripts/generate-theme-icons.js，同步生成产品资源。收入绿／支出红继续表达财务语义。

刷新稳定性：已加载页面保留已有数据和空结果；“我的”返回时不清零数量、不暂时撤销已连接状态。首页、明细、账本、账户、分类、统计与个人页仅在固定位置显示更新图标，删除数据流中的临时加载行。主题切换标记保留固定宽度与边框厚度；系统栏不重复播放颜色过渡。账户证据和统计分类弹层在读取前后保留同一高度，长内容在内部滚动。

首页账户余额标题后保留固定刷新槽，仅更新期间显示细线圆环，结束后隐藏，失败时原位重试。容器使用透明 view，避免微信原生 button 的禁用样式覆盖主题底色；重复点击仍由 loading 守卫拦截。

导入步骤轨道使用透明背景，选中项用实色步骤圆点区分，连接箭头对齐相邻圆点中心；核对筛选减少重复色块和粗下边线。账户步骤标题与说明、账户名共用左侧基线，不叠加 content-inset。

分类列表与交易核对复用同一紧凑卡片：单行浅色状态头、右侧分类操作、日期／交易内容／来源／金额行；不再重复展示大商户标题和第二行笔数。商户只用于搜索和排序，问题组与批量保存边界不变。点击卡头进入分类，点击交易行查看原始证据且不同时打开分类；多笔省略提示在原卡片底部显示。

分类下提供「未分类／已分类」，默认未分类并显示各自总笔数。已分类从当前批次有分类的有效收支／手续费事件生成，展示现有分类、前置日期和交易证据；原始识别已有分类同样可查看。搜索作用于当前列表但不修改总数；分类保存重读后更新两个列表，最后一组完成时留在分类页，点击下一步进入入账确认。已分类本轮提供只读复查，不新增改分类接口。
整理默认待核对，分类默认未分类，不设置全部筛选或额外进度卡。两视角共用有效／排除／明确重复来源集合，顶部拆分总数与入账确认／成功页一致；有效数按唯一经济事件计算，明确重复数按 duplicate 证据关系计算，不等同于文件原始行数。核对支持只读已核对，含自动通过；分类只保留待分类／已分类／无需分类，已排除／重复只在核对查看，所有有效交易均有归属。前置核对未完成的未分类记录可跳到已有问题；待处理数按真实成员去重而非问题组数。搜索匹配完整组成员但不改批量保存范围与总数；同批保存后留在当前页签，下一步由原完整性与阻塞门槛控制。
整理页核对／分类的二级状态栏统一单行，选中态仅用浅色背景与文字强调，不带底边框；核对四项、分类三项均等宽同屏显示，状态名称与数量不换行。

顶部数量说明使用 18rpx 双等式，每个等式固定一行：有效＋已排除＋重复＝全部记录；有效＝待核对＋已核对＝待分类＋已分类＋无需分类。均取未受筛选影响的总数，整理／确认入账／完成页保持一致。

整理标题复用账户页层级：「账单整理 · STEP 03」／单行标语「核对每笔交易，按需补全分类，让收支更清晰。」，不另设重复说明。分类为非阻塞建议，入口保留批量选择与证据，下一步和入账只以必须核对事项及服务端完整性为门槛。入账后可从现有补分类入口继续完善。

核对弹层的普通字段保存按钮与提交共享 `buildIssueFieldsDraft` 校验：账户占位、无效新名称、未知性质、缺少或相同资金端点均禁用，字段修改后即时重算；组合支付与合并还款沿用各自完整性门槛。两端都缺的资金卡提供两个独立账户选择器。组合支付复用相同资金卡，左侧展示所选付款账户，右侧在还款时选择真实目标、消费时展示用途，不增加虚构账户；目标与分项表单共用状态。


2026-09-06 组合金额与分隔线：组合支付复用还款分配行的金额框与“全部”；两账户输入金额联动差额，多账户仅全部自动分配。零金额须显式确认，保存仍要求账户／性质／目标／说明完整。所有处理弹层证据区使用统一 3rpx 单分隔线。


2026-09-06 组合付款展示精简：页头不重复解释支出／还款，资金卡后保留性质选择；“付款分配”标题右侧显示橙色“还差／超出”金额，合计一致显示绿色“已分配完成”。标题下只保留“按支付详情填写各账户金额。”，核对说明短标签与输入同行，仍为必填。所有交易处理弹层的 3rpx 证据单线使用更深的 theme-text-subtle。仅展示与文案调整，不改变金额、身份、保存和正式入账规则。

账户确认列表使用白底容器和细分隔线；已确认项收为两行摘要，点击更改打开选择面板，修改后再次确认。待确认项的方式、名称、类型同排白底，状态跟随标题，确认按钮下一行靠右。单项确认立即在本地收起并持久化，后台自动同步；修改后需重新确认。第二至第四步固定底栏提供上一步，第二步下一步保留“还需确认 N 项”；全部确认后下一步等待同步并校验，成功才前进，失败保留尚未提交的输入与确认，重试复用发送前持久化的完整幂等请求。版本与接口保持不变；细则见入账逻辑说明 6.3.1。

整理页恢复原标语“核对每笔交易，按需补全分类，让收支更清晰。”。两条原公式完整常驻且各占一行：有效交易 + 已排除交易 + 重复交易 = 全部记录；有效交易 = 待核对 + 已核对 = 待分类 + 已分类 + 无需分类。分类部分不随页签隐藏，仅移除重复总数行与展开收起入口，不改变数量和统计。

已有导入批次返回第一步时，解析摘要下并排显示“放弃本批账单”和“下一步：确认账户”；下一步仅返回已开放的账户步骤，不重新解析，放弃沿用原确认弹窗和处理接口。

整理步骤的“下一步：确认入账”按钮在待核对交易非零时附带“还需核对 N 笔”，直接使用待核对页签的交易数，清零后不显示括号提示；原禁用条件不变。

导入草稿由 `services/import-draft-session.js` 统一管理，账户、核对、分类及多笔同账户选择共用持久化队列。仅在服务端验证批次归属后恢复，离页后未结束的请求继续落盘，重新进入复用会话；网络错误有限退避重试，业务冲突保留原选择供重新核对。客户端仅投影已选择的状态与分类，不计算正式金额、退款、重复合并或最终就绪状态。下一步读取权威结果，入账保留完整幂等请求并使用原子 post；放弃先等待在途请求结束。

导入步骤切换刷新：草稿同步回调将服务端投影与当前步骤、忙碌状态合并为一次 `setData`；下一步等待同步并校验剩余事项后切换，失败保留当前步骤和选择。常规同步不插入占位提示，仅错误显示原有重试入口。入口：`import-workbench/index.js` 的 `applyUpdateView`、`finishDraftStep`；回归：`test/import-account-choice.test.js` 的步骤切换中间帧检查。

重新进入未完成导入自动恢复内容并停在第一步，保留原文件、选择和后台同步；复用放弃本批／下一步，不弹恢复确认。已入账记录仍显示结果页。资金信息卡复用选中账户浅橙底色。

自动恢复的加载区可直接“放弃上次导入”。点击即停止后续恢复，当前在途请求落定后按最新版本放弃；成功才清本地草稿，失败保留并可重试，迟到结果不覆盖页面。

还款归属弹层的他人还款处理方式使用全宽两行选择项：主标题为是否收回，小字说明记为支出或保留待核对，右侧圆形选中标记，选中沿用浅橙底。

导入输入期间后台保存继续，页面投影延后到失焦；更新不再重发整份编辑表单，账户／付款／还款列表按变动字段更新，避免名称、金额和说明被后台重绘干扰。

确认入账页标题、常驻双公式、数据汇总统一内容缩进；双公式在完成页同样直接显示，确认页汇总为静态数据展示。

第一步摘要跟随当前账户确认和交易核对状态，不再以原始未完整识别数提示剩余待办；已排除或已核对的记录不会因此要求重复处理。

确认页的数据区展示已分类、未分类、无需分类笔数及入账后可补分类的说明；数据复用整理页分类状态。


确认页分类计数合并为一行；收入逐笔清单仅在对话中按用户要求展示，不添加到确认页。

分类摘要的“一行”指复用上方金额区的一行三等份布局，每格标题在上、数字在下并以竖线分隔，不是串联三个文字计数。

确认页分类摘要以“交易分类”为独立标题，下方保持已分类、未分类、无需分类三等份布局。

确认页“资金流转”展示内部转账、借款、还款三等份金额和笔数；点击底部弹窗查看记录及关联账户。汇总只含当前 ready 事件，未分类不影响。

确认页全部金额、笔数、账户与分类汇总统一底部弹窗查看，取本地数据，无列表网络请求；交易行只展示摘要，不跳转原始凭据。六类金额项统一显示名称、金额、笔数，分类数量带“笔”，查看箭头统一跟随笔数；交易笔数共用22rpx、400字重的次要文字样式，金额保留强调。资金流转不再在页面内展开。

普通页面请求通过read-policy/read-cache统一复用：查询在连续前台使用期间保留，本机写入按领域失效，App从后台恢复统一失效；完整参数区分、并发同查询合并，首页完整账户可供账户列表复用。正式写入按领域失效，退出清空内存及旧会话视图。页面命中不闪加载，失效后保留内容重读，已有手动刷新强制联网；明细回访保持分页，记账仅退款时查原支出。导入草稿读取不加入通用缓存。策略、边界和扩展矩阵见specs/mini-1906ui-request-efficiency/README.md。

统计页复用单次statistics.get返回绘制月收支柱图、累计结余折线、收支同卡分类环图及支出日历；点击图表本地查看，月份选择按参数读取。当前无收支提示并可跳到近六月最近有收支月。累计结余不等于余额，负净支出保留；金额字符串精确计算，坐标近似绘制。展示模型见pages/statistics/model.js，回归见test/statistics-charts.test.js。

统计空月提示收口：空月条件与近六月跳转逻辑不变，移除独立说明卡和收支口径长文，改为单行“本月暂无收支 · 查看最近有记录月份”；无可跳月份时只显示前半句，不新增请求。

统计页最新展示调整（0.4.37）：恢复日期左右切月箭头，日期文字本身仍可打开月份选择器且不带下拉箭头；删除空月近期跳转及交易笔数/记账天数/日均支出/单笔最高四块指标卡，收支构成紧接顶部收支总额，先于半年趋势。指标仍可存在于原服务端响应，不新增请求、不改变统计口径。切月沿用chooseMonth加载与保存门禁。

统计收口0.4.38：待分类入口移至支出环图下的紧凑文字链接，仍打开原本月全部未分类收支面板，不将总数误标为仅支出；收支分类默认收起，点同一环图展开/收起，切另一环图切换，换月重置收起，全为本地状态。去掉越界选中横线并明确按钮border-box约束；累计结余图移除，保留顶部结余、分类构成、半年趋势与支出日历。

0.4.39分类入口收口：移除环图下独立待分类链接，在展开分类的categoryId为空行显示红色“待分类 N笔”，保留该分类金额与占比，点击该行进入原本月补全面板。N沿用本月全部未分类收支总笔数，入口打开同一全月集合；已分类行点击不触发补全。

导航0.4.41：底部账本入口替换为统计，我的页在设置之前提供账户管理和分类管理，沿用登录门禁与原管理页面；首页资产/账户入口保留。首页与导入统计使用switchTab；导入补全通过一次性openStatisticsCompletion意图进入当月统计，加载成功打开补全面板，跳转失败或退出登录清除意图。普通切页保留月份和展开状态并复用读取缓存；统计onShow同步Tab选中及会话隔离，退出登录清空旧视图，未登录不读数据。分类读取回调校验会话。无数据库写入/事务变化。入口：custom-tab-bar、pages/profile、pages/statistics、pages/index、pages/import-workbench；验证：test/cached-page-requests.test.js及原开发者工具导航走查。


TRANSACTION-EDIT-043：编辑账单按点击单笔打开；导入信息只读、收入/支出分类可改，手工账单沿用信息与分类编辑。移除整批维护页面路由及工作台入口，当前注册10页；分类筛选新增全部分类下方的未分类，须与云端新筛选逻辑配套。


首页登录入口：未登录时页头右侧显示紧凑“登录”按钮，隐藏该处头像；登录后同一位置显示用户头像或默认猫头像。移除最近账目后的登录按钮，点击仍打开原共享登录面板并返回原入口。面板头像下提供“使用微信头像”选择按钮；昵称使用紧凑单行框，下方“使用微信昵称”仅聚焦原生 nickname 输入框，用户仍需在微信候选中确认，候选样式由微信控制。默认资料、身份和入账规则不变。


首页页头采用单行左右布局：左侧“把账，记得有温度。”，右侧未登录显示实主题色白字胶囊登录按钮（可见112×56rpx，外层触控高度88rpx），登录后显示头像；删除“我的账本”角标，月份移至净值卡右上角，与“人民币净值”同行。月份继续使用原 monthLabel，不改变净值或月度统计计算。


首页账户与最近账目的空状态文字在区块内居中，包括登录提示与辅助说明；真实账户、交易列表继续使用原对齐方式。仅样式调整，无数据和请求变化。


明细与统计未登录空状态提供居中的实主题色白字胶囊登录按钮，复用原登录面板，成功后仍在当前页加载数据。统计单独维护产品登录许可展示状态，读取中或读取失败不显示登录按钮；昵称使用24个完整名字的本机随机展示资料，允许重名；用户定位使用“我的”页展示的原 uid（ID）。

首页、明细、统计与我的页登录按钮统一可见112×56rpx的实主题色白字胶囊，外层保留88rpx触控高度，位置与原登录回调不变。暖橘主题为橙底白字，其他主题跟随主题主色。昵称与 ID 规则见 MINI-1904E。


## 账号 ID 与轻量昵称（MINI-1904E）

“我的”昵称下完整展示10位数字ID，点击复制同一值，值就是 `catledger_users.uid`，经可信微信身份对应的 `bootstrap` 返回；没有另设公开编号、新业务表或额外身份映射。旧UUID通过0011统一迁移，重登沿用迁移后的值。uid 只留在本次登录的内存及受会话保护的读取缓存，退出清除；旧请求不能回填，旧后端缺少该字段时显示“ID 暂未获取”并提供重试。仍拒绝客户端 uid / OpenID，ID 不参与客户端鉴权。

新默认昵称从24个完整名字中选取，允许重名；“换一个”只改面板草稿，登录成功才保存。原有昵称和头像继续优先、仅本机保存。`catledger-api` 已于2026-09-10按用户明确授权更新，两处uid透传已生效；当前真实微信会话已确认收到原uid。界面已在原开发者工具编译，尚未重新上传小程序；新用户10位UID代码已于22:10:50更新，旧用户迁移已完成，具体证据见MINI-1904F验收记录。


MINI-1904E 界面收口：昵称下只保留ID；移除常驻成功连接文案和三列摘要卡片，账户/分类计数仍由原请求更新并在管理入口展示。未取得ID且正在读取时显示“ID 读取中…”，读取结束仍缺少时显示“ID 暂未获取”；点击该处重试或错误提示时以force重新读取bootstrap与账户数据，避开旧响应缓存。内部connected仍区分身份和局部摘要结果，失败不伪造零值，也不清除同会话已知ID。首页三个分区标题改为28rpx、页头头像56rpx/16rpx圆角；金额、图表高度与比例、请求和数据流均沿用原逻辑。验证入口为既有page-refresh、cached-page-requests、ui-all-pages、ui-system及微信开发者工具；不为尺寸另写测试。


ID显示长度：`profilePresentation.displayUserId`完整保留服务端值；迁移后展示和复制都是同一10位数字UID。displayUid随uid一起在退出与换会话时清空，不参与缓存键、云端查询或身份验证。cached-page-requests回归验证完整显示、复制与旧会话失效。

MINI-1904F：新用户由服务端生成10位数字字符串，数据库唯一主键与bootstrap整事务重试裁决碰撞；旧UUID通过0011在暂停业务访问的维护窗口完成29张业务表迁移，非UID数据、时间、版本、幂等结果和已有对象路径保持。迁移使用临时加密映射支持分段恢复，执行后清理；迁移脚本与完整验证入口见migrations/README及入账逻辑说明。开发云切换状态以规划看板本任务验收记录为准。

MINI-1904F开发云状态：2026-09-10最终开发云切换已完成：1个旧用户改为10位UID，29表79944行的数量与迁移前一致且UID格式全部通过，72个外键验证通过；账户、分类、正式交易及用户表非UID字段和可信身份归属聚合指纹与迁移前完全一致。0011原checksum已登记。真实微信会话已确认10位UID、页面显示与app会话同值、无加载错误。catledger_app已解锁，临时函数/账号/存储过程均已清理。缓存调整尝试未生效，最终仍为原256MB；未扩容或购买。
