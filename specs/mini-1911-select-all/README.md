# MINI-1911 明细完整全选

状态：待验收。用户要求修正“全选只能选 100 笔”。当前主会话串行负责，基线 main@62abdf57，直接在用户 catledger 主目录修复，保留已有账单设置、首页修复和其他 UI 改动。

契约：全选覆盖当前月份/日期、账户、分类、来源、搜索及导入范围内的全部可选手动/导入交易，自动读完分页；不包含余额调整，不跨筛选。手动选择不再限制 100 笔。读取失败保留已选，未读完不标全选；重试继续，换会话/筛选/数据版本取消旧选择。

删除仍由用户确认准确笔数，冻结原 ID/version/requestId；同一用户锁、同一事务完成所有校验及软删除。数据库分块限制单条 SQL 大小，不把一次确认拆成多次提交；任何版本、退款、贷款、现金或数据库失败整组回滚。大列表通过入口身份检查，但不能利用选择数组夹带身份或复杂嵌套。没有 schema 迁移。

文件范围：transactions/batch-delete.js、read-cache.js、API transaction-batch-delete.js/handler.js、相应测试/契约/需求/业务规则/入账逻辑/现行说明。既有账户详情 UI 不在本次范围。

验证：超过 100 笔和超过缓存容量的多页全选；读取失败/重试、取消、会话和版本变化；一次完整删除及响应丢失恢复；隔离 MySQL 大批量混选、组外退款、现金全组判断、后段版本冲突、写入中途故障回滚、用户隔离与并发幂等。最后刷新实际主目录开发者工具，只读真实账本，不删除真实账目。

2026-09-23 实现与证据：

- 已移除客户端勾选/全选和 API 的 100 笔上限；先复现 1505 笔/51 页因首屏缓存淘汰导致选择归零，再通过令牌最近使用顺序修复。前端逐页显示选择进度，选中标记按路径分段更新。
- 后端按 100 个 ID 控制单条 SQL 大小，所有分块仍在同一用户锁与幂等事务内；严格平面 ID/version 列表不再消耗通用 1000 节点嵌套预算，身份注入和复杂嵌套保护保留。
- 定向缓存/页面/在途恢复/API 单元测试 108 项通过，追加全选中离页/退出会话回归 1 项通过。一次性 MySQL 8.4 的批量管理及分页 11 项、大批量原子删除 6 项通过，合计 126 项有效验证，零跳过。1505 笔混选并发重放只产生一个回执和一次账本修订；后块版本冲突、组外退款、现金余额反例及第二块更新故障均整组回滚。测试夹具账户重名造成的初次失败已修正并重跑通过；测试数据库与容器已清理。
- 工程检查通过 820 个文件/138 个运行时模块，`git diff --check` 通过。用户实际 `/Users/gaohongxiang/projects/catledger` 微信开发者工具重新编译成功，明细加载无错误；原生运行时独立内存合成对象验证 205 笔全选、205 个勾选和取消成功，真实账本零写入。
- 开发云已更新：用户明确授权后复用原有 `cloud_fn_inc_deploy` 请求，确认成功更新 `catledger-api/src` 的 54 个文件（86.4 KB），未重新发起授权。更新后 `cloud_fn_info` 核对 Active、Nodejs18.15、20 秒超时；工具不返回 VPC/环境变量/触发器，因此不额外声称已重新核对这些字段。本次只增量更新代码。用户主目录开发者工具重新编译成功，真实微信会话的 `reads.validate` 只读校验通过，首页无错误。CLI 失效登录路径已停止。没有新增迁移、没有上传手机包、未提交推送或正式发布；真机操作仍待用户验收。
