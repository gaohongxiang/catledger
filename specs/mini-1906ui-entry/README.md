# 当前入口契约（2026-09-14）

中央“记账”恢复方式选择，提供“账单导入”和“记一笔”；选择后按需登录并继续原选择。取消按钮或遮罩关闭菜单，不读取目录、不写账。两项共用导航防重入，失败后可重试。记一笔页整行导入卡片和我的导入项源码保持原样；各入口复用同一工作台与原批次恢复规则。

本批在 `codex/mini-1906o2-catalog-loading`、基线 `058858e0` 实施，当前状态只查主看板19.10。下方旧直达、旧服务兼容、旧分支及部署限制均为历史记录；当前服务已于C1部署，旧兼容由MINI-1906S移除。本次不改数据协议、云函数或迁移。

- 本地定向回归76/76（cached-page-requests、home-visual-sample、ui-system、login-default-profile），0失败、0跳过；仓库检查625文件/121模块通过，git diff --check通过。
- 原生工具使用内存合成响应及catledger存储替身，不写真实账本。检查菜单显示、取消、登录后继续、两条路径及原导入卡片往返；菜单截图见[恢复的选择菜单](RESTORED-CHOOSER.jpg)，记一笔原入口见[保留的导入卡片](EDITOR-IMPORT-PRESERVED.jpg)。详细实际结果见主看板19.10；手机与真实云端业务验收仍属19.9待办。

---

> 2026-09-13 分支整合：本任务52127eb2全部代码已快进纳入 `codex/mini-1906o2-catalog-loading`；不再在旧入口分支开发。下文分支/worktree为历史证据，本机未提交工作另行核对，不能自动删除。后续见[性能收口计划](../mini-1906o-consolidated-optimization/PERFORMANCE-CLOSURE-PLAN.md)。

# MINI-1906UI-ENTRY：记账直达与昵称内嵌操作

状态：待验收（本地与真实旧版服务读取已验证，待集成及真机验收）。负责人：当前Codex App主会话。
基线：0a3c4263（O1–O4候选）；分支：codex/mini-1906ui-entry-direct；worktree：/private/tmp/catledger-mini-1906ui-entry。

## 范围与设计

1. 目的：减少随手记账的一次选择，批量导入维持首屏可见，切换后继续原草稿。
2. 风格：沿用暖橙手账；复用已有主题令牌、原生组件和图片，不新增装饰资源。
3. 颜色与字体：暖橙#BE5B24、强调#A34D20、浅橙#FCEEE3、纸色#FAF8F4、白#FFFFFF；其他主题跟随现有令牌。PingFang SC、Source Han Sans SC和既有字重，优先满足项目设计系统。
4. 布局：昵称与“换一个”同框左右排列；“记一笔”标题下方、类型切换上方提供整行浅橙导入入口，左侧标题与说明、右侧复用菜单箭头。“我的 → 账本管理”第一项增加同名入口。金额仍为手记主操作区域。编辑/查看已有账单和关联退款隐藏导入。

所有权：登录组件、底栏、记账页、我的页、API目录读取适配及对应测试/文档。导航保留现有登录门禁与会话隔离；不增加API、迁移或账务写入，不合并main、不部署。继承O1–O4已有性能及云端未部署限制。

## 验证

### 2026-09-13 真实服务兼容与失败提示修复

- 用户实际打开记一笔时出现“当前操作尚未开放”：原生真实云调用确认catalog.get返回UNSUPPORTED_ACTION，accounts.list/categories.list成功。上一轮合成响应验收遗漏了前后端版本衔接。
- 客户端优先使用新目录接口，仅明确不支持时自动兼容旧版读取，复用同会话身份缓存并投影账户元数据；显式重试强制刷新全部依赖。完整成功才缓存目录，权限/非法参数/旧会话不降级，部分失败不启用提交。旧版组合读取仍非一致快照，保存由服务端最终校验。
- 移除大面积错误块和“重新读取账户和分类”按钮；正常页面自动填好选项，失败时一行提示与“重试”，保留已填金额和备注。沿用暖橙手账主题、PingFang SC与既有色彩令牌，左侧提示、右侧重试。
- 根目录298/298通过，0失败、0跳过；新增7项行为回归覆盖兼容读取、身份/目录缓存、结构变更、部分失败、非法响应重试、权限和会话隔离。静态517文件/100运行时模块通过，git diff --check通过。
- 微信开发者工具最终页面编译运行，WXML/WXSS两项通过。真实服务自动加载后账户与分类均选中、目录就绪且无错误；注入一次合成失败验证轻量提示与28.50/备注保留，实际点击重试后恢复真实云端读取并保持草稿。未保存真实交易，截图中的账户与分类名称使用展示别名；临时替身、展示别名和合成草稿均已恢复。
- 真实旧版读取已验证；新版catalog.get云函数仍未部署，不能把本次兼容验收记作新接口发布验收。本轮CI以对应提交的“仓库验证”结果为准。

### 2026-09-13 导入入口增强

- 在已推送的d9cf46fc上，将标题右侧小按钮改为标题下方整行入口，并增加我的账本管理首项；两处均进入同一工作台。
- 全根目录291/291通过，0失败、0跳过；静态517文件/100运行时模块通过。新增我的入口回归覆盖登录许可、成功后继续、无额外读写、防重复导航与失败重试；原草稿保留、模式与会话门禁回归继续通过。
- 微信开发者工具重新编译运行：记账页与我的页WXML/WXSS共4项通过；实际截图确认浅橙入口位于标题与类型切换之间、整行可点、首屏仍显示确认记账，我的入口位于账本管理第一项。
- 实际点击验证我的入口：游客取消留在原页且0云调用；合成登录失败留在面板，重试成功直接进入导入第1步；已登录时导航失败可重试。底栏记账直达新增页，输入28.50与备注后通过整行入口进入导入，返回完整保留输入；返回前保存计数0，确认记账后计数1并返回我的。
- 上述原生验证使用内存合成云响应及本地存储替身，不写真实账本；未检出ReferenceError、TypeError、SyntaxError。重新编译后已确认临时替身清除。导入既有批次恢复逻辑未修改，相关回归通过，本轮实际点击覆盖无待恢复批次的进入路径。
- 本轮CI由任务分支提交触发，以对应提交的“仓库验证”结果及交付链接为准。

### 首版记账直达与昵称框

- 本机Node24：全根目录289/289通过，0失败、0跳过；静态517文件/100运行时模块通过，git diff --check通过。
- Node18/MySQL8.4 CI：c4adc5e3的run34739695231全绿，621/621（根289/API97/import235），0失败、0跳过；生产依赖审计与仓库静态门禁在同一作业通过。后续仅收紧按钮宽度并补全文档，最终CI以交付链接为准。
- 微信开发者工具2.02.2607312，skill0.3.8，iPhone 12/13 (Pro)模拟器；新窗口s19明确指向本任务worktree。登录组件、底栏、记账页WXML/WXSS共6项编译通过；原生运行时未检出ReferenceError、TypeError、SyntaxError。
- 使用真实WXML/WXSS/JavaScript及实际输入、点击、导航。云函数和catledger本地存储临时替换为内存合成响应；保存计数不代表真实账本写入。自定义底栏不在CLI页面选择器范围内，使用桌面窗口实际点击验证。

| 验证流程 | 结果 |
| --- | --- |
| 框内换昵称 | 名字变化，原本未聚焦时仍未聚焦，0次云调用 |
| 主动编辑昵称 | “使用微信昵称”可聚焦，长昵称可编辑，继续换昵称成功 |
| 游客点击记账、取消 | 原页打开登录面板；取消后原页且0次云调用 |
| 登录失败与重试 | 错误留在面板；合成响应恢复后直接进入记一笔 |
| 已登录点击记账 | 一次点击到新增记账页，表单与目录就绪 |
| 导入往返 | 进入第1步；返回保留28.50和备注、目录就绪，保存计数仍0 |
| 导航失败重试 | 草稿不变，按钮解除禁用，可再次进入导入 |
| 返回后保存 | 原确认记账按钮成功，保存计数1并返回首页 |
| 编辑/查看/关联退款 | 页面正常，均无导入按钮；其他模式及会话/保存门禁还有自动回归 |

首版页面检查发现导入按钮继承了全局固定宽度，当时用width:auto收紧。本轮改为整行入口并显式设置width:100%，原生截图确认布局正确。旧底栏直达导入契约测试已同步为新路径；没有扩张账单导入业务范围。

## 交付边界与回滚

本轮兼容补丁回退后，旧云函数上会重新出现目录不支持错误，无数据库回滚。

用户已授权提交并推送任务分支；未合并main、未上传小程序、未部署函数或执行迁移。真机键盘/触摸和真实云端新接口未验证；父O1–O4候选的catalog.get等尚未部署，上限批次性能限制继续见父规格，不属于本次导航修复的通过范围。

导航返回保留的是原页面内存草稿，进程退出或切换Tab销毁页面后的持久恢复不在本次承诺内。回退本任务相关提交可恢复旧选择弹层与昵称布局，无数据回滚；部署仍须满足父候选的服务端兼容顺序。
