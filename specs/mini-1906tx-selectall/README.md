# MINI-1906TX-SELECTALL

状态：待验收。基线main `5728c859`；当前主会话串行负责，独立分支/worktree。用户要求尽快移除明细导入记录入口并提供全选，沿用GitHub和手机开发版更新授权。

明细页移除“导入记录”按钮；批量选择提供“全选/取消全选”。全选沿当前筛选自动加载后续页，最多选择100笔有效手动/导入账目；读取失败保留已选并允许重试，不越过删除确认。切换页面或会话终止旧选择，选择期间禁止改筛选或删除。

设计沿用现有账本：1. 明细快速选择；2. 暖色编辑式风格，#faf8f4/#ffffff/#29231e/#746a60/#d97732；3. PingFang SC/Source Han Sans SC/Noto Sans CJK SC；4. 批量删除右对齐，全选在已选数量右侧。既有主题、字号和88rpx触控尺寸不变。

无后端/接口/数据库变更。58项相关客户端测试通过，包含跨页混选100笔、取消全选、加载失败重试、确认冻结和会话保护。原生验证及0.4.47上传结果待补录。
