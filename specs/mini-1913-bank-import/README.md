# MINI-1913 银行表格账单接入

状态：待验收。2026-09-23 银行表格导入已实现、验证并更新开发云；已根据用户明确提供的兴业 XLS 完成本机只读复现和格式修复，手机体验版 0.4.65（含历史查重补充）上传成功，待用户真机导入确认。用户明确要求补齐旧版已有的银行账单导入。主会话独立维护 `codex/mini1913-bank-import`，基线 main@62abdf57；工作目录 `/private/tmp/catledger-mini1912-bank-import`，交付已同步到用户 `/Users/gaohongxiang/projects/catledger`，保留主目录既有修复。

影响：导入文件选择/准备、表格读取、银行列映射与字段解释、整理页账户确认、解析接口可选参数及必要测试/说明。复用既有导入身份、原始证据、整理和原子入账流程，无新账本表；银行账户、流水含义和原始编码不由文件扩展名猜测。

契约：支持银行 CSV、XLS、XLSX。已有微信与支付宝模板优先；银行表头自动提出唯一列建议，每份银行文件先确认所选工作表、表头与金额方向后解析；歧义列保持未选。一次解析一个明确选定的工作表。支持收入/支出分列、金额+方向及用户明确正负方向的带符号金额。借贷方向和信用卡正负数不无条件套用同一口径；不明款项留在整理确认。时间、金额、币种、公式、文件/行列上限和原始来源定位必须校验。只确认文件映射不入账，全部通过既有确认后才一次入账。

隐私与幂等：原件只在既有受用户身份保护的临时云存储中读取，预览只返回给该用户；日志/提交/测试仅合成数据。稳定流水号依来源账号隔离，无稳定来源身份使用文件摘要+物理行；同额同日不会自动合并，不按银行名称认定为同一账户。不修改真实账本验证。

验证：合成二进制 XLS、XLSX、UTF-8/GB18030 CSV；唯一/歧义表头、正负金额、分列零值、日期、货币、重复表头/汇总行、公式和超限/损坏输入；原有微信支付宝回归；列映射输入校验、当前页失败重试及接口级已上传文件恢复；隔离 MySQL 混合文件、用户隔离、同文件重复/并发、独立同额交易与失败回滚。完成后同步主目录并在原开发者工具验证，更新导入云函数使用已有微信连接，不走失效的 tcb 登录路径。

依赖：XLS 使用 SheetJS CE 0.20.3 官方发行包，版本来源 https://docs.sheetjs.com/docs/getting-started/installation/nodejs/；固定 tarball URL 和 lockfile 校验，安装禁用生命周期脚本。现有有界 XLSX 解析器保留。

## 首轮开发验证与交付证据（2026-09-23）

- 导入及页面定向回归共 315 项：291 项通过、0 失败；24 项要求数据库的既有用例在本次非数据库命令中跳过。新增银行隔离 MySQL 8.4 集成用例另跑 8 项，全部通过、无跳过，覆盖用户隔离、重试并发、重复和身份冲突、混合文件原子入账、回滚与日志隐私。测试仅使用合成账单。
- 主目录工程检查通过（835 文件、141 个运行时模块），生产依赖审计 0 漏洞，差异空白检查通过。XLS 依赖与锁文件已安装到主目录。
- 用户原微信开发者工具已核对项目路径为 `/Users/gaohongxiang/projects/catledger`；选择文件允许 CSV、XLS、XLSX，银行确认表单真实渲染，并检查金额列加方向、按正负区分及必填方向显示。临时合成表单已移除，保留导入入口。
- 使用已有微信连接完整更新 `catledger-import`，任务 `confirmation_cloud_fn_deploy_348eaba7-dce7-41e9-98fd-cf2027a597f9` 返回 `success / execution_success`，6202 文件、10.3 MB。随后查询 `Active`、`Nodejs18.15`、60 秒超时；原生微信会话对不存在的合成导入 ID 只读查询返回预期 `NOT_FOUND`，验证函数及新依赖能加载、数据库读取路径可用。首次运行检查遇到 TLS 连接中断，重试成功；未重复部署。该查询工具未提供 VPC、环境变量和触发器细节，本轮未宣称验证这些配置。
- 首轮没有使用真实账单做写入验证，没有数据库迁移、提交推送或新手机包上传；手机反馈后的交付见下节。银行 PDF/图片不在此次范围，每个文件一次选择一个工作表，仅支持人民币。

## 手机反馈与兴业 XLS 格式修复（2026-09-23）

用户确认失败来自手机体验版，并明确给出原件路径。原件不复制到仓库，诊断不输出金额、完整账户、交易或原始说明。只读复现发现：中文表头同时有交易日/记账日，旧推断把两者当歧义，转而选下一行英文表头；YYYYMMDD HH:mm 日期未规范化；“共计 N 条记录”和独立说明被当成交易。初始 43 行均日期无效，修复后 41 条真实数据全部有效，与尾部笔数一致，保留原始日期及所有字段。正负金额仍由用户明确确认；无法确定的经济性质留给既有整理流程，不自动写账。

实现：日期列分交易/记账优先级，同优先级歧义保留待选；双语同位表头保留为装饰证据；紧凑日期加时分/秒受现有日期校验；独立笔数控制与完整数据行数核对，不符时整文件无批次；单格说明保留元数据，有交易字段的损坏行不跳过。bank adapter/parser/normalization 升至 v2，未改变身份、权限、数据表或正式入账事务。

交互：“需要处理”混合统计移除，待确认列/读取失败摘要可操作；已入账独立统计。首次读取收口后自动打开待确认表单，文件内提供完整操作文案。失败原因不裁切，入口打开原因与重试/调整列面板；内存预览丢失会读取原上传恢复，不能恢复时显示原因。临时验证使用合成文件，原生文件处理按钮实际点击成功、完整失败弹层可见；验证后清理页面状态。

验证：354 项定向回归中 330 通过、0 失败、24 项数据库用例在非数据库命令中跳过；隔离 MySQL 另跑 9 项全部通过、无跳过（新增双语信用卡成功解析及笔数不符不建批次）。首次测试容器就绪检测过早命中初始化实例，改为 TCP 就绪后重跑通过。工程检查 836 文件/141 模块通过，WXML/WXSS 原生编译通过。真实原件仅本机解析，没有入账或持久化任何真实账务数据。

交付：通过已有微信连接增量更新 `catledger-import/src`，首次任务 `confirmation_cloud_fn_inc_deploy_e5a2fbbe-4643-4f30-85a3-4bb82a58a2f0` 明确返回 `execution_failed / socket hang up` 后重试，任务 `confirmation_cloud_fn_inc_deploy_c31644c4-1025-495f-aab8-d13d13b71f54` 返回 `success / execution_success`（86 文件、178.7 KB）；随后核对 `Active / Nodejs18.15 / 60 秒`。该工具未提供 VPC、环境变量和触发器明细，本轮未复核这些配置。使用用户实际 catledger 目录上传手机体验版 `0.4.64`，返回 `success: true`，代码包 1,029,492 字节；原生合成验证页面已恢复为空闲状态。本轮未更新 API 云函数、迁移数据库、提交推送、提交审核或正式发布；手机上的重新导入和最终确认入账尚待用户验收。

诊断曾尝试查云存储根目录和通过目录接口获取身份，自动审批以可能超出单文件诊断范围拦截；未执行这两项查询。用户提供精确本地原件后，改为仅从该文件只读定位，阻塞已解除。

## 交易类型文案与历史疑似重复核对（2026-09-24，待验收）

用户要求解释并修正“补全共同字段”，且每次入账必须和已入账账目比对。范围由当前负责人串行维护：导入整理、历史查重、问题成员分页、手机核对页面及合成回归；保留其他任务。继续使用本任务分支，交付同步实际 catledger 目录。

契约：已有文件摘要及稳定来源身份去重继续生效；新增同一用户、同一资金账户、相同金额和相容资金方向、前后 48 小时的正式有效交易作为“疑似已入账”候选，覆盖手工账和其他来源导入。相似仅提示，不自动合并或删除。核对时选择具体历史交易复用，或明确全部为独立交易；候选和决定绑定事件核心信息、历史交易 ID 与版本。交易被修改/删除或新增候选后重新核对。每次正式入账在用户锁内重查，未核对或发生竞态则整批拒绝写入并返回核对。历史候选有界读取并分页展示，超过总匹配上限时明确阻断，不静默忽略。无新表、无运行时建表；仍使用既有问题/成员/决定/历史关联实体。

页面将内部 shared_fields 改成“确认交易类型”，说明消费、收入、退款、还款、转账等具体选择。已有待整理批次恢复和进入整理时可触发历史复查；失败保留草稿。用户真实账本不用于写入验证，采用隔离合成场景验证跨来源、同额独立交易、同用户竞争、跨用户、旧决定失效、回滚和重复提交。

验证与交付（2026-09-24）：

- 主目录定向页面/草稿/分页/契约回归 79 项通过，随后新增“候选读取失败不能确认独立、可重试”的用例，历史 UI 专项 5/5 通过。原有导入 MySQL 集成 23 项通过；新增历史专项最终 8/8（含父测试）在本任务工作区和合并后的主目录分别通过，覆盖微信→银行、手工账、同额独立、跨用户、并发两批、重复请求、候选变更/删除、未知类型恢复及分页。测试只用合成账单，未操作真实账本。
- 数据库首轮全套执行因测试库未预先迁移出现缺表，完善一次性容器建库/迁移后重跑通过。工程检查发现延迟 require 仍形成模块环，已把旧历史关联恢复留在 review 服务，查询/候选模块保持单向依赖；再次通过检查及数据库专项。最终主目录 861 文件/148 运行时模块检查通过，任务工作区 820 文件/141 模块通过，差异空白检查通过。
- 主目录 WXML 编译成功；独立发布包工程检查通过，微信原生合成页面验证历史候选、选中状态、对应记录和两个决定按钮。未登录的临时窗口使用合成青绿主题数据作布局检查，候选点击后读取选择 ID 验证成功；该窗口无错误日志，随后已关闭并清理。主目录另一任务同时操作贷款模拟器，本次未覆盖其业务状态。
- 本次修改已按文件和文档章节合入实际 catledger 目录，保留 MINI-1914 分期修改。由于该分期任务尚未部署迁移/API，手机发布包使用其同步前基线叠加本任务，未把未部署的分期依赖带入手机。云端仅发布本任务 catledger-import/src，不更新 API 或数据库。
- 云函数增量任务 confirmation_cloud_fn_inc_deploy_49d3ff47-44b7-443d-b138-c4dfcbca8c8d 返回 success / execution_success（87 文件、182.8 KB）。已有授权对应的一次性工具确认完成后，复核 Active / Nodejs18.15 / 60 秒；该查询仍不提供 VPC、环境变量键或触发器细节，未宣称复核。手机体验版 0.4.65 上传 success: true，代码包 1,035,916 字节；未提交审核或正式发布。
- 临时发布副本、合成截图和本次数据库运行脚本已清理，保留有未提交实现的工作区。没有提交、推送或真实财务写入；手机继续原批次的核对与最终入账仍待用户验收，因此状态为待验收。



## 原始字段同层展示回退修复（2026-09-24，待验收）

回退定位到 2026-09-13 `67c13604`：分页收口把原文共享加载器换成只设标记的占位，新的历史查重弹层沿用了仅显示链接的模板。用户此前要求直接展示仍然有效，本轮直接在实际 catledger 主目录修复，保留 MINI-1910/1911/1912/1914 的未提交工作。

打开交易核对或账户记录面板即自动展示当前页原始列和值，付款核对默认展开原文；每页 8 笔、最多并发 2 笔，每笔一份来源，原位翻页切换。每份自动读取最多两个 2048 字符片段，超长只展示完整字段并明确保留完整原文入口。重复列按列索引区分，空值显示“（空）”，零值原样保留；读取失败原位单笔重试。关闭、成员翻页、会话/版本变化和离开页面后丢弃迟到结果。长历史候选标题省略在左侧，金额和选择控件保留右侧宽度。没有修改查重判断、决定保存或正式账务写入。

验证与交付：

- 定向 67 项测试全部通过、0 失败/0 跳过，包含新增原文回归 10 项，以及分页、历史核对、草稿恢复、视觉和契约回归。新增测试中缓存命中曾令迟到测试等待不到网络回调，已改成独立会话构造版本变化场景后完整通过；没有通过生产代码绕开验证。
- 工程检查通过（866 文件、148 运行时模块），差异空白检查通过，最终 WXML 原生编译成功。按 CloudBase 审查规则确认没有新增身份路径、后端接口或敏感原文日志。
- 用户实际开发者工具项目 `/Users/gaohongxiang/projects/catledger` 内完成合成核对：原文 7 行直接渲染，同层滚动可见；两列同名备注、空值及零值均保留；超长候选标题与金额不重叠；点击合成候选只改变选择。未点击保存或入账，未修改真实账本。
- 使用主目录上传体验版 **0.4.69**，返回 `success: true`，包大小 **1,067,515B**。保留已发布 0.4.68 的分期能力，不需要更新云函数或迁移数据库。本轮无提交、推送、提审或正式发布；手机实际使用待验收。
- 入账逻辑说明 0.3、0.6、6.4.5～6.4.7、PERF-5 与复盘段已同步，需求、业务规则和现行界面说明一并更新；旧 20 笔追加说明和不再适用的原文日期转换说明已修正。
