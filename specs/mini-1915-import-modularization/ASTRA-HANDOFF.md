# MINI-1915：Astra 实施交接

2026-09-24。用户要求先优化计划并提交 GitHub，再交给 Astra 实施。当前只完成计划修订；**业务实现未开始**。本文件作为用户转交后的实施指令，不把文档存在本身当作已开始开发或发布授权。

## 任务入口

仓库：`gaohongxiang/catledger`。唯一任务分支：`codex/mini-1915-import-modularization`。初稿/修订起点：`e87416bbbe0a5189486e4664b364f9e48db2b396`；业务基线：`87b31d04f3791e36b387d073547a9c10593a178b`。先 fetch 并核对远端任务分支实际 HEAD，不能把初稿 SHA 当成优化后计划的 SHA。

先读 [AGENTS.md](../../AGENTS.md)、[实施规划](../../docs/招财猫记账本实施规划.md) 和本目录 [README](README.md)、[design](design.md)、[requirements](requirements.md)、[tasks](tasks.md)，再开始 P0。本目录修订已落实评审，不再从头另写大架构方案。

## 直接执行的指令

你是 Astra，本任务唯一主实施负责人。请在 `codex/mini-1915-import-modularization` 上实施 MINI-1915 的等价模块化拆分。按 P0 → P1-A → P1-B → P2 → P3 → P4 → P5 逐段验证、提交并推送；已授权范围内不逐文件询问，也不只停留在重新制定计划。

先核对用户真实工作区 `/Users/gaohongxiang/projects/catledger` 的修改、分支/worktree、远端差异，以及微信开发者工具实际打开的工程。已有修改必须保留，不能为获取本计划强切、reset --hard、force push 或覆盖并行成果。必要时用同一任务分支的隔离 worktree，不另开阶段长期分支，也不默认替换用户正在运行的主线工程。

P0 在实施规划中认领 MINI-1915，补充本修订版与负责人；原“供 6 sol 评审”的记录是初稿编写过程，不能据此回退到旧计划。记录真实调用/状态所有权、测试覆盖和性能基线，区分原有缺陷与新回归。保留 MINI-1910～1914 已有实现及原待验收状态。

**首个业务代码提交只提取纯规则。** 迁移 FIELD_MASK、applyFields、resolvedReasons、validateDecision、assertDecisionMatchesIssue 等确需函数，并在同一提交更新生产消费者和测试。不能修改 SQL、事务、页面、版本、action 或账务语义；不新增内部兼容转导出、fallback、能力探测或双实现。

后续严格遵循以下边界：

1. `saveEvent` / `saveEvents` 包含引用校验、可入账重算、原因、版本与操作来源；原样保留差异、preserveReferences 与 semantic 路径，不把它们改成普通 CRUD 或相互循环调用。
2. 保持原 executeIdempotentMutation、可信身份、同一连接、各入口原锁序/版本检查/写入顺序和幂等回执。历史恢复、候选同步、草稿可达性与计数收尾不能漏掉或重复；批量操作不退化为逐笔事务。
3. 五个工厂方法逐个落实，包括 refreshAccountGroups；只有四个对应公开入口。reviseAccountMapping 是内部 service 方法，没有同名公开 action，不得为凑齐入口新增网络接口。分组刷新保留无候选/无业务变更路径。
4. 页面 runtime 只协调生命周期。读版本/分页仍归 import-view-session；草稿、在途请求、requestId 和 postFlight 仍归 import-draft-session。旧读响应停止回填，但已发写命令按原协议确认或用原请求恢复；不得丢成功事实、跨用户回填或生成第二笔入账。
5. 按流程移出页面方法就删对应旧覆盖，最终移除 paged.enhance；保留 boundedSetData、view-patch、原布局、导航和同层原始字段展示。
6. P5 默认只审计来源规则，不顺带统一银行身份语义或不同的资金方向回退。不改正式入账 SQL，不换数据库/框架/插件体系，不为减少行数制造新抽象。
7. 对比合成业务关系图与实际 SQL/云请求/字节/派生/setData 开销。随机 ID 使用一致一一映射，不直接删关联字段；既满足硬预算，也不能无解释退步。复用已有行为测试，只补缺失接缝。

P1-B/P2 必须有受影响的隔离 MySQL 回归；P3/P4 验证真实 Page 入口、生命周期/回执恢复以及原生开发者工具合成交互；P5 运行完整门禁和精确 SHA CI。数据库仅用明确隔离的测试实例，严禁拿真实云库跑迁移/回归。工具不可用、测试失败或跳过必须如实记录；不能把历史 CI 或只测假对象说成当前实现已通过。

发现基线缺陷时记录复现与影响，单列修复范围；不要借结构提交暗改业务。只阻塞受影响阶段，其余可独立验证的工作继续。每个阶段留下可验证的小提交与回退依赖，不用巨型提交一次迁完全部。

## 授权与交付边界

用户转交此指令后，授权本任务分支内的实现、合成/隔离验证、必要文档更新、提交和推送。**未另行授权不得合并 main、部署云函数、迁移云数据库、新建云资源、上传小程序、提交审核、正式发布或修改真实账目。** 不重复之前任务的部署，也不因代码通过就清除原功能待真机验收状态。

交付报告给出：任务分支及精确 SHA、完成阶段、实际修改边界、通过/失败/跳过和精确 CI、业务图/性能比较、可回退提交及依赖、原生/真机/发布状态。同步入账逻辑说明、架构、受影响现行说明和实施规划。未获合并授权时交付可集成候选，不声称已同步用户主线目录或手机已更新。

计划文档的当前修订不是 P0～P5 的实施证据；勾选任务必须对应实际完成记录。
