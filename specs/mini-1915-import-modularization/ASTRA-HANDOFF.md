# MINI-1915：一期优化交给 Astra

本启动指令编写于 2026-09-24，当时仅完成一期计划整合。下文保留用户转交后的范围和授权；实际实施、验证与发布状态只看 [实施规划](../../docs/招财猫记账本实施规划.md)，本文件不另维护状态。

## 读取与基线

仓库 `gaohongxiang/catledger`；继续使用唯一任务分支 `codex/mini-1915-import-modularization`。初稿 `e87416bb`、模块化修订 `b4868527` 均是历史节点；先安全 fetch，读取包含本次“一期减复杂度”整合的任务分支最新提交，不回退或覆盖后续成果。

先读 [AGENTS.md](../../AGENTS.md)、[实施规划](../../docs/招财猫记账本实施规划.md)，再读本目录 [README](README.md)、[design](design.md)、[requirements](requirements.md)、[tasks](tasks.md)。本文件不重复完整契约，不重新起草另一套大方案。

## 直接执行

你是 Astra，MINI-1915 的唯一主实施负责人。请把以下四项作为同一期交付：导入模块化、文档入口/状态收敛、本期相关测试整理，以及缓存测量和有证据的局部简化。按 P0 → P1-A → P1-B → P2 → P3 → P4 → P5 实施、验证、更新必要文档、分小提交推送；一期不是一个巨型提交，不逐文件重复询问。

开始前核对 `/Users/gaohongxiang/projects/catledger` 的修改、分支/worktree、远端差异及开发者工具工程路径，保留并行成果和 MINI-1910～1914 原待验收状态。必要时同一任务分支隔离 worktree，不强切主目录，不 reset --hard/force push/危险 rebase。P0 先在实施规划认领一期范围、负责人、基线及文件所有权，区分历史初稿与本次范围。

首个业务代码提交仍只能提取纯规则并同步消费者/测试，不改 SQL、事务、页面、版本、公开 action 或账务语义。后续保存/历史同步/批次收尾、五工厂方法与四对应公开入口、页面读写恢复严格执行 design 与 R1～R6；不要为旧内部路径增加兼容转导出/双实现，也不把隐式覆盖变成散落多文件的共享状态。

文档与相关测试按 R7 做减法：当前状态集中到实施规划，入口只导航，历史证据保留日期/SHA；删除断言前提供替代验证，不能删掉财务安全、恢复或真实 UI 约束。不要新增第二个看板、交接体系或全库测试框架。

缓存按 design 4.3 和 R8：先测冷/热、并发、前台、写后/失败写、用户切换的请求/失效/渲染；tag 和 dataRevision 不是可直接互替的重复。只有局部等价和收益有证据才独立提交简化；没有证据就保留原实现并记录结论，不强行重做一致性协议或绕过历史复查。不新增缓存层、SQLite/Redis，不改 revision 协议、schema、TTL/前台策略。

本期不做复式分录/不可变账本、影子双写、历史回填、普通 API/贷款体系重写、微服务/新框架、插件/AI 或全站 UI 重设计。长期规划保留为本期范围外，不自动取消其他任务或启动下一期。

复用既有工程/行为测试；P1-B/P2 跑受影响隔离 MySQL，P3/P4 跑真实 Page 和原生合成交互，缓存条件项独立回归，P5 跑最终完整门禁及精确 SHA CI。基线 bug 单列，影响阶段安全则阻塞该段；其他可独立验证的继续。缺工具、失败和跳过如实记录，不用旧 CI 充当新验证，不用删测试制造通过。

## 授权与交付

用户转交后按其授权在本任务分支实施、合成/隔离验证、更新文档、提交和推送。未另行授权不合并 main、不部署云函数、不迁移云数据库、不新增云资源、不上传/审核/发布小程序或修改真实账目。数据库测试只能使用明确隔离实例；不能把任务分支同步说成用户主目录或手机已更新。

交付精确分支/SHA、完成步骤、指定依赖/覆盖/状态负担的减少证据、实际测试与 CI、业务图/资源对比、测试替代映射、缓存已改/保留/后续判定、回退依赖及原生/真机/发布边界。任务勾选必须对应实际证据；缓存测量后保留原实现可以完成该条件项，但不能宣称已有缓存性能提升。
