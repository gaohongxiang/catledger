# MINI-1915 一期内部任务

实施状态与阶段证据见 [实施规划](../../docs/招财猫记账本实施规划.md)；下列勾选只记录相应验收项，不另维护状态看板。边界见 [README](README.md)、[design](design.md) 和 [requirements](requirements.md)。P0～P5 同属一期，沿 `codex/mini-1915-import-modularization` 分小提交验收，不拆成多套新计划，不一次性提交全部改动。

Astra 在用户转交 [交接指令](ASTRA-HANDOFF.md) 后任唯一主负责人，按顺序实施、验证、更新文档、提交和推送。默认串行；不逐文件重复审批，不将授权扩大为合并 main、部署、迁移、上传或发布。

## P0：基线、范围与文档入口收敛

所有权：现有任务/入口文档、必要补缺测试与测量；不改业务实现。先记录基线，再在单独文档提交中收敛状态入口。

- [x] 0.1 核对真实目录、git status/worktree、main/任务分支 SHA、未提交修改和工具窗口。实施规划登记 MINI-1915 一期、Astra、基线、文件所有权与状态；保留其他任务历史及待验收状态。（R6、R7）
- [x] 0.2 枚举总服务全部生产/测试消费者，补 design 的函数副作用表；核对五工厂方法/四对应公开入口和内部 reviseAccountMapping 消费者。（R2、R3、R5）
- [x] 0.3 枚举 index/paged 覆盖、生命周期顺序、WXML、分页器、订阅、定时器和共享字段 owner。页面/读会话/持久写请求各有明确权威。（R4、R5）
- [x] 0.4 将已有测试映射到字段、保存、账户分组、历史、旧计划、入账和恢复。拟移除断言列保护目标与替代入口；先补缺失行为测试并在原业务基线上验证。（R1～R4、R7）
- [x] 0.5 固定合成输入、业务图比较与 ID 一致映射、实际 SQL/云请求/字节/派生/setData 及硬预算；记录代表性操作的入口/处理者/状态 owner，作为减复杂度对照。（R5、R6）
- [x] 0.6 测量 design 4.3 的冷/热、同 key 并发、前台、写后/失败写、换用户场景；定位既有缓存测试、读策略和 observer。只列有证据的候选，记录“拟简化/保留/超出本期”，不先删除机制。（R8）
- [x] 0.7 精简现有根 README/docs 索引中的重复当前任务、部署版本和测试数字，改指向实施规划；历史事实保留日期/SHA。specs/交接只维护各自职责，不新建第二个看板，不机械扩大到全库文档重排。（R7）
- [x] 0.8 记录基线失败和环境缺口。未知失败未解释不得开始受影响重构；基线缺陷单列，不以删测试、清草稿、改语义制造通过。不受影响的工作可继续。（R1～R8）

出口：职责/副作用/所有权/覆盖和资源基线可追溯，文档当前入口已收敛，缓存已测量且候选有边界。P0 文档修改不算业务代码开始；所有实际状态只按证据更新。

## P1-A：最小首个业务代码提交——纯规则

依赖 P0。所有权：review-issue-service.js、新增 review/policy.js、实际规则消费者的 require、相关测试及必要入口文档。

- [x] 1A.1 原样提取 FIELD_MASK、applyFields、resolvedReasons、validateDecision、assertDecisionMatchesIssue 等确定性规则，检查直接/传递依赖不引入 SQL、存储或服务工厂。（R1、R5）
- [x] 1A.2 同提交迁移生产/测试引用，保留正式工厂，不加旧内部转导出、兼容包装、fallback 或双实现。（R3、R5）
- [x] 1A.3 验证掩码、决定/类型、错误码、对象行为、原优先级和维护/旧计划调用；工程/依赖检查通过。（R1、R3、R6）

出口：不改 SQL、事务、锁、页面、数据/计划版本、公开 action 或账务语义。带副作用 helper 留到 P1-B，不为凑范围抽象；本提交不混入缓存修改或测试删减。

## P1-B：事件/问题存取与事务内收尾

依赖 P1-A。所有权：总服务、review/event-store.js、issue-store.js、reconciliation.js，core/read/maintenance/semantic-plan-upgrade 的必要引用和回归。

- [x] 1B.1 移动读取/旧证据补齐、引用目录、关系上下文和保存；保留单笔/批量差异、版本条件、原 SQL 与批量方式。（R1、R2）
- [x] 1B.2 保留 finalizeSavedEvent 对状态/原因/版本/操作来源和 next 的修改，旧升级 preserveReferences/semantic 路径不变。（R1、R3）
- [x] 1B.3 提取问题/成员存取、后续问题占用/分类/退款上下文及版本同步，不重写判断规则。（R1、R2）
- [x] 1B.4 提取有效投影/投影刷新、完整历史恢复与候选同步、草稿可达性和批次收尾；按各入口保留顺序/次数，不新增统一自动收尾。（R1～R3）
- [x] 1B.5 同提交更新消费者；四类调用方不再为公共能力依赖总入口，读路径不调用写收尾，无新增兼容导出或循环。（R3、R5）
- [x] 1B.6 受影响单元/隔离数据库验证引用拒绝、升级/历史恢复、并发冲突/回滚/隔离和单笔/批量保存；对比业务图与查询数。（R1～R3、R6）

出口：依赖方向和副作用有证据，原连接/事务/持久格式不变。可继续按函数接缝分小提交，不以一个大搬家提交完成全部。

## P2：核对场景

依赖 P1-B。所有权：原工厂、account-mapping.js、event-decisions.js、repayment.js 及直接相关回归。

- [x] 2.1 迁移单项/批量账户确认、修订和普通 resolve 中的账户步骤，保留锁序、成员状态、映射索引、草稿边界与一次批次收尾；不互调公开命令。（R1、R2、R5）
- [x] 2.2 迁移 refreshAccountGroups 的旧引用展开、问题替代/重开、分组/版本/映射/后续问题；验证无候选不新增业务行动/升版、再次执行不重复展开、回执不变。（R1～R3）
- [x] 2.3 迁移字段、同笔/不同笔、排除、舍弃证据、退款和历史关联分支；保留顺序与拒绝条件，不同时统一相似分支。（R1、R2、R5）
- [x] 2.4 最后迁移 setRepayment，复用既有规则并验证历史、贷款与分期费用交界。（R1、R2）
- [x] 2.5 核对五方法/四公开入口和工厂契约；仍由原幂等入口持有同一事务，子模块不提交，不误新增接口。（R2、R3）
- [x] 2.6 每场景分别验证并发、重放、过期版本、历史变化、失败回滚、用户隔离和资源；同步逻辑入口。（R1～R3、R6）

出口：方法/场景/规则/存取可直接定位，指定反向依赖已解除，正式入账未重排。每场景单独验收，不强制凑文件数量。

## P3：生命周期、请求恢复与上传

依赖 P2。所有权：index.js、paged.js、runtime.js、upload-flow.js；共享 services 默认只调用。

- [x] 3.1 显式装配 onLoad/onShow/onHide/onUnload、主题/登录、订阅与资源清理；保留 this/回调/原顺序，各页面实例隔离。（R4、R5）
- [x] 3.2 区分读回填和写回执：runtime 管页面活动，view-session 管读版本/分页，draft-session 管草稿/原 requestId/postFlight；验证离开后的原命令确认/恢复。（R3、R4）
- [x] 3.3 迁移上传、重试、进度/节流和银行映射接续，保留存储/上传协议及单文件隔离；boundedSetData 只包装一次。（R3、R4、R6）
- [x] 3.4 验证后台/恢复、卸载/重开、换用户、版本变化、迟到响应、写成功后读取失败；只补缺失接缝，不重建服务框架。（R4、R6）

出口：生命周期唯一协调，状态权威未复制，旧页面不覆盖新页面也不丢可恢复写入；不借本步骤修改通用缓存策略。

## P4：工作台流程与相关测试整理

依赖 P3。所有权：账户/交易核对、入账展示、必要 model/presentation 与相关测试。

- [x] 4.1 迁移账户核对/目录/UI 草稿，验证批量范围、账户 ID、修订和跨页选择。（R1、R4）
- [x] 4.2 迁移核对项、历史候选、表单与同层原文，验证输入保护、翻页/关层/切项迟到响应、原始空值/零/重复列和单项重试。（R1、R4）
- [x] 4.3 迁移汇总、入账展示和明细刷新，仍用原 draft-session，不复制 postFlight；成功读失败不能再生成入账。（R2～R4）
- [x] 4.4 随消费者移纯模型，每组迁移即删旧覆盖，最终 enhance 为零；重复生命周期/事件和缺失 WXML 处理者在检查中报错，不用对象合并顺序消解。（R5）
- [x] 4.5 验证真实 Page 入口、回调、实例隔离、重复订阅、过期 finally 与 24990 条分页/缓存预算。（R4、R6）
- [x] 4.6 按 P0 映射，在独立测试提交中合并/替换确实重复的内部写法断言；先证明替代覆盖，保留布局/可访问性/忙碌门禁和关键财务/恢复正反用例，不做全库测试重写。（R7）
- [ ] 4.7 在核对过路径的微信开发者工具做原生编译与合成交互，保留/恢复用户原会话；不用真实账务写入。工具不可用则明确待验收。（R6）

出口：实际处理者/状态 owner 可查，无旧覆盖，交互/预算不变，相关测试保护目标不减少。未出现可合并断言可保留测试，不以测试数下降验收。

## P5：缓存条件项、最终集成与交付

依赖 P4。所有权：P0 认领的客户端读取链路局部改动、边界检查、相关测试与既有文档；不修改服务端读取协议。

- [x] 5.1 复核缓存候选。只有具备触发/冗余证明、原与新计数、保留副作用/回调、一致性和恢复回归的候选进入代码；否则记录保留或后续原因，不为凑成果改代码。（R8）
- [x] 5.2 通过准入的缓存减法独立小提交/定向回归；不改 tag/revision 职责、写前/写后保护、前台/TTL、新鲜度、schema、force 或 knownRevision/unchanged 语义，不绕过历史复查。没有准入项则明确“不适用：测量完成，未改缓存”，不虚报性能改善。（R4、R6、R8）
- [ ] 5.3 审计银行身份和资金方向分支，只记差异/后续；来源代码提取非必做，语义/字段/版本变化退出本期。确认复式分录/影子双写/新缓存层等没有被隐式带入。（R1、R3、R5）
- [ ] 5.4 检查依赖、读写调用、页面装配、两函数独立部署和共享规则一致性；对照代表性路径证明指定复杂度已减少，无新转发空层、双实现或占位模块。（R3、R5）
- [ ] 5.5 最终候选工程检查、完整单元/隔离 MySQL、业务图与资源对比、精确 SHA CI；原生/真机缺口单列。相同未受影响通过项不机械重复，最终集成门禁不能省。（R1～R8）
- [ ] 5.6 同步入账逻辑说明/架构/现行说明的实际入口，实施规划记录状态与证据，入口文档不重复复制当前版本。记录必做结果、条件项保留结论和后续项，不新建另一套看板。（R6、R7）
- [ ] 5.7 分批推送原任务分支，核对远端 SHA、差异和本机路径，交付可集成候选及逆依赖 revert 入口；安全清理仅本任务临时资源，未提交/未集成 worktree 不删。未授权不改 main/用户正在使用的主线目录、不部署、不发布。（R6）

一期出口：导入职责/状态明确，公共反向依赖与 enhance 消除，文档唯一当前入口、测试保护等价、缓存测量/判定完成、业务和资源无无解释退步。条件项无代码变化不阻碍完成；必须的行为验证缺失则仍待验收，不能把推送说成手机已更新。

## 每个提交的最小记录

使用现有任务记录/提交说明，列阶段与 SHA、原职责/消费者、实际验证和通过/失败/跳过、业务/资源差异、回退依赖及剩余风险。测试整理列旧保护目标与替代验证；缓存列准入证据或保留原因。全部使用合成/隔离数据，不记录真实账单、绑定值或凭据。
