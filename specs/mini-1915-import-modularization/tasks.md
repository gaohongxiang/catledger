# MINI-1915 分阶段任务

方案已按评审修订；实施状态：**未开始**。本次只是文档修订，以下实施项均未执行。入口见 [README](README.md)，约束见 [requirements](requirements.md)，函数与状态边界见 [design](design.md)。

用户把 [交接指令](ASTRA-HANDOFF.md) 交给 Astra 后，由 Astra 单一主负责人沿 `codex/mini-1915-import-modularization` 实施、验证和分批推送。默认串行，不逐文件重复审批；不把该授权扩大为合并 main、部署、迁移、上传或发布。

## P0：冻结可验证基线

所有权：计划/证据、相关补缺测试；不改业务实现。

- [ ] 0.1 核对真实目录、git status/worktree、远端 main 与任务分支 SHA、未提交修改及工具窗口；在实施规划中认领 MINI-1915，记录本修订版、负责人、分支、基线和文件范围。（R6）
- [ ] 0.2 完整枚举核对总服务的生产/测试消费者。补齐设计中的函数副作用表；区分五个工厂方法与四个已注册公开入口，查明内部 reviseAccountMapping 消费者。（R2、R3、R5）
- [ ] 0.3 列出 index/paged 的实际覆盖、调用顺序、WXML 绑定、分页器、订阅、定时器和共享字段 owner。分别标出页面、读会话、持久写请求的权威。（R4、R5）
- [ ] 0.4 将已有测试映射到字段规则、单笔/批量保存、账户分组、历史核对、旧计划、正式入账及恢复；补缺失的行为用例，先在未改业务基线上运行。（R1～R4）
- [ ] 0.5 固定合成输入、输出/关系比较方法、实际 SQL/云请求/字节/派生/setData 指标及既有硬预算。列明随机 ID 的一致映射，不能删除关系字段。（R6）
- [ ] 0.6 记录基线失败与所需环境。影响当前阶段的缺陷先隔离或以独立修复任务处理；未知失败未解释不得开始该段重构。不得通过清草稿、改语义或放宽断言制造绿色基线。（R1～R6）

出口：每个拟移动职责有原位置、消费者、输入输出/副作用、owner 与验证；基线差异可解释。P0 的证据随提交记录，不需要另造文档体系或压测框架。

## P1-A：最小首个业务代码提交，只提取纯规则

依赖 P0。所有权：review-issue-service.js、新增 review/policy.js、maintenance/semantic-plan-upgrade 等实际纯规则消费者的 require、对应测试及必要入口文档。

- [ ] 1A.1 原样提取 FIELD_MASK、applyFields、resolvedReasons、validateDecision、assertDecisionMatchesIssue 等本次确需的确定性规则；检查其直接/传递依赖不引入 SQL、存储或服务工厂。（R1、R5）
- [ ] 1A.2 同一提交更新生产和测试引用，保留正式 createReviewIssueService 工厂，不为旧内部路径增加转导出、包装、fallback 或双实现。（R3、R5）
- [ ] 1A.3 验证字段掩码、决定/问题类型、错误码、对象修改行为、原校验优先级及维护/旧计划的受影响行为；运行工程/依赖检查。（R1、R3、R6）

出口：首个业务代码提交可单独验收和撤回。**不改 SQL、事务、锁、页面、数据/计划版本、公开 action 或账务语义。** 若某候选 helper 带副作用，就留在原处到 P1-B，不为凑首批范围抽象化。

## P1-B：事件/问题存取与事务内收尾

依赖 P1-A。所有权：核对总服务、review/event-store.js、issue-store.js、reconciliation.js，以及 core/read/maintenance/semantic-plan-upgrade 的必要引用和回归。

- [ ] 1B.1 移动事件读取、旧支付证据补齐、引用目录、关系上下文及保存；保留 saveEvent/saveEvents 差异、版本条件和批量方式。（R1、R2）
- [ ] 1B.2 明确 finalizeSavedEvent 的状态/原因/版本/操作来源副作用；保持旧升级 preserveReferences 与 semantic 来源路径。（R1、R3）
- [ ] 1B.3 提取问题/成员存取、后续问题分类/占用/退款上下文及版本同步，保持现行 SQL 与分类规则。（R1、R2）
- [ ] 1B.4 提取有效投影与投影刷新，完整保留失效历史关联恢复、候选同步、草稿可达性和批次计数更新；写清各入口顺序与次数，不新增统一自动收尾。（R1～R3）
- [ ] 1B.5 同提交迁移实际调用方；core/read/maintenance/upgrade 不再为公共能力依赖核对总入口，读路径不调用写收尾；不新增兼容导出或循环。（R3、R5）
- [ ] 1B.6 跑受影响单元和隔离数据库用例：引用拒绝、语义升级、历史恢复、并发冲突/回滚/隔离、单笔和批量保存；对比实际查询次数与业务图。（R1～R3、R6）

出口：公共依赖方向正确，事件保存与收尾的全部副作用有行为证据；原 SQL、连接、事务及持久格式未改变。禁止用一次“大搬家”提交包含全部能力，可按上述接缝继续细分。

## P2：按场景提取，不重写事务

依赖 P1-B。所有权：原工厂、review/account-mapping.js、event-decisions.js、repayment.js 和直接相关回归。

- [ ] 2.1 迁移账户确认、批量 resolve/revise 与内部单项修订步骤，记录普通 resolve 中账户相关分支归属。保留原锁序、成员状态、映射索引、草稿边界和批次一次收尾；不互调外部命令。（R1、R2、R5）
- [ ] 2.2 明确迁移 refreshAccountGroups：旧引用展开、旧问题替代/重开、分组/成员/事件版本、映射及后续问题；验证无候选不新增业务行动/升版，再次执行不重复展开，原回执协议不变。（R1～R3）
- [ ] 2.3 迁移普通字段、同笔/不同笔、排除、舍弃证据、退款及历史关联分支，保留决定顺序与拒绝条件；不同时统一相似分支或改变历史窗口。（R1、R2、R5）
- [ ] 2.4 最后迁移明确还款 setRepayment，复用既有规则并验证历史核对、贷款与分期费用交界。（R1、R2）
- [ ] 2.5 对照五方法/四公开入口清单，验证对外集合和原工厂契约不变；各命令仍由原幂等入口持有同一事务，子模块无独立提交。（R2、R3）
- [ ] 2.6 每个场景分别运行受影响并发、重放、过期版本、历史变化、中途失败/回滚、用户隔离及资源回归；同步逻辑说明入口。（R1～R3、R6）

出口：入口、场景、存取和规则可直接定位；refreshAccountGroups 和内部修订没有遗漏，没有误新增公开接口；正式入账实现未重排。各场景独立提交，不作为一个大提交验收。

## P3：明确页面生命周期、请求适配与恢复

依赖 P2。所有权：import-workbench/index.js、paged.js、新增 runtime.js、upload-flow.js；既有共享 services 原则上只调用。

- [ ] 3.1 按 P0 所有权表显式装配 onLoad/onShow/onHide/onUnload、主题/登录、订阅/定时器/分页器释放；保留 this、回调和原调用顺序。（R4、R5）
- [ ] 3.2 区分读回填和写回执：runtime 管页面活动；view-session 管读版本/缓存；draft-session 管草稿、幂等请求和 postFlight。验证离开页面后已发命令仍按原协议确认或恢复，而不是换 requestId。（R3、R4）
- [ ] 3.3 迁移上传、重试、文件进度和银行映射接续；不改变原草稿格式、上传协议或单文件隔离；保持 boundedSetData 只包装一次。（R3、R4、R6）
- [ ] 3.4 验证切后台/恢复、卸载/重开、退出登录/换用户、批次/版本变化、上传/命令迟到响应以及写成功后读取失败；按缺失覆盖补测试，不重建服务框架。（R4、R6）

出口：生命周期单一协调，读/写权威未复制，旧页面不回填新页面，离开页面不丢可恢复命令事实；真实用户隔离不放松。若发现基线 bug，先单列，影响阶段安全时不得宣称通过。

## P4：逐流程拆分导入工作台

依赖 P3。所有权：account-review.js、transaction-review.js、posting-flow.js、必要的 model/presentation 迁移及原页面对应方法。

- [ ] 4.1 迁移账户核对、目录选择和对应 UI 草稿；验证批量范围、账户 ID 稳定、映射修订和跨页选择。（R1、R4）
- [ ] 4.2 迁移交易核对、历史候选、表单与同层原文；验证编辑保护、翻页/关闭/切项后的迟到响应、原始空值/零/重复列及单项重试。（R1、R4）
- [ ] 4.3 迁移最终汇总、入账展示和明细刷新；仍调用原 draft-session 的提交/恢复，不复制 postFlight。验证成功事实先保留，读取失败不再生成入账。（R2～R4）
- [ ] 4.4 按实际消费者迁移纯模型函数；每移一组即删除对应旧覆盖，最后删除 enhance。检查重复生命周期/事件和缺失 WXML 处理者，不靠 spread/Object.assign 顺序消解冲突。（R5）
- [ ] 4.5 复用加载真实 Page 入口的测试；检查 setData 回调、页面实例隔离、订阅重复和过期 finally。完成合成全流程及 24990 条分页/缓存预算验证。（R4、R6）
- [ ] 4.6 在已核对工程路径的微信开发者工具执行原生编译与合成交互；保留/恢复用户原会话，不用真实账务数据写入验证。工具不可用则明确待验收，不计通过。（R6）

出口：WXML 事件和状态变更都有明确归属；旧覆盖移除，布局、导航、原文直接展示与已有请求/缓存预算保留。

## P5：边界审计、集成和候选交付

依赖 P4。所有权：边界检查、验证、文档和任务分支交付；来源规则默认不改。

- [ ] 5.1 记录银行身份冲突与资金方向分支差异，仅审计并列后续项。除非确实阻碍当前目标边界且能独立证明等价，不执行来源规则提取；涉及语义/字段/版本的修改退出本轮。（R1、R3、R5）
- [ ] 5.2 检查新依赖方向、读写调用、页面装配和两云函数独立部署/共享规则一致性；无新增框架、双实现、内部兼容层或未使用抽象。（R3、R5）
- [ ] 5.3 对最终候选运行工程检查、完整单元和隔离 MySQL 门禁，记录精确 SHA 的 CI；比较业务关系图、实际 SQL/请求/字节/派生/setData 与硬预算。（R1～R6）
- [ ] 5.4 更新入账逻辑说明、架构、受影响现行说明与实施规划；记录每段回退入口和未完成的原生/真机验证，不把源码和手机版本状态混写。（R6）
- [ ] 5.5 分批推送同一任务分支；核对远端 SHA、差异与本机实际工程路径，交付可集成候选。没有合并授权不更改 main/用户正在使用的主线目录；集成主目录、部署和发布另按授权。（R6）
- [ ] 5.6 安全清理仅属于本任务的临时验证资源；未集成/未提交的 worktree 不删除。后续提交依赖前项时，正常 revert 按逆依赖次序进行，不清库、不删草稿、不强推。（R6）

出口：范围内职责分离、行为等价及资源不退步有证据；文档与代码一致。来源规则未抽取不构成未完成。工具/验证有缺口则交付候选并明确待验收，不虚报“已完成”。

## 每个小提交的最小记录

在现有任务记录/提交说明中记录：阶段、基线与结果 SHA、移动职责和消费者、行为测试及通过/失败/跳过、业务图/资源差异、剩余风险、回退依赖。金融异常和恢复分支不能只用字符串断言；既有通过且未受影响的用例不机械重复。所有测试使用合成数据或隔离数据库，真实账单、绑定参数和凭据不进入仓库或日志。
