# MINI-1915 分阶段任务

实施状态：**未开始**。方案待 6 sol 评审；当前没有领取实施文件或进行结构改动。需求编号见 [验收要求](requirements.md)，模块边界见 [设计](design.md)。

评审给出建议不等于开始开发。用户确认实施后，负责人按下面依赖和门禁连续推进，在已授权范围内不重复请求审批。

## P0：冻结可验证基线

- [ ] 0.1 认领当前主线 SHA、实施负责人和命名分支，核对同时进行的工作与文件所有权。（R6）
- [ ] 0.2 记录核对总服务的真实消费者、公开导出，以及 `index/paged` 覆盖、生命周期调用和共享状态所有者。（R5）
- [ ] 0.3 将现有测试映射到整理、账户、核对、历史比对、旧草稿、正式入账及页面恢复分支；只补缺失的行为验证。（R1～R4）
- [ ] 0.4 固定相同合成数据的输出、查询次数、响应体和页面缓存预算；区分基线缺陷与新回归。（R6）

出口：能指出每个拟移动职责的原位置、消费者、状态所有者和对应验收；已知基线失败已解决或明确单列，不带着不明失败重构。

## P1：先解除后端公共能力倒置

依赖 P0。所有权：`review-issue-service.js`、新增 `review/policy.js`、`event-store.js`、`issue-store.js`、`reconciliation.js`，及 `finance-update-core/read/maintenance`、`semantic-plan-upgrade` 的必要导入变更。

- [ ] 1.1 提取字段掩码、字段应用和决定校验，保持纯规则的输入/输出。（R1、R5）
- [ ] 1.2 提取事件/问题的真实共享存取，保留 SQL、uid 过滤、引用校验、批量行为和锁位置。（R2）
- [ ] 1.3 提取投影刷新、失效历史关系恢复、历史同步与计数收尾；不遗漏旧映射、草稿可达性。（R1～R3）
- [ ] 1.4 将 core/read/maintenance/旧计划升级改为依赖正确公共模块，检查无新循环、无新模块反向依赖旧服务。（R5）
- [ ] 1.5 运行受影响语义/历史/旧计划/维护和数据库回归，更新入口文档。（R6）

出口：上述消费者不再为公共能力依赖核对总入口；原对外命令、事务和数据结构不变。必要兼容导出有明确消费者清单。

## P2：拆分事务内核对场景

依赖 P1。所有权：核对工厂、新增 `review/account-mapping.js`、`event-decisions.js`、`repayment.js` 及直接相关回归。

- [ ] 2.1 先迁移账户批量确认、映射修订，保持同一事务和统一收尾。（R1、R2）
- [ ] 2.2 迁移普通字段、同笔/不同笔、排除、退款与历史关联分支；分支与决定对应关系可直接查阅。（R1、R5）
- [ ] 2.3 迁移明确还款操作，复用已有还款规则；连同历史核对、分期费用复用验证。（R1、R2）
- [ ] 2.4 检查工厂保留原事务、锁与回执入口，子模块不自行提交；运行并发、回滚、隔离及重放验证。（R2、R3）
- [ ] 2.5 删除已无消费者的兼容导出，补充静态依赖方向检查并更新逻辑说明。（R5、R6）

出口：核对入口可以直接读出场景与事务关系；正式入账实现未重排，已确认和未确认历史候选均按原规则处理。

## P3：明确页面生命周期与请求恢复

依赖 P2。所有权：`import-workbench/index.js`、`paged.js`，新增 `runtime.js`、`upload-flow.js`；共享 services 原则上只调用。

- [ ] 3.1 依据 P0 清单显式装配生命周期、请求适配、订阅及资源清理，保留方法的 `this` 和执行顺序。（R4、R5）
- [ ] 3.2 迁移上传、重试、解析及银行映射接续；原草稿请求和持久化格式不改。（R3、R4）
- [ ] 3.3 逐项验证切后台、卸载、退出登录、重新打开、版本变化与写入成功后读取失败；保留有界 `setData`。（R4、R6）

出口：生命周期只有一个明确所有者；每项请求可追溯到同一会话与版本，旧页面不能覆盖新页面，恢复不生成新入账请求。

## P4：按用户操作拆分导入工作台

依赖 P3。所有权：新增 `account-review.js`、`transaction-review.js`、`posting-flow.js` 及必要的 model 拆分；复用已有展示/证据模块。

- [ ] 4.1 迁移账户核对与目录选择，验证批量范围、账户 ID 稳定和映射修订。（R1、R4）
- [ ] 4.2 迁移交易核对、历史候选、表单与原文展示，验证输入保护、候选分页及同层原文。（R1、R4）
- [ ] 4.3 迁移最终汇总、入账和回执恢复，明确保存成功与明细刷新状态。（R2～R4）
- [ ] 4.4 随实际消费者移动纯模型函数；移除已替代覆盖，最终删除 `enhance` 并检查重复事件名。（R5）
- [ ] 4.5 在用户实际目录的微信开发者工具完成合成全流程及大批次分页验证；恢复原会话，不写真实财务记录。（R6）

出口：所有 WXML 事件和状态写入都有可定位归属；原页面交互、直接展示原文和请求预算保持。

## P5：边界收口与集成交付

依赖 P4。所有权：来源/语义边界的必要小改动、工程检查及相关文档；不含新增模型、接口或迁移。

- [ ] 5.1 对比银行身份冲突及资金方向真值表；只有原样等价的提取进入本任务，需改变语义/版本的部分登记后续工作。（R1、R3、R5）
- [ ] 5.2 维持两云函数独立打包及共享规则一致性，扩展实际生效的边界检查，不引入未使用抽象。（R5）
- [ ] 5.3 完成工程检查、全套单元与隔离 MySQL 回归，记录精确提交的 CI；验证 UI 原生编译与合成交互。（R1～R6）
- [ ] 5.4 同步逻辑说明、架构设计、相关现行说明和实施规划，清理临时验证资源；不把当前设计写成早已实现。（R6）
- [ ] 5.5 集成回实际 catledger 目录，核对本地/远端 SHA 和工作区状态，给出提交/回退入口及发布、真机待验收边界。（R6）

出口：受影响职责分离且行为等价有证据，文档与代码一致。是否发布另按用户授权执行，不能把推送 GitHub 当成已更新手机。
