# 实施与证据

状态：待验收

- [x] 迁移、权限、导出及明确还款领域契约。
- [x] 手动入账、导入整理/整批入账、后续关联和真实待办查询。
- [x] 小程序入口、确认表单、待办数量及零状态。
- [x] 合成 MySQL 集成验证、原生预览、必要回归。
- [x] 更新需求/业务规则/入账逻辑/现行说明；部署两支云函数、上传开发版并推送手机预览。

2026-09-20 验证：`npm run test:db` 共 848 项通过（root 499、API 104、import 245），零失败、零跳过；覆盖普通信用卡/其他负债转账不产生待办、明确零值与未知、暂缓及立即关联、整批失败回滚、重试、并发关联、跨用户、费用入账/汇总、待办分组分页、原贷款维护和导出恢复。数据库测试按文件串行，避免隔离库间争抢同名迁移锁。`npm run check` 730 文件/135 运行时模块通过；两函数生产依赖审计为零。完整验证包含此前已进入 main 的界面提交，集成基线为 `324a86d2`。

原生验证：确认表单与后续关联页面已预览；合成待办数量从 2 变 0 时入口数量从 1 变 0；已恢复所有内存模拟。重新编译主目录后，可信微信会话只读调用 `loans.list` / `loans.unassigned` 成功、贷款页无错误，脚本/模板错误过滤为空。没有向真实账本写入测试记录。

开发云：`0020_explicit_repayment.sql` 已执行并登记 SHA-256 `2739ffbdbb36c48ef3da298f8ffd4b6be4bc6f74eb7853b7033a31f2dfa912e1`；核对新表列、四个同用户外键与处理方式约束，运行账号仅新增该表 SELECT/INSERT。`catledger-api` / `catledger-import` 分别于 21:10:44 / 21:11:03 更新，均为 Active / Available、Nodejs18.15，VPC、内存、超时、入口、角色、环境变量键与触发器保持原样。

小程序：直接从 `/Users/gaohongxiang/projects/catledger` 上传开发版 `0.4.55` 成功（897836 字节），贷款首页手机预览推送成功（878957 字节）。仓库验证记录见 [main 验证](https://github.com/gaohongxiang/catledger/actions/workflows/verify.yml?query=branch%3Amain)。

真机完整走查和正式审核发布未完成时保持待验收，不报告正式上线。
