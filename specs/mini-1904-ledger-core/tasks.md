# MINI-1904 实施任务

> 状态：已通过最终差异复核并以 `daa43fd6` 集成到 `main`；基础账本、具体日期、统计、六套本机主题、开发云部署、真实微信身份验证和真机预览推送均已完成。

- [x] 1. 建立可重入账本迁移与金额/时间基础能力
  - 新增账户、正式交易、幂等收据表及用户边界内的键和外键
  - 为迁移运行器增加并发锁，并覆盖重复执行和半完成恢复
  - 实现金额字符串、账户名称和本地时间校验
  - _Requirement: R1, R2, R4, R5_

- [x] 2. 实现账户生命周期与余额调整
  - 创建、列出、改名、停用账户
  - 在账户创建和余额校正时原子写入 `balance_adjustment`
  - 验证资产、负债、溢缴、同名并发、版本冲突和跨用户隔离
  - _Requirement: R1, R3, R5_

- [x] 3. 实现手工交易写入、修改、删除与幂等
  - 实现支出、收入和单条双端转账
  - 统一账户锁顺序、分类校验、软删除和乐观版本
  - 实现同键同请求回放、同键异请求冲突及失败回滚
  - _Requirement: R2, R3, R5_

- [x] 4. 实现月份列表、搜索、余额与首页统计
  - 实现本地自然月、具体日期、账户/分类/备注筛选和稳定游标
  - 服务端计算收入、支出、结余、账户余额、人民币净值、每日趋势与分类排行
  - 排除转账、余额调整和软删除交易
  - _Requirement: R1, R4, R5_

- [x] 5. 扩展 `catledger-api` 协议与客户端服务层
  - 增加动作路由、递归身份字段拒绝、稳定公开错误和净化日志
  - 封装小程序云函数调用、金额显示、重复提交保护和冲突处理
  - 保持 `bootstrap` 兼容且不向客户端返回内部身份
  - _Requirement: R1, R2, R3, R4, R5_

- [x] 6. 实现暖橘色原生小程序账本界面
  - 重构首页为净值、本月摘要、近半年收支趋势、账户和最近交易，三个内容模块统一缩小后的标题与“全部”入口
  - 新增明细、记一笔、账本、我的、账户管理和分类管理页面，以及带本地图标和中央无文字大号加号的自定义 `tabBar`；一级导航为“首页 / 明细 / 中央记账 / 账本 / 我的”，账本以统一点击行进入账户和分类，加号打开“导入账单 / 手动记一笔”面板，导入占主位
  - 增加未登录公开浏览与按需登录门槛；登录面板按微信现行能力提供可选头像昵称，留空也可继续；成功后留在当前页继续导入、手记、账户或分类操作，“我的”按选择结果展示且不使用品牌 Logo 充当用户头像
  - “我的”增加只清除本机状态、不删除云端账本的退出登录入口
  - 覆盖创建、编辑、删除、停用、校正、空态、加载和错误态
  - _Requirement: R1, R2, R3, R4_

- [x] 7. 完成自动测试、现行说明与本地验收
  - 运行单元测试、MySQL 集成测试、语法/路径检查和 `git diff --check`
  - 更新需求、业务规则、实施规划和 `docs/现行说明/`
  - 审查敏感日志、真实数据、构建产物和任务范围
  - _Requirement: R1, R2, R3, R4, R5_

- [x] 8. 经过部署门禁后完成开发版云端与真机验证
  - 只读复核目标环境、MySQL、迁移版本、函数环境变量和 VPC
  - 经用户确认后应用迁移并更新云函数，验证最小真实微信身份闭环
  - 微信开发者工具以真实微信身份走通账户、支出、收入、转账、修改、删除、校正、停用、分页、搜索与筛选；真机预览包推送成功，未审核发布前仅称开发版
  - _Requirement: R1, R2, R3, R4, R5_

- [x] 9. 实现可扩展的本机视觉主题系统
  - 建立六套原创主题的纯注册表、语义令牌合同、非法值回退和本机持久化服务
  - 在“我的”增加主题入口与微型账本预览选择页，切换后同步当前页面、微信导航栏和自定义底栏
  - 将现有页面的画布、表面、文字、强调、收支、线条、阴影与圆角迁移到语义令牌，不改变任何业务请求或数据
  - 补充注册表单测、编译与六主题视觉走查，同步需求、规则、实施规划和现行说明
  - _Requirement: R6_
