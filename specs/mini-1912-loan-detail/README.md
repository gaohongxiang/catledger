# MINI-1912-LOAN-DETAIL：贷款详情对齐本息小记

2026-09-23 后续纠正：用户否定仅把登记/对账/记录藏入“更多”的方案，要求重梳分期导入、手动记录及逐期进度；新目标见 [MINI-1914](../mini-1914-installment-flow/requirements.md)。本文件保留视觉实现和验证证据，不代表旧账务流程已获认可，也不代表新流程已实现。

状态：待验收。负责人：当前主会话。基线：main@62abdf57；直接在用户主目录修复已有详情页，保留 MINI-1910 / MINI-1911 / MINI-1913 等其他任务修改。

用户指定本地 `loan-cost-calculator` 的贷款详情及提供的截图为视觉基准。本次影响贷款详情布局、只读展示转换和相关验证说明；复用已有 loans.get / periods / previewPlan / payments，不改变数据表、写入接口和账务。

界面规格：用途是直接查看一笔贷款的进度、成本和逐期还款。采用参考图的浅绿账单式卡片：顶部状态、名称、日期和进度，下半区并列近期应还与剩余应还；中部双栏成本卡含利息费用、IRR / 单利年化、总还款与成本占比；下方四列逐期表。参考色 #F1F6F3 / #FBFDFC / #EDF7F2 / #173A30 / #A94B40，字体 PingFang SC 与 DIN Alternate 数字；沿用既有主题适配，青绿主题还原参考配色。左右 28rpx，卡片 30rpx 圆角，贷款名 39rpx，成本 42rpx，表格四列。操作收在表格之后，保留登记、计划对账、编辑和实际记录。

展示契约：历史已还来自 installmentSetup，接入后的已还仅来自实际期次分配；到期不自动已还。历史行由原参数只读试算恢复并注明历史来源，不能伪造流水；后续表格以已保存的期次及修订为准。成本卡按原分期方案计算并明确为预计值，不冒充账本已发生支出；未知项显示待补充。一次性费用只在成本汇总计入一次，IRR 净到账扣除该费用，零付款月份保持期号。读取失败可重试，换会话和页面关闭后的结果不能回填；期次按页展示，既有贷款操作不丢失。

用户后续明确质疑三个账务入口的复杂度：普通页底部改为编辑资料与更多操作，登记借还/还款计划与对账/查看借还记录归入更多，历史面板按需显示。来源关联保留直接继续入口。这次只整合视觉入口，登记付款与分配期次仍是独立流程，不能宣称已经自动对账。

验证完成：loan-detail、loan-schedule-form、ui-all-pages、ui-system、read-cache、cached-page-requests 合计 173 项通过；工程语法/配置/依赖边界检查通过，最终核对 diff 无空白错误。旧 UI 测试只更新原贷款容器对应断言，其他任务原有修改保留。

原生验证：主目录项目 WXML/WXSS 编译通过。合成 36 期结清场景显示原本金、总成本、100% 进度与历史表，翻页从第 1 页 20 行到第 2 页 16 行，返回第 1 期；主卡与参考图已目视核对。最终恢复真实服务后，只读核对现有贷款资料、预计成本与 20 行期次均加载成功，无页面或详情错误；年化说明真实渲染可见并已关闭。390px 窗口中信息卡和成本卡宽度均为 362px。最终底部编辑按钮 1、更多 1、常驻还款按钮 0、默认记录面板 0。测试替身已完全移除，未写真实账务。

CloudBase 复核：沿用原生微信身份与现有只读/写入服务，无新 SDK、数据库、权限、云函数或公共接口改动。展示缓存只接入服务端有版本元数据的接口，periods/previewPlan 沿用原读取协议；新增协议边界回归防止错误套用。当前交付是本地开发状态，未提交推送、未上传小程序，手机走查待用户验收。
