# MINI-1906V2 账单语义识别规则需求

> 状态：精简需求已确认；V2A 与整行语义核心已形成本地候选，等待真实账单盘点和 V2C 差分切换。
>
> 基线：MINI-1906V1 本地修复候选 `3e83a9e2f872efdc5c7b1199f35314a60f69c430`

## 1. 要解决的问题

当前代码可以识别已经写过特例的支付宝、微信记录，但不能证明一份新账单是否被完整理解：

- `parsers/platform.js` 保留了 `rawFields`，却只把已知列投影给后续逻辑，未知列没有进入语义覆盖判断；
- `parsers/normalize.js` 用“包含成功、完成、到账”等宽泛文本提前判断状态和资金效果；
- `source-action.js`、`source-funds.js` 与 `organizer-planner.js` 分别读取不同字段并重复推断语义；
- 同一个 `/`、同一个“收入”或同一个“转账”，在不同动作和状态下可能表示不同含义；
- 测试只能证明已有样本通过，不能发现新列、新枚举和新字段组合没有规则。

本阶段只解决一件事：

> 对账单每行的全部来源字段进行一次完整观测，再由来源 profile 下的组合规则确定资金效果、来源动作、账户端点、经济性质和关系；任何无法唯一确定的记录都明确进入待确认或未知，不得静默入账或消失。

## 2. 目标架构

```text
账单文件
  → Source Adapter
  → Row Semantic Resolver
  → Relation Resolver
  → EconomicEvent Builder
  → 现有 FinanceUpdate → Transaction
```

### 2.1 Source Adapter

每个真实导出模板维护一个版本化 profile：

```text
wechat_csv
wechat_xlsx
alipay_app_csv
alipay_web_csv
未来的 bank_xxx_csv
```

每个 profile 首期只是一个受测试的 JavaScript 配置及配套解析函数，负责：

- 识别模板、表头和数据区；
- 声明必需列、可选列、列别名和允许扩展列；
- 把来源列映射为标准字段，同时保留全部原始字段及位置；
- 解析日期、金额、币种和来源方向；
- 区分 `/`、空单元格、缺少列、解析失败和未知枚举；
- 登记该模板实际允许的交易类型、状态、方向等有限 token。

Adapter 还必须先区分文件中的记录种类：

```text
dataRows         交易数据行
controlFields    行数、汇总、账期或余额控制
metadataRows     用户、账户和导出信息
decorativeRows   说明、空行、分隔线和重复页眉
```

只有 `dataRows` 进入逐行语义解析和行数守恒；其他记录仍须保留来源位置和用途，不能靠遇到空行或分隔线直接截断后续内容。

Source Adapter 不判断最终收入、退款、账户归属或正式交易，也不创建 Account、Category、EconomicEvent 或 Transaction。

用户手动选择“微信”或“支付宝”只能缩小候选，不能绕过缺列、重复列或模板冲突。

### 2.2 RawBillRow

Source Adapter 输出统一 `RawBillRow`：

```text
时间、金额、币种
原始交易类型、方向、状态、支付方式
交易对方、商品、备注
交易号、订单号、商户订单号
全部额外字段及来源位置
```

`RawBillRow` 只统一字段形状，不提前决定最终账务语义。原始值继续保存在现有 Evidence/raw snapshot 中，不额外复制一套敏感数据。

### 2.3 Row Semantic Resolver

每个 profile 拥有一套完整行决策规则。输入是完整 `RawBillRow`，输出简单的 `RowSemantic`：

```js
{
  resolutionStatus, // resolved / unknown / conflict / invalid
  moneyEffect,      // financial / non_financial / failed / closed / pending / unknown
  sourceAction,
  settlement,
  amountMinor,
  currency,
  fromAccountRef,
  toAccountRef,
  identityRefs,
  ruleIds,
  issues
}
```

Resolver 按以下顺序处理：

1. 判断资金效果：资金变化、非资金、失败、关闭、处理中或未知；
2. 判断来源动作：消费、收入、退款、转账、充值、提现、还款、借款、收益、费用或未知；
3. 根据动作解释账户端点；
4. 检查单行关键事实是否完整。

首期不建设通用规则 DSL。规则使用 profile 内的小型决策表或纯函数表达，并至少记录 `ruleId`、读取字段、条件、输出和版本。

同一结论可以获得多条证据；无规则命中产生 `unknown`，多个不同结果同时命中产生 `conflict`。不得依赖代码顺序或最后写入覆盖冲突。这个合并逻辑只是共享纯函数，不是新的数据库领域对象。

来源动作与最终经济性质必须分开：

```text
SourceAction:
PURCHASE / RECEIPT / TRANSFER_SENT / TRANSFER_RECEIVED /
REFUND_CREDIT / TOP_UP / WITHDRAWAL / REPAYMENT / BORROW / FEE / YIELD

EconomicNature:
EXPENSE / INCOME / INTERNAL_TRANSFER / REFUND /
REPAYMENT / BORROW / FEE
```

`SourceAction` 只表达来源记录声称发生的动作；最终 `EconomicNature` 必须等账户端点和关系解析完成后确定。收益沿用现有 `INCOME`，不为同一种账本影响另增枚举；`unknown`、`non_financial`、失败、关闭和处理中是解析或资金状态，不是经济性质，也不得进入资金事件 Builder。

组合支付不得默认取 `&` 前第一个账户。一个资金账户加若干已认证非资金优惠成分时，可以使用该资金账户；多个资金账户且来源提供逐项金额时，必须按金额拆分并守恒；多个资金账户但没有逐项金额时，必须产生阻断 ReviewIssue，不得猜测主账户；用户可按文末“人工补齐组合支付”规则核对详情后作出完整人工决定。

### 2.4 Relation Resolver

Relation Resolver 处理不能由单行确定的关系：

- 退款与原消费；
- 微信、支付宝与银行卡中的同一笔转账；
- 信用卡还款和聚合还款；
- 多来源重复证据；
- 同订单部分退款。

它只读取 `RowSemantic`、稳定来源标识和现有账本候选，不再读取微信、支付宝原始字符串。

来源交易号、订单号等已认证强标识可以自动关联；仅金额、日期、商户或备注相近只能生成 ReviewIssue 候选，不能自动合并。

最终行归宿只有一个生产入口：关系解析完成后，由共享纯函数联合 `RowSemantic`、关系结果和现有用户决定生成。Row Semantic Resolver 不提前写最终归宿，Relation Resolver 和 ReviewIssue 处理器也不得各自维护一套相互覆盖的 `disposition`。

### 2.5 EconomicEvent Builder

Builder 不知道支付宝、微信或具体银行，只处理已经确定的标准 `EconomicNature`：

```text
EXPENSE
INCOME
INTERNAL_TRANSFER
REFUND
REPAYMENT
BORROW
FEE
```

它把已经解析的动作、账户端点和关系转换为现有 EconomicEvent。失败、关闭和非资金记录只保留 Evidence 与归宿，不生成正式资金事件。

posting 继续消费已经保存的 EconomicEvent，不在正式提交时重新解析账单。预览和提交至少使用相同的 profile/rule policy 版本与分析摘要，版本或事件集合变化时要求重新整理。首期复用现有 `plan_version`、事件/证据/关系结果和请求摘要，不新增运行快照表；分析摘要必须覆盖 Evidence 集合、profile 版本、规则版本、语义结果、关系结果和已应用用户决定。

### 2.6 覆盖报告

覆盖报告只保留三个真正需要的判断：

1. 所有行都有归宿：资金事件、非资金、重复证据、需要确认、用户排除或无效来源行；
2. 所有准备入账的事件都具备金额、日期、币种、正确经济性质、必要账户端点和必要关系；
3. 认证 profile 的当前文件没有未知交易类型、未知状态、规则冲突或待确认关键行。

字段清单、枚举清单和实际组合命中情况作为同一份轻量诊断信息输出，不建立独立业务模型。

用户排除一条已经识别的记录不降低语义识别率；用户排除一条未知记录不能把它伪装成“整份账单已完整识别”。

产品只暴露两个不同结论：

```text
statementFullyRecognized   整份交易数据是否都已被理解
selectedEventsReadyToPost  本次选中的事件是否已经满足入账门禁
```

“整份账单已识别”不等于“整份账单已全部入账”。入账成功前不得使用“完整导入”文案；存在已识别但按规则排除的记录时，应同时显示资金记录数、排除数和本次入账数。

## 3. 验收需求

### R1. 模板和字段完整读取

- 当系统读取一个候选账单时，应保留全部表头、全部数据字段和来源位置，不得只保存当前认识的列；
- 当出现未知列、缺少必需列、重复冲突列、错误工作表或关键公式字段时，应明确报告；
- 当结构与唯一 profile 不匹配时，不得仅凭最高相似分继续按认证模板处理。

### R2. 原始枚举完整盘点

- 当有限枚举字段出现新 token 时，应标记 `unknown_token`，不得归入宽泛“其他”；
- `/`、空值、缺列和解析失败不得归为同一种状态；
- 当状态包含“成功”或“完成”但不在该 profile 的已知组合中时，不得自动视为已结算。

### R3. 完整行组合判断

- 当系统判断资金效果和来源动作时，应联合读取 profile、交易类型、方向、状态和金额类别；
- 当支付宝记录严格匹配零金额免押、冻结或解冻生命周期组合时，应归为非资金记录且不再询问账户；任一必要条件变化都不得继续命中；
- 原消费行的退款状态与独立退款到账行必须分开表达，不能生成重复退款；
- 前一阶段已经确定失败、关闭或非资金的记录，不得继续进入账户选择。

### R4. 账户端点

- 当来源账户未识别且没有用户已确认的归属时，页面必须显示“请选择账户归属”，使用橙色待选样式及“待确认”文字标记，与白底已确认项区分；不得默认进入新建账户表单，待选择状态不得提交。只有用户明确选择新建后才要求填写名称和类型。
- 已上传但未入账的同字节文件再次解析时，只有解析指纹与当前版本一致才复用原批次；指纹变化必须保留旧批次并新增当前版本解析批次。已入账文件仍按既有重复入账门禁处理。
- 账户归属默认只展示账户选择、确认状态与轻量“查看 N 笔”入口，不铺开交易摘要打断流程。用户主动查看后展示时间、金额、方向、交易对方、商品、原支付方式和可用的原状态，账户及待整理详情均在每笔摘要下直接展示完整原始字段，不再要求二次点击；待整理详情先呈现确认内容，原文在同一滚动区，保留清晰的主要保存操作；列表问题标题与说明保持单行轻量提示及原有标题底色，突出交易本身。单笔查看面板按内容自然展开，多笔组按时间列出全部成员、显示日期范围并分段滚动展示，查看后返回账户列表；不能把未知账户记录合并成一个默认批量决定。
- 同一文件摘要、物理行、可信身份锚点均相同且金额/币种/方向未变时，规则升级改变资金效果属于来源解释升级；保留旧身份摘要与旧证据，不制造虚假身份冲突。任一条件不同仍按身份冲突门禁处理。
- 当微信转账收入的支付方式为 `/` 或空、状态精确为“已存入零钱”且金额为正时，应把微信零钱作为流入端；`/` 本身不得直接推出账户未知或微信零钱；
- 当记录属于提现、充值、余额宝转入转出或信用卡还款时，应按动作决定支付方式代表流出端还是流入端；
- 当账户引用只有“银行卡”“信用卡”等泛称时，不得自动绑定现实账户；
- 当组合支付只有一个资金账户且其余均为已认证优惠成分时，应使用该资金账户；当存在多个实际资金账户时，只有来源给出完整逐项金额才允许拆分，否则必须阻止自动入账，允许按人工补齐规则确认全部分项；
- 历史账户或忽略决定应绑定稳定来源引用键，并支持已知旧键 alias；规范字符串变化不得让相同来源引用重新询问，概念确实拆分或发生冲突时不得自动继承。

### R5. 经济性质与关系

- 当来源写“收入”“支出”或“转账”时，系统应结合动作、账户端点和关系确定最终 EconomicNature，不得直接照抄来源方向；
- 只有稳定官方标识或经过认证的强关系可以自动合并事件；金额、日期、商户和备注相近只能形成 ReviewIssue 候选；
- 退款、本人转账、还款和借款不得降级成普通收入或支出以绕过缺失关系或账户端点。

### R6. 入账门禁

- 普通支出必须有金额、币种、日期和流出账户；普通收入必须有金额、币种、日期和流入账户；
- 内部转账必须有两个不同账户；信用还款必须有付款资产账户和目标负债账户；退款必须有到账账户以及原支出或明确待关联状态；
- 任何关键字段为 `unknown` 或 `conflict` 时，不得生成 ready 事件；
- 分类完整度可以单独提示，但不得改变 Account / Transaction 的既有权威边界。

### R7. 行守恒和诚实提示

- Source Adapter 应明确区分交易数据行、控制字段、元数据行和说明/装饰行；只有交易数据行参与语义行数守恒；
- 交易数据行数应严格等于各最终记录归宿数量之和，不得静默跳行、把控制行当交易，或把交易行当尾注丢弃；
- 覆盖报告应分别显示已识别、待确认、未知、冲突、用户排除和无效数量；
- 只有全部疑似资金记录均已识别、关键未知和冲突为零、可入账事件事实完整且来源提供的控制项通过时，才可显示“整份账单已完整识别”；
- 只导入用户选中的部分记录时，应显示“已选择 N 条，可入账”或对应阻断原因，不得冒充整账完整；正式提交成功前不得显示“完整导入”。

### R8. 测试与切换

- 每条关键规则必须有一个正例和逐个破坏必要条件的 near-miss 反例；
- 微信 CSV/XLSX、支付宝 App/Web 应分别维护脱敏金标夹具和实际 token 清单；
- 新逻辑应按稳定 Evidence/来源身份比较事件性质、金额、账户端点、关系和可入账性，不比较随机 ID 或展示顺序；
- 新逻辑在四个 profile 的金标、未知检测、冲突检测和真实账单回归通过前，只能影子运行；
- profile 切换后才按该 profile 删除旧字符串判断，不一次性删除全部旧逻辑。

### R9. 兼容现有正式账本

- 本阶段继续使用现有 Evidence、EconomicEvent、ReviewIssue、FinanceUpdate、Transaction 和 Account；
- 不新增 JournalEntry / Posting，不建设第二套正式账本；
- 规则层只生成语义结果和候选事件，无权直接创建正式 Account、Category 或 Transaction；
- 规则版本应记录在现有分析/计划版本中；同一批预览与提交必须使用同一版本，版本变化时要求重新整理；
- 所有读取、映射和报告继续使用服务端认证上下文的 uid 隔离，不接受客户端自报 uid。

## 4. 首轮范围

首轮只迁移四个现有 profile：

- 微信支付 CSV；
- 微信支付 XLSX；
- 支付宝 App CSV；
- 支付宝 Web CSV。

新增使用既有经济语义的机构或模板时，只增加 profile 和来源规则，不修改通用 Relation Resolver、EconomicEvent Builder 或 FinanceUpdate 主链路。只有首次出现贷款拆分、多币种换汇、证券买卖等全新经济语义时，才允许显式扩展通用类型和 Builder；机构名称和机构原始字符串始终不得进入通用核心。

用户自定义列映射、JSON 映射配置、银行通用导入、PDF、OCR、AI、远程规则包、动态脚本和通用账户概念图谱均不属于本期。

## 5. 完成定义

本阶段只有同时满足以下条件才算完成：

1. 四个 profile 都能完整保留字段并报告未知列和 token；
2. 微信、支付宝来源字符串判断从 normalize、source-action、source-funds 和 planner 收口到 profile + Row Semantic Resolver；
3. Relation Resolver 与 EconomicEvent Builder 不读取来源专属原始字符串；
4. 用户真实问题中的微信零钱、小荷包历史忽略、支付宝零金额非资金记录全部通过；
5. 关键规则正例和 near-miss、行数守恒、未知/冲突阻断和新旧结果差分全部通过；
6. 真实微信身份下完成单文件、混合文件、重复上传、返回恢复、账户确认和整批入账验收；
7. 未部署、未迁移开发云、未上传体验版，除非另行获得明确授权。

### 同银行待确认账户推荐

- 缺少尾号但银行和账户类型明确时，仅推荐同银行、同类型的已有账户；唯一候选显示待确认建议，多候选不预选。无候选仍可选择其他账户或新建。不得据此自动建立历史归属规则。
- 同批、同银行、同类型且缺少同一资金端点的其他问题，可展开逐笔查看原始证据并勾选共同应用；默认不勾选其他问题。保存采用已有逐问题版本校验，部分完成或冲突必须明确提示并刷新状态。
- 摘要跳过“/”等占位标题，原始字段保留原值；Excel 日期序号仅在日期/时间字段转换为可读时间，不转换订单号或金额。

### 人工补齐组合支付（2026-09-05 用户确认）

此前“无来源分项金额只能阻断”的规则增加明确人工确认路径：用户从支付详情核对后，逐资金成分选择账户、填写金额，明确本笔为支出还是偿还既有欠款，并填写核对说明。完整决定及原始证据分开保存；单纯选一个账户仍不能解除阻断。任何其他来源冲突、未知状态、身份冲突继续阻断。

第一版仅处理多个已识别资金成分的出款，支持支出或还款到单一负债账户；未知成分、入款、退款、多个还款目标不能借此路径强行通过。金额使用整数分，逐项正数、全成分覆盖、账户不同、总额严格守恒；还款不重复记支出。正式入账将一个事件关联到多条分项交易，保留统一证据和审计；整批撤销覆盖全部分项。涉及拆分原消费的退款以及单笔维护不得暗中只操作其中一条，应明确阻断不受支持的局部操作。


2026-09-05 组合支付预填修正：明确的还款性质及唯一匹配的付款账户应自动填入核对表单，用户可修改；未知/同分歧义/同一账户重复命中保留未选。预填不保存、不解除后台阻断、不改变原始记录；分项金额不推测。准确匹配优先级、性质识别条件及限制见入账逻辑说明 COMBO-02。


组合支付分步修正：账户阶段只确认付款账户，独立保存 paymentAccounts 后剩余问题进入整理；金额、性质、还款目标和核对说明只在整理填写。整理恢复已保存账户，允许修改。账户确认不解除正式入账阻断；细则见入账逻辑说明 COMBO-08。


按账户分组修正：第二步不再设置组合支付独立组。组合记录分别关联每个付款账户组，明确的 1688 先采后付还款目标也进入独立信用账户组；同一事件不复制。旧草稿通过显式、幂等、版本受控的 refreshAccountGroups 原子升级账户引用及问题成员，保留已有完整人工决定。分组、确认、修改、排除及限制的完整口径见入账逻辑说明 COMBO-09（AGROUP-01～10）；替代旧版独立多账户弹层。
