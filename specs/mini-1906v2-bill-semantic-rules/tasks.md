# MINI-1906V2 实施任务

> 状态：待验收。V2A/V2B 与 V2C 本地实现、四模板差分、用户文件复核已完成；开发云已更新至本候选，小程序开发版已上传；真实异常、完整业务流程与真机仍待验收。
>
> 原则：每一步先写失败测试，再做最小生产改动；V2A/V2B 原本仅交付本地候选；2026-09-05 用户已明确授权部署，实际执行证据见实施规划 21.4～21.7。

## 0. 基线与真实材料保护

- [x] 0.1 记录 `main`、MINI-1906V1 和本分支基线；在独立 worktree 开发并保护主工作树现有文件。
- [x] 0.2 复核 MINI-1906V1 的微信零钱、小荷包旧键和支付宝零金额测试，将它们登记为 V2 必须保持的金标。
- [x] 0.3 只通过用户主动选择读取真实文件；仓库只保存脱敏最小夹具、token 编码和组合计数。

## 1. V2A：Source Profile 与记录分类

- [ ] 1.1 已覆盖缺必需列、重复冲突列和结构错误；已补多 Profile 冲突拒绝；多工作表与手选平台完整交互验收仍待补充。（R1）
- [x] 1.2 建立 `profiles/index.js`、微信 CSV/XLSX、支付宝 App/Web 四个 Profile，迁入字段别名、格式和已知 token 域。（R1、R2）
- [x] 1.3 先写 data/control/metadata/decorative 分类与中间空行不截断的失败测试，再让 Adapter 输出四类记录。（R1、R7）
- [x] 1.4 建立 RawBillRow，保留全部字段位置并区分 `/`、空值、缺列、解析失败和未知 token；不重复保存原始敏感值。（R1、R2）
- [x] 1.5 已输出 Profile、未知字段与真实样本组合诊断，并完成四模板合成金标差分。（R8、R9）

## 2. V2B：共享整行语义契约

- [x] 2.1 建立相互独立的 `SourceAction`、`moneyEffect` 与 `resolutionStatus`；最终 `EconomicNature` 仍由后置事件逻辑确定。（R3、R5）
- [x] 2.2 先写零命中、同值多命中、异值冲突和规则顺序变化测试，再实现共享 Row Semantic evaluator。（R2、R3）
- [x] 2.3 Row Semantic Resolver 只输出行语义，不提前写最终归宿或最终 EconomicNature。（R3、R5）

## 3. V2B：微信整行规则迁移

- [ ] 3.1 已为当前已知微信组合建立正例与关键 near-miss；完整真实 token 盘点待用户文件验收。（R2、R3）
- [x] 3.2 已迁移当前代码中的商户消费、二维码收付款、转账、红包、群收款、零钱充值/提现和信用卡还款规则。（R3、R4）
- [x] 3.3 固化“转账 + 收入 + 已存入零钱 + `/`/空 + 正金额”到账微信零钱规则，任一条件变化均不得命中。（R4）
- [x] 3.4 区分原消费退款状态与独立退款到账，不产生重复退款。（R3、R5）
- [ ] 3.5 未登记类型/状态及已测试的不兼容组合进入 unknown/conflict；完整组合域认证仍待真实文件盘点。（R2、R6）

## 4. V2B：支付宝整行规则迁移

- [ ] 4.1 已为当前已知 App/Web token 建立关键正例、near-miss 和未知测试；完整真实 token 盘点待用户文件验收。（R2、R3）
- [x] 4.2 已迁移当前代码中的普通收支、退款、余额充值/提现、余额宝、收益、还款、借款和费用规则。（R3～R5）
- [x] 4.3 固化零金额免押/冻结/解冻生命周期规则，非资金记录不进入账户问题。（R3）
- [x] 4.4 保持小荷包稳定引用键和旧 alias，已确认忽略不因展示规范变化失效。（R4）
- [x] 4.5 先写组合支付失败测试，再实现唯一资金账户加优惠、多资金账户无分项阻断和未知成分阻断；来源提供分项金额后的拆分留待真实格式出现时实现。（R4、R6）

## 5. V2B：关系、归宿与事件生成

- [x] 5.1 让 Relation Resolver 只消费 RowSemantic、稳定来源身份和现有账本候选，禁止读取机构字符串。（R5）
- [x] 5.2 覆盖退款、本人转账、信用还款、聚合还款、重复 Evidence 和同额独立交易；弱关系只生成候选。（R5、R6）
- [x] 5.3 建立唯一 `deriveRowDisposition`，联合行语义、关系和现有用户决定生成七类最终归宿（含未分配错误态）。（R7）
- [x] 5.4 EconomicEvent Builder 由标准行语义计算 EconomicNature，未知保留待确认；Builder/通用 planner 不再读取平台类型和状态字符串。（R5、R9）
- [x] 5.5 复用现有 posting 完整性门禁，验证未知、冲突、缺账户或缺关系均不能生成 ready 事件。（R6、R9）

## 6. 覆盖结果与界面契约

- [x] 6.1 先写 dataRows 行数守恒和用户排除语义测试，再实现轻量 Coverage Report。（R7）
- [x] 6.2 输出 `statementFullyRecognized` 与 `selectedEventsReadyToPost`，同步可选公共契约字段。（R7、R9）
- [x] 6.3 界面分别显示整账识别状态和所选事件可入账状态；不得用排除未知提高完整度。（R7）
- [ ] 6.4 保持现有视觉体系，只验证不同状态、长数字和小屏下的信息层级与可读性。

## 7. 预览、提交与差分

- [x] 7.1 Profile/Adapter/规则已版本化，计划版本为 `organizer-plan-v27`；标准语义、证据、关系、决定和文件诊断进入稳定摘要。（R8、R9）
- [x] 7.2 验证预览后版本或事件集合变化返回冲突并要求重新整理；posting 不重新解释原始账单。（R9）
- [x] 7.3 对四个 Profile 分别双跑新旧结果，按稳定 Evidence 比较动作、金额、账户端点、关系、EconomicNature 和可入账性。（R8）
- [ ] 7.4 人工确认预期差异后逐 Profile 切换；稳定后才删除该 Profile 在 normalize/source-action/source-funds/planner 中的旧分支。（R8）

## 8. 验证与交付

- [x] 8.1 运行根目录全部测试和 JavaScript/JSON 语法检查。
- [x] 8.2 运行 `catledger-api` 全部非数据库与 MySQL 8.4 测试。
- [x] 8.3 运行 `catledger-import` 全部非数据库与 MySQL 8.4 测试。
- [x] 8.4 现有全量测试覆盖重复整理、事务回滚、幂等、并发和跨用户隔离；新增诊断不记录原始字段值。
- [x] 8.5 两支函数生产依赖审计为 0；`git diff --check`、财务正确性与安全 diff 审查通过。
- [ ] 8.6 用用户主动选择的真实微信/支付宝文件完成 token/组合盘点和 MINI-1906V 回归；真实账单不得提交或写入普通日志。
- [x] 8.7 清晰报告分支、worktree、提交 SHA、修改文件、测试、未运行项与风险；未完成真实微信身份和平台走查时只写“本地候选通过”。

## 9. 本阶段明确不领取

- [ ] 用户自定义列映射、JSON 映射、银行通用导入、PDF、OCR、AI；
- [ ] DSL、动态插件、远程规则、运行快照和差异审批平台；
- [ ] JournalEntry / Posting、贷款拆分、多币种和证券语义；
- [ ] MINI-1906A1 的正式账本维护修复（独立任务，发布前仍须完成）。

## 10. V2-REVIEW 工程收口

- [x] 10.1 交易记录结构分类、唯一模板与缺列观测回归。
- [x] 10.2 生产规则冲突合并、profile 动作域与组合支付硬门禁。
- [x] 10.3 版本化行语义/文件分析保存、逐行归宿和覆盖守恒。
- [x] 10.4 整理层消费统一语义、关系策略与事件构建边界整理。
- [x] 10.5 服务端拒绝过期计划，更新不变量测试。
- [x] 10.6 统一验证入口与 MySQL 8.4 集成门禁，同步准确完成证据。

本轮本地验收证据见实施规划第 20 章；任务勾选表示代码与对应本地回归落地，不代表已部署、真实文件覆盖或 V2C 完成。


2026-09-05 补充：用户四份文件只读盘点与 V1/V2 双跑完成，560 行中 556 行明确、20 项控制通过 19 项；完整真实平台认证仍不勾选；开发云切换已执行。基础四 Profile 已统一进入 Resolver，旧版本只供 CLI 从 Git 临时提取对比。当前部署、界面与剩余门禁见实施规划 21.4～21.7，21.2 为部署前历史检查点。

2026-09-06 整合补充：当前开发版 0.4.28；支付宝 App 分类角色 policy-v4、保留人工决定的旧计划升级和 payment-resolution-v2 已落地。完整 Node 18 / MySQL 8.4 回归 433 项通过；当前进度、GitHub 交付与剩余验收统一见实施规划 0.3～0.6、19、22.13～22.17。上述 560 行统计为历史样本检查点，不代表当前规则结果；A1 独立任务的实现包含在同一提交候选中。
