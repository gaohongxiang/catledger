# CatLedger：Astra 当前接手入口

更新：2026-09-14。C1 已收尾并完成必要迁移/部署；用户最新决定为本版不加入注销及其删除流程，并询问真实云端和手机验证方式。当前结果和验收办法统一见实施规划第19.8～19.11节，其余未领取的C2/C3工作仍暂停。中央记账已恢复方式选择，记一笔页导入卡片保留。

唯一任务分支：`codex/mini-1906o2-catalog-loading`。
当前固定worktree（开发者工具也应打开此目录）：`/Users/gaohongxiang/projects/catledger-worktrees/mini-1906o2-catalog-loading`。入口功能提交`90d35b0d`，CI run34808656754通过；旧/private/tmp入口工作区已清理，勿重新打开旧路径。
本轮部署源码：`a4cd49972d15d0aa2486887d2021e41162dcb02a`。S/R/A/B/C1 的批次证据见主看板与本目录 C1-ACCEPTANCE.md；历史交接基线不能当当前状态。

## 先读这些

根`AGENTS.md`、实施规划0.1～0.6和第19章、本目录[下一轮实施计划](NEXT-IMPLEMENTATION-PLAN.md)及[最新本机验收](PERF5-ACCEPTANCE.md)。仅在核对性能预算与历史行为时读取旧`PERFORMANCE-CLOSURE-PLAN.md`、`PERFORMANCE-V2-CONTRACT.md`。旧文档的兼容要求被用户最新决定替代，不再照旧执行。

PERF-0～5已有代码候选和本机证据，历史647项通过不是未来提交的验证结果。不要重做小回执、服务端分页、批量SQL，也不要再把983秒/61MB当作当前性能。

## 开始与执行

先运行`git status --short --branch`、`git worktree list`、`git fetch origin --prune`。复用唯一任务分支工作区；已有未提交工作先辨认，不reset、不强行切换、不丢掉用户文件。不得再开第二条远端任务分支。

下次先核对主看板与用户新范围，再登记待做项；不要重做已推送成果或自动继续已暂停的 C2/C3。动态状态与所有权只写实施规划，不在多份规格重复维护看板。

原实施顺序（已完成部分不重复执行，最新停点以主看板为准）：

1. MINI-1906S：删除目录/统计fallback、旧完整视图和回执模式分叉、旧服务能力协商；只保留当前执行路径。保护已有真实数据、在途草稿和幂等键；一次性数据整理不变成永久兼容层。
2. MINI-1906R：复杂账单与并发性能、结果不明恢复、分期本金无依据排除等专项回归，测出问题才局部优化。
3. MINI-1908A/B：独立“我的 → 贷款管理”，完成资料、实际借还/本金利息费用拆分、已有交易关联、更正撤销及最小还款计划。复用现有账本，不等待不可变分录或AI，不新增通用金融引擎。
4. 沿用 MINI-1907-0 的导出与隐私成果，完成当前版本云端/手机验收；注销已移出本版。使用已确认的专用测试微信身份和合成数据，执行边界见主看板 19.9，不自动操作既有真实账本。

**入口最新决定（2026-09-14）：**中央记账恢复“账单导入 / 记一笔”选择，具体范围及验收见主看板19.10。记一笔页导入卡片原样保留，不增加首页双入口，不移动或删除“我的”原导入项，不改其他底栏入口和导入后续导航。贷款仍为独立入口与页面。

## 验证与提交

每批源码、契约、测试和行为文档一起提交并push同一分支。保留已有账务反例与隔离/幂等/原子性；新回归使用真实隔离MySQL，完整验证命令见新计划。贷款资料与未来计划不直接记钱，已导入扣款关联后不再扣一次，本息费未知不得默认全本金。

先完成本地实现和回归，稳定后调用微信开发者工具；手机只补键盘/焦点、触摸、前后台、弱网和真实云延迟。报告实际SHA与CI，区分本机、原生工具、真机、已部署，不把历史成绩挪用。

用户已授权并完成本轮 0013～0017 云迁移、必要表权限和两函数代码部署；未授权合并 main、小程序上传发布或真实账本业务操作。保留main和Web归档，不force push，不创建临时GitHub Actions中转工作流，不用清空真实数据解决无兼容切换。
