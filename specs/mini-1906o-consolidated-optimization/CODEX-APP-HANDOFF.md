# CatLedger：当前接手入口

更新：2026-09-16。用户已授权将当前开发成果合并 main，并清理本地及 GitHub 任务分支。此前截至 `75db69a9` 的 43 个提交已快进集成；当前集成记录与远端任务分支删除随 main 原子推送生效。动态状态及证据统一见实施规划 0.5 与 19.12。

当前主线：`main` / `origin/main`。
当前项目目录（微信开发者工具也应打开此目录）：`/Users/gaohongxiang/projects/catledger`。
原 `codex/mini-1906o2-catalog-loading` 分支和对应 worktree 已收口，不重新打开或复建旧任务路径。主目录原有未提交 `AGENTS.md` 改动保留，不覆盖、不代为提交。

## 先读这些

根 `AGENTS.md`、实施规划 0.1～0.6 与第 19 章，按任务读取本目录 [下一轮实施计划](NEXT-IMPLEMENTATION-PLAN.md) 和既有验收证据。历史规格中的旧分支、授权与执行次序不能覆盖当前看板或用户最新指令。

开始先检查 `git status --short --branch`、`git worktree list`，再 `git fetch origin --prune`。已有未提交工作先辨认，不 reset、不覆盖用户文件。后续持续功能开发按根 AGENTS.md 使用新的短期命名分支与独立 worktree，完成后及时合并清理，不累积长期任务分支。

## 当前成果与剩余范围

- O1～O3、PERF-0～5、S/R、独立贷款资料/实际结算/期次管理、C1 导出均已集成 main；不重复开发已有性能改造。各批测试及性能证据只证明对应提交和环境，不能挪作完整云端或手机验收。
- 中央记账已恢复“账单导入 / 记一笔”选择，记一笔页原导入卡片保留；贷款仍为独立入口与页面。入口验收及 CI 见实施规划 19.10。
- C1 的 0013～0017 云迁移、表权限与两函数部署记录见实施规划 19.8、C1-ACCEPTANCE.md、C1-DEPLOYMENT.json；已记录的部署源码为 `a4cd49972d15d0aa2486887d2021e41162dcb02a`。本次合并没有再次部署。
- 本版不加入账户注销及其删除流程；其余未领取的 C2/C3 工作继续暂停。完整真实云端联调、手机与云容量验收仍待执行，方法见实施规划 19.9。
- 用户确认现有云端数据均为测试数据，并决定暂时维持一套数据库；分环境/分库仅做过调查，没有实施，不自动购买资源或清库。

## 验证与授权边界

源码基线 `75db69a9` 的 [CI run34810242927](https://github.com/gaohongxiang/catledger/actions/runs/34810242927) 已通过；本次合并无代码冲突，主目录静态检查和 diff check 通过。本轮仅更新集成与交接记录，最终 main 提交及其 CI 以 GitHub 当前结果为准。

本次用户授权覆盖合并 main、推送 GitHub、删除已合并任务分支/worktree，并将开发者工具项目切回主目录。保留 main、Web 归档和既有历史标签；不 force push，不创建中转工作流。不把合并视为上传、审核、正式发布、新增云资源或业务数据操作授权。
