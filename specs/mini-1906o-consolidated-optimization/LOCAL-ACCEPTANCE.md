# MINI-1906O 本机验收与性能记录（2026-09-13）

任务分支：`codex/mini-1906o2-catalog-loading`；主线保持 `a4e3fab7`。O1–O4候选已实现，本机验证已执行，状态为待验收。上限批次性能阻止整体验收通过；不合并main，不部署函数，不迁移开发云，不修改真实账本。

## 功能与验证范围

- 本地全量隔离库：617项（根285/API97/import235），0失败、0跳过；包含迁移重入、退款双向约束、身份冲突、跨用户隔离、事务回滚和并发幂等。
- 两支函数按锁文件重新安装依赖，生产依赖审计均为0漏洞；静态515文件/100运行时模块通过。
- Node18/MySQL8.4 CI：`b3af866` run34586160526、`ecc64db8` run34706094263、`9906751c` run34706396747全绿；最终界面修复另有后续CI。
- 微信开发者工具2.02.2607312、skill0.3.8、基础库3.17.3；模拟器iPhone 12/13 (Pro)，platform=devtools，视口390×753。使用任务worktree真实页面、真实输入/点击和内存合成云响应，导入草稿存储也替换为内存。不是已部署云端或真机验收。
- 10个页面的WXML/WXSS共20项原生编译通过；运行时未检出ReferenceError/TypeError/SyntaxError。首次自动化连接未就绪导致超时，重建连接后成功；不把超时操作算通过。

## 模拟器实际操作

| 流程 | 结果 |
|---|---|
| 目录请求悬挂时输入金额 | formReady=true，catalogReady=false，真实输入28.50成功 |
| 目录返回与备注输入 | 金额28.50与合成备注均保留 |
| 保存后返回、连续记账 | 保存立即回首页；再次打开目录就绪，目录累计仍只请求1次 |
| 切换退款 | 普通记账0次原支出读取；切退款后1次，候选就绪 |
| 无账户、新建后返回、目录失败与重试 | 新建入口可点击；返回仍保留19.60；失败时草稿保留，重试后1个账户且无错误 |
| 首页/明细/统计/我的切换 | 均可显示，无页面错误提示 |
| 恢复24990条事件 | 首帧第1步；逻辑层保留全集，page.data.events为0，未调用无效分组刷新 |
| 核对列表翻页 | 40项/页，共625页；第2页从第41条开始；末页30条且最后一条未丢失 |
| 重复与证据展开 | 进入重复tab不请求证据；打开一条后仅1次证据请求 |
| 最终明细 | 总数24990，每页40项，共625页 |

界面复测发现并修复：记账页使用显式hasAccounts控制无账户入口和提交禁用；导入长计数公式允许换行；最终明细切页使滚动位置回到开头。

## 服务端合成账单性能

环境：本机macOS/Node24.14.1，Docker MySQL8.4，127.0.0.1，数据库仅合成数据。5份微信CSV；每份2条头部记录，测试1000与4998条业务记录（CSV解析器最多5000条含头部）。文件下载由内存存储适配器提供；不包含云存储网络、云函数冷启动或手机延迟。

各阶段按序执行；getCold表示本次首个独立get调用，数据库此前已被解析/整理预热，不能称为云端冷启动。getWarm是紧随其后的重复get。SQL次数包括事务控制；SQL毫秒是客户端等待时间。userLockHoldMs从取得用户行锁到commit/rollback返回，代表本机单用户锁持有时间，不是并发锁等待测量。

| 业务行数 | 阶段 | 耗时秒 | SQL次数 | SQL等待秒 | 用户锁持有秒 | 返回MB（十进制） |
|---:|---|---:|---:|---:|---:|---:|
| 5000 | prepareFiles | 0.013 | 10 | 0.012 | 0.000 | 0.001 |
| 5000 | parseFile1 | 0.470 | 41 | 0.241 | 0.000 | 0.001 |
| 5000 | parseFile2 | 0.372 | 41 | 0.155 | 0.000 | 0.001 |
| 5000 | parseFile3 | 0.370 | 41 | 0.166 | 0.000 | 0.001 |
| 5000 | parseFile4 | 0.376 | 41 | 0.176 | 0.000 | 0.001 |
| 5000 | parseFile5 | 0.381 | 41 | 0.166 | 0.000 | 0.001 |
| 5000 | prepareUpdate | 2.534 | 194 | 0.884 | 0.000 | 4.261 |
| 5000 | organize | 0.372 | 24 | 0.326 | 0.000 | 4.261 |
| 5000 | getCold | 0.257 | 18 | 0.217 | 0.000 | 4.261 |
| 5000 | getWarm | 0.246 | 18 | 0.215 | 0.000 | 4.261 |
| 5000 | resolveAccounts | 25.884 | 45034 | 25.191 | 0.000 | 12.181 |
| 5000 | post | 10.931 | 30071 | 10.767 | 10.918 | 12.221 |
| 24990 | prepareFiles | 0.007 | 10 | 0.007 | 0.000 | 0.001 |
| 24990 | parseFile1 | 1.694 | 141 | 0.816 | 0.000 | 0.001 |
| 24990 | parseFile2 | 1.533 | 141 | 0.663 | 0.000 | 0.001 |
| 24990 | parseFile3 | 1.539 | 141 | 0.662 | 0.000 | 0.001 |
| 24990 | parseFile4 | 1.516 | 141 | 0.666 | 0.000 | 0.001 |
| 24990 | parseFile5 | 1.513 | 141 | 0.668 | 0.000 | 0.001 |
| 24990 | prepareUpdate | 61.237 | 794 | 5.658 | 0.000 | 21.292 |
| 24990 | organize | 2.151 | 24 | 1.940 | 0.000 | 21.292 |
| 24990 | getCold | 1.442 | 18 | 1.262 | 0.000 | 21.292 |
| 24990 | getWarm | 1.446 | 18 | 1.255 | 0.000 | 21.292 |
| 24990 | resolveAccounts | 457.981 | 224944 | 448.700 | 0.000 | 60.876 |
| 24990 | post | 983.028 | 150011 | 981.999 | 983.009 | 61.076 |

修复前5000笔入账的历史复用查询，抽样309次共耗时278.2秒；该基线进程在运行5分41秒后停止，连接断开回滚，未记为通过。固定从当前事件证据开始连接后，5000笔posting为10.931秒，上限批次同一查询累计24990次仅5.5秒。上限仍有其他慢点：后续分类问题检查24990次累计约380.2秒，完整视图读取包含大字段分组及问题成员查询。

两组最终新建笔数分别严格等于5000与24990，均通过脚本数量断言。上限整套运行超过25分钟，posting最终为983.028秒；曾准备中止，但发出停止命令时进程已正常结束，所以没有把该上限成功结果误记为回滚。该批事务用户锁约983.009秒、返回约61.08MB，不能视作大批次已达到可用性能。

## 客户端数据量与派生

同一合成基础视图、24990条事件，b3af866初次恢复首步约498ms，setData约54.77MB。活动步骤裁剪后，该基础视图首步数据不足1KB。由真实解析/整理生成的上限视图，在9906751c逻辑层测量：首步62ms、setData2293字节；第3步780ms、setData14969字节；第4步198ms、setData656字节。同一步重复view的setData为0，但真实业务view重算仍存在。第3步完整page.data约20.8KB。

这些数字是Node逻辑层与JSON字节测量，不是微信渲染耗时或可输入P95。真实导入视图5000条的早期客户端采样发生在最终摘要精简前，不能直接拿它与上限样本作同比。同步状态单独变化不再调用eventView/分类卡/核对组/资金与最终摘要的回归持续通过。

## 尚未通过的验收项

- 上限批次性能：账户整理约458秒、入账约983秒、完整响应约61MB；需继续优化查询/问题批量生成及完整响应的传输策略，之后再验证云调用。常见5000笔本机正确入账不能替代该验收。
- 真机：键盘/焦点、前后台、触摸滚动、弱网/断网恢复、真实云调用延迟未验证。模拟器悬挂/失败的是合成请求。
- 云端：catalog.get、statistics趋势终点、O1/O4函数逻辑未部署；实际开发云不能据此视为具备新接口。未上传、审核、发布。

## 复现入口

`scripts/benchmark-import.js --database`只允许本机、名称以_test结尾的隔离库；先用`npm run test:db`建立schema，再设置CATLEDGER_TEST_DB_*运行。不要连接真实账本。`--small`只运行5000条；不带--database只测客户端。`--baseline-ref b3af866`可复测基础视图旧客户端基线。输出只包含计数、耗时、阶段与字节，不含绑定值或原始字段。

功能入口：`test/import-account-choice.test.js`、`test/import-workbench-model.test.js`、`test/cached-page-requests.test.js`及两支函数test目录。
