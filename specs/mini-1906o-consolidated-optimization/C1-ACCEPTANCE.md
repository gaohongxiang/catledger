# C1 数据导出收口证据

日期：2026-09-14。源码/部署提交：`a4cd49972d15d0aa2486887d2021e41162dcb02a`；[CI 通过](https://github.com/gaohongxiang/catledger/actions/runs/34776620060)。唯一分支 `codex/mini-1906o2-catalog-loading`；动态进度以实施规划第 19.8 节为准。用户本次要求先完成 C1，授权必要云迁移和部署，C2/C3 暂停。

## 交付行为

“我的 → 数据与隐私”生成完整 JSONL 和交易 CSV，覆盖 33 张业务表。身份、修订门禁、签名游标、每页 50 行/64 KiB、原文字段和整数分保持、CSV 文本转义、失败/过期/退出/离页清理均按 [数据生命周期说明](../../docs/现行说明/数据生命周期.md) 实现。原记一笔和导入布局、入口、导航没有改变。

本次依次补齐贷款所需 0013～0016 和导出 0017，不新建第二账本，不回填真实贷款或改写既有账务。两函数延续当前运行时与容量。

## 自动回归与性能

- `npm run test:db` 在独立 MySQL 8.4/运行角色下共 694 项（根 352/API 97/import 245），0 失败/跳过；后续扩充原始导入转贷款再更正的恢复夹具，相关 6 项通过。
- `npm run check` 最终源码阶段检查 620 个文件、121 个运行时模块；两函数 `npm run audit:prod` 均 0 漏洞，`git diff --check` 通过；源码 CI Node 18 通过。
- `test/data-export.test.js` 覆盖完整清单与非生成列、跨用户隔离、宽 Unicode JSON 分段、修订/重放/并发写入失效、过期、终止令牌和开启外键的逐表恢复。恢复后余额、统计、贷款与期次结果一致。
- 使用 `benchmark-performance-matrix.js --rows 24990 --runs 1` 及 `benchmark-complex-import.js --rows 24990 --scenario history|upgrade --enforce`，Node 24/256 MiB V8/隔离 MySQL。普通 post 7759 ms、1597 SQL、锁 7744 ms；历史准备 9774 ms/重复入账 4903 ms；升级 12904 ms/post 10494 ms。分别符合原冻结普通和复杂预算。
- 首轮普通场景触发 10 秒用户锁门槛；旧脚本先抛错，没有保留失败 post 数字。本次将观察输出移至预算裁决前，预算不变；失败记录与成功复测一起存于 [C1-METRICS.json](C1-METRICS.json)。每类只有一份成功样本，不能作为 P95 或云端容量保证。

## 云迁移与部署

环境 `cloud1-d6gqxki3s97cc4ee9`，现有 MySQL 实例/schema，未新建数据库产品或修改容量。

1. 先核对 0001～0012 checksum、云函数配置、已有权限和五张业务表计数/聚合指纹。
2. 0013～0017 顺序执行；0017 的列存在性守卫按无会话依赖的先查再 ALTER 等价执行，随后创建导出表，结构成功后登记原文件 checksum。
3. 最终 17 个 checksum 全部匹配，37 张表、81 个外键。仅给现有 `catledger_app` 的 11 个新表增加所需 DML；既有权限裁决器检查 API/import 152 项要求，无缺失。没有新增管理员权限、账号或撤回既有权限。
4. 两次 `manageFunctions(updateFunctionCode)` 使用 `functionRootPath=<worktree>/cloudfunctions` 和各自函数名。两函数均 `Active / Available`，Nodejs18.15、512 MB、API 20 秒/import 60 秒、原 VPC/子网、环境变量键、空触发器保持；当前 API 入口包含导出服务。
5. 迁移后及部署后，五张既有业务表计数与聚合指纹均与操作前相同。

结构、权限检查和脱敏函数配置证据见 [C1-DEPLOYMENT.json](C1-DEPLOYMENT.json)。自动审批拒绝了云端业务接口探测，原因是可能触及真实账本；两次探测均未执行，没有绕行调用。不把函数 Active 状态当成云端业务联调通过。

## 微信开发者工具

原生模拟器 → 本机真实两个 handler → 一次性 MySQL 和分离最小角色，全程合成账本。64 次调用全部成功；所查 TypeError/ReferenceError/SyntaxError/WXML/WXSS 编译错误为 0。

- 数据与隐私按钮实际点击生成完整导出：首轮 656 条，JSONL 910559 B；主动清除文件成功。
- 贷款资料 200 元 → 新建 210 元期次 → 实际还款 110 元（本金 100、利息 10，2 条正式交易）→ 分配到期次后为部分支付，未付 100 元，剩余本金 100 元，计划本金差额 0。
- 再导出 670 条记录，覆盖 33 表清单，贷款、实际付款、期次及分配均存在；JSONL 916399 B/CSV 747 B。结束标记与行数匹配，过期清理成功，剩余导出文件 0。没有向联系人实际分享。
- [导出页](C1-NATIVE-export.jpg)、[贷款详情](C1-NATIVE-loan.jpg)、[期次对账](C1-NATIVE-plan.jpg) 截图已查看，关键按钮、数字和状态可见；没有发现需修复的问题。

验证后恢复模拟器原始调用与缓存实现，关闭验证窗口；回收桥接及其一次性数据库/角色，移除本次测试容器和私有预览配置，保留任务 worktree 与用户原有修改。

云端认证业务联调、物理手机、云容量和小程序上传/发布未完成。C2 注销与 C3 运行时/后续准备仍暂停；本记录不代表整体产品已发布。
