# 招财猫记账本项目规则

本文件维护长期协作、安全与交付约束。理解项目先读 [项目逻辑说明](docs/招财猫记账本入账逻辑说明.md)；现行任务与验收状态只在 [实施规划](docs/招财猫记账本实施规划.md) 维护，阶段历史从 [归档](docs/归档/README.md) 与 [specs 导航](specs/README.md) 查证。

## 事实与文档

- 需求定义用户目标与产品范围，业务规则定义正确性与验收，逻辑主文档按业务顺序说明现有行为，架构定义当前模块/数据/接口边界，实施规划只维护当前任务、状态和交付证据指针。代码、迁移和测试证明实际行为；矛盾时核对实现并修正文档，不能用旧规划覆盖代码事实。
- 跨模块工作先核对任务、依赖、负责人、基线和文件所有权；共享契约由一个负责人协调。公共接口、数据表或身份规则变化先明确契约。现有公开接口不等于已有页面，已有代码不等于已部署或手机已验收。
- 业务变化直接更新逻辑主文档对应步骤的输入、判断、结果和失败路径；产品口径变化同步需求与规则；入口移动同步架构/专题。不要在顶部追加“最新逻辑覆盖下文”，不要在多份文档复制当前状态、版本号和完整测试日志。遵循 [文档维护约定](docs/文档维护.md)。
- 小范围 bug、文案和样式修复复用当前任务和提交记录，不机械新建整套需求/设计/任务/交接模板。历史规格和旧授权不能自动触发新开发或发布。
- 首发界面只维护简体中文；代码标识和外部协议保持英文。不得改变账单原始字段和编码语义。
- AppSecret、密钥、Token、原始账单、完整账户与交易信息、真实敏感夹具不得进入提交、普通日志或聊天。AppID 非密钥；仓库使用测试配置，个人环境配置不提交。

## 工作区与协作

- 小范围 bug 修复、文案和样式微调默认直接在 `/Users/gaohongxiang/projects/catledger` 修改，并在用户已打开的同目录微信开发者工具中验证，不为小修另开 worktree。持续功能或确需隔离的并行开发再使用独立命名分支与 worktree。
- 交付核对用户实际目录与工具窗口路径。用户要求先在分支验收时，不提前合并 main 或强切正在使用的工程；远端提交不能证明本机已同步。并行任务应有独立范围，共享数据模型、迁移、身份和幂等规则串行协调。模型和推理档位沿用当前会话或用户指定。
- 保留已有修改和并行成果；禁止 force push、`git reset --hard` 和未经确认、可能丢失工作的 rebase。本次文档备份 `backup-docs` 不作为开发分支改写，也不纳入默认清理。

## 架构与数据

- 客户端使用微信原生小程序，不引入跨端框架、桌面客户端或移动 Web。常规业务在 `catledger-api` 云函数，账单解析与整理在 `catledger-import`；不引入云托管或常驻服务，除非实测导入负载证明云函数不足。
- 正式业务数据使用 CloudBase MySQL。云存储仅保留待解析或短期追溯的原始文件，入账后按保留策略清理；数据库不存文件二进制或 Base64。
- 用户业务表、查询、唯一约束、存储路径和幂等键使用不可伪造的服务端身份；不信任客户端 `uid`。
- `Transaction`、`Account` 和分类是余额、现金流与统计的权威；原始行、来源身份、整理决定、正式交易和贷款计划保持独立实体及关系。
- 同来源身份持久化，并以数据库唯一约束裁决并发；金额、时间或文本相似不足以自动合并。核心语义冲突保留证据并转人工处理。
- 导入默认整批原子入账；上传、解析、整理和入账可重试且幂等，单文件失败不污染其他文件。
- schema 使用连续、forward-only、可重入的显式 MySQL 迁移，不依赖运行时自动建表。云函数无状态，不依赖实例本地持久化；日志不输出原始账单字段或数据库绑定值。

## 分支与发布

- `origin/main` 是小程序主线；`origin/catledger-web` 与 `web-v2-final` 保留旧 Web 版。不得用 Web 分支或上游重置 `main`。历史节点用标签，不新增长期开发分支。
- 功能分支使用 `codex/<task-id>-<short-name>`。合并前确认范围、必要验证与契约一致；本次文档整理使用单一审阅分支 `codex/docs-20260926-reorganization`，用户验收前不得合并。
- 任务提交进入目标分支且 worktree 洁净后，用 `git worktree remove <路径>` 与 `git branch -d <分支>` 清理，不用 `rm -rf`。有未提交或未合入成果的工作区不能删除；用户备份另按明确授权处理。
- 提交、合并、推送和其他远端写入依据用户授权；已授权的完整阶段在验收、合入 `main` 且工作树洁净后可安全快进推送，无需重复确认。先前阶段授权不自动覆盖用户本次明确要求的审阅限制。
- 发布云函数通过 `manageFunctions(action="updateFunctionCode")` 时，传 `functionRootPath=<worktree>/cloudfunctions` 和 `functionName=<函数目录名>`；根路径必须直接包含各函数目录，不用 `contentPath` 代替。发布后用 `queryFunctions(getFunctionDetail)` 核对 `Active / Available`、运行时、VPC、环境变量键和触发器。
- 小程序可见改动用微信开发者工具预览，发布前真机走查；正式上传、审核、发布未完成时，只能报告已验证的开发状态。纯文档整理不授权云函数部署、数据库迁移或小程序上传。

## 验证与完成

- 验证与受影响行为、风险和验收条件匹配。结构或配置变化检查语法、微信项目路径和函数入口；数据层变化覆盖迁移、约束、事务回滚、用户隔离与幂等；导入变化覆盖重复、身份冲突、同额独立、并发、单文件隔离和隐私。
- 页面变化覆盖真实 Page、用户会话、迟到响应、编辑保护和写入恢复；原生编译、合成交互、手机走查分别报告。未运行、失败或跳过不能计为通过。
- 文档变化运行 `npm run check:docs`，关键步骤对照代码与测试。历史 specs 正文保留当时上下文，旧章节从固定归档阅读；新现行链接必须有效。完整运行命令只维护在 [开发与验证](docs/开发与验证.md)。
- 交付核对当前任务差异和 `git diff --check`；不提交构建产物或敏感数据。精确提交 CI 与本地测试分别记录，工具不可用时说明未验证项。
- 规划状态使用 `未开始`、`进行中`、`待验收`、`已完成`、`阻塞`；主负责人仅在集成、规定验证、必要文档及安全清理完成后标记已完成。用户要求验收的文档本身，在批准前保持待验收。
