# 招财猫记账本项目规则

本文件维护长期协作、安全与交付约束。理解项目先读 [项目逻辑说明](docs/招财猫记账本入账逻辑说明.md)；现行任务与验收状态只在 [实施规划](docs/招财猫记账本实施规划.md) 维护，阶段历史从 [归档](docs/归档/README.md) 与 [specs 导航](specs/README.md) 查证。

## 事实与文档

- 需求定义用户目标与产品范围，业务规则定义正确性与验收，逻辑主文档按业务顺序说明行为，架构定义当前模块/数据/接口边界，实施规划只维护当前任务、状态和交付证据指针。代码、迁移和测试证明实际行为；矛盾时核对实现，不能用旧规划覆盖代码事实。
- 已确定但未实施的业务用明确目标标识和一份实施方案承接，与当前代码差异并列；不能改了文档就说功能已实现。当前贷款改造入口为 [统一实施方案](docs/贷款与分期改造方案.md)。
- 跨模块工作先核对任务、依赖、负责人、基线和文件所有权；共享契约由一个负责人协调。公共接口、数据表或身份规则变化先明确契约。现有公开接口不等于已有页面，已有代码不等于已部署或手机已验收。
- 业务变化原位更新逻辑主文档的输入、判断、结果和失败路径；产品口径同步需求与规则；入口移动同步架构/专题。不要顶部追加“最新逻辑覆盖下文”，不要复制当前状态、版本号和完整测试日志。遵循 [文档维护约定](docs/文档维护.md)。
- 小范围修复复用当前任务和提交记录，不机械新建整套需求/设计/任务/交接模板。历史规格和旧授权不能自动触发新开发或发布。
- 首发界面只维护简体中文；代码标识和外部协议保持英文。不得改变账单原始字段和编码语义。
- AppSecret、密钥、Token、原始账单、完整账户与交易信息、真实敏感夹具不得进入提交、普通日志或聊天。AppID 非密钥；仓库使用测试配置，个人环境配置不提交。

## 工作区与协作

- 小范围 bug、文案和样式修复默认直接在 `/Users/gaohongxiang/projects/catledger` 修改，并在用户已打开的同目录微信开发者工具中验证，不为小修另开 worktree。持续功能或确需隔离的并行开发再使用安全工作区。
- 交付核对用户实际目录与工具窗口路径。用户要求先在分支验收时，不提前合并 main 或强切正在使用的工程；远端提交不证明本机已同步。共享数据模型、迁移、身份和幂等规则串行协调。模型和推理档位沿用当前会话或用户指定。
- 保留已有修改和并行成果；禁止 force push、`git reset --hard` 和未经确认、可能丢失工作的 rebase。backup-docs 是用户临时原文备份，不改写、不默认清理；用户验收并确认无需保留后再删除。

## 架构与数据

- 客户端使用微信原生小程序，不引入跨端框架、桌面客户端或移动 Web。常规业务在 catledger-api，解析与整理在 catledger-import；不引入云托管或常驻服务，除非实测证明云函数不足。
- 正式业务数据使用 CloudBase MySQL。云存储只保留待解析或短期追溯的原始文件；数据库不存文件二进制或 Base64。
- 用户业务表、查询、唯一约束、存储路径和幂等键使用可信服务端身份，不信任客户端 uid。
- Transaction、Account 和分类是余额与收支的权威；来源、决定、交易和贷款计划保留独立关系。逐期已还保存可同时记缺失费用；历史补记须与余额保全成对保存，不再另设用户授权步骤。旧自动方案仍受其原授权约束；不在只读查询/派生函数中生成交易，不伪造银行扣款。
- 同来源身份持久化，由用户范围唯一约束裁决并发；金额、时间或文本相似不足以自动合并。核心冲突保留证据并转人工处理。
- 导入整批原子入账；上传、解析、整理和入账可重试且幂等，单文件失败不污染其他文件。专用费用同步的有界批次不能改变原导入原子性。
- schema 使用连续、forward-only、可重入的显式 MySQL 迁移，不运行时自动建表。云函数无状态；日志不输出原始账单字段或数据库绑定值。

## 分支与发布

- origin/main 是小程序主线；catledger-web 与 web-v2-final 是旧 Web 归档，不用上游重置 main，不因文档清理删除另一产品归档。
- 一般功能分支沿用 codex/<task-id>-<short-name>；**本次文档及贷款改造例外：按用户要求统一在 docs 工作和验收，不再新建同任务分支。** codex/docs-20260926-reorganization 停止维护，确认当前 tip 被 docs 包含且评审已迁移后清理；旧 PR 关闭不等于已合入 main。
- 有未提交或未合入成果的工作区不能删除。安全清理用 git worktree remove 与 git branch -d，不用 rm -rf；备份在用户验收后按明确授权处理。
- 用户转交贷款方案后，Astra 可按范围实现、验证、提交并推送 docs；未获用户验收合并指令，不合并 main。过去任务中的合并/部署授权不自动覆盖本轮。
- 发布云函数通过 manageFunctions(action="updateFunctionCode") 时，传 functionRootPath=<工作区>/cloudfunctions 和 functionName=<函数目录名>，根路径直接包含各函数目录，不用 contentPath 代替。发布后用 queryFunctions(getFunctionDetail) 核对 Active/Available、运行时、VPC、环境变量键和触发器。
- 小程序可见改动用微信开发者工具预览，发布前真机走查。代码/文档分支实施不自动授权云数据库迁移、云函数部署、小程序上传、提审或发布。

## 验证与完成

- 验证与受影响行为及风险匹配。数据层覆盖迁移、约束、事务回滚、用户隔离、并发与幂等；导入覆盖身份冲突、同额独立、单文件隔离及隐私。新的目标行为用明确反例验收，未涉及功能保持等价。
- 页面变化覆盖真实 Page、会话、迟到响应、编辑保护和写入恢复；原生编译、合成交互、手机走查分别报告。失败、未运行、跳过不能计通过。
- 文档运行 npm run check:docs，并对照代码与测试。历史 specs 正文保留当时上下文，旧章节从固定归档阅读；新现行链接有效。完整命令只维护在 [开发与验证](docs/开发与验证.md)。
- 交付核对任务差异和 git diff --check；不提交构建产物或敏感数据。精确提交 CI 与本地测试分别记录，工具不可用说明未验证项。
- 状态使用未开始、进行中、待验收、已完成、阻塞；只有集成、规定验证、必要文档及验收完成后才标已完成。
