# 仓库代理协作规则

本文件只保存整个仓库长期有效、每个开发会话都必须遵守的规则。阶段状态、负责人、冻结契约细节和完成证据只维护在 `docs/个人财务实施规划.md`。

## 1. 开始工作与事实来源

1. 首次进入项目必须完整阅读 `docs/个人财务实施规划.md`；涉及产品口径、会计语义或验收标准时，再读 `docs/个人财务用户需求.md` 和 `docs/个人财务业务规则与验收.md` 相关章节。
2. 检查目标目录是否存在更深层的 `AGENTS.md` 或 `AGENTS.override.md`；更深层规则只覆盖其目录范围。
3. 执行 `git status --short --branch`，确认分支、工作树和已有修改；不得覆盖、重置或顺手提交不属于当前任务的改动。
4. 在规划看板核对任务 ID、依赖、负责人、基线、分支/worktree、文件所有权和验收条件；未登记的跨模块工作先由主负责人拆分。

事实优先级：本文件定义长期规则，需求文档定义产品口径，实施规划定义架构与动态状态，`docs/现行说明/` 按子系统描述当前行为与扩展路径，代码、迁移和自动测试是已落地行为的最终证据。

文档升级路由：用户可感知的行为变化先改需求、再改业务规则；对错标准变化而界面不变只改业务规则；公共接口、数据表或注册方式变化先在实施规划冻结契约；仅新增账单模板或解析细节只改代码并更新对应现行说明页。现行说明与代码冲突时以代码为准并修订文档；现行说明不复制冻结契约（表结构、checksum、索引）正文，只放指针。

## 2. 语言与敏感信息

- 面向人的文档、任务说明、评审结论和必要注释使用中文；约定文件名、代码标识、API、数据库字段、配置键和第三方协议保持英文及现有风格。
- UI 文案必须走现有国际化体系，至少维护 `zh_Hans` 和 `en`，其余语言遵守仓库校验要求。
- 不翻译第三方许可证、协议字段、原始错误码或兼容性文本。
- 原始账单、完整账户标识、完整交易内容和真实敏感夹具不得进入普通日志、聊天输出或提交。

## 3. 主负责人、执行会话与模型

- 每个阶段或集成批次只能有一个主负责人；其负责拆分、依赖排序、契约冻结、分支集成、总体验收和规划状态。
- 执行会话一次只领取一个边界明确的任务，不得扩展相邻模块、合并 `main`、修改全局看板或宣称阶段完成。
- 执行会话交付必须包含：任务 ID、分支/worktree、提交、修改文件、测试、风险、迁移/回滚影响和建议合并顺序；主负责人复核集成后才更新完成状态。
- 更换主负责人前，旧负责人必须把所有活动任务的真实工作区、提交、测试和阻塞写入规划；新负责人核对仓库后接管，不依赖聊天记忆。
- 短时且不修改生产文件的调查、评审和测试分析可用子智能体；持续实现或需要提交的工作必须使用侧边栏可见的独立执行会话、分支和 worktree，避免嵌套。
- 子智能体修改文件前也必须取得明确文件所有权；会产生提交时升级为独立 worktree。同一任务不得同时交给子智能体和执行会话。
- 数据模型、迁移、公共接口、状态机和其他共享契约由主负责人串行冻结；并行结果未经复核、集成和门禁只能视为候选。
- 主负责人对所有执行会话的最终代码质量负责，必须从全局架构最优和长期演进角度审查依赖方向、公共契约、数据一致性、隐私、三库兼容、可维护性与回归风险；局部可用但损害整体架构的交付必须退回或重构，不能仅凭测试通过就集成。
- 所有会话只使用 `gpt-5.6-sol`。主负责人固定 `max`（最高）；其他任务按难度使用 `medium`（只读核对/机械验证/小文档）、`high`（常规实现/测试/审查）或 `max`（共享契约/迁移/并发一致性/高风险集成）。
- 禁止 `xhigh`（极高）、`low`、`ultra` 及其他档位；`xhigh` 的 token 成本不能成为过度创建智能体的理由。分派任务时必须写明模型和推理强度。
- 会话过多时，及时归档不再需要的已完成会话；仅在证据已入规划、提交已集成、验证和资源清理均结束后归档，活动、待验收、待决或仍需取证的会话不得归档。
- 用户当前请求的授权边界始终优先；本文件不自动授权提交、合并、推送、远端删除或其他外部写操作。

## 4. 并行、分支与 worktree

- `origin/main` 是产品权威稳定分支，`upstream/main` 只是受控更新源；不得把产品主线重置为上游。
- 功能分支使用 `codex/<task-id>-<short-name>`，阶段集成分支使用 `codex/pf-phase-<阶段>`；每个并行任务独占一个分支和 worktree。
- 只有依赖方向明确、公共契约已冻结且文件集合不重叠的任务才能并行；共享契约、同一核心文件和阶段集成必须串行或先重新切分。所有权和依赖写入规划。
- 执行会话不得进入或修改其他会话的 worktree；上游依赖合入后，合并或变基策略只由主负责人决定。
- 禁止对共享/已发布分支 force push，禁止 `git reset --hard`、未经确认的 rebase 和其他会丢失工作的操作。
- 合并前确认任务工作树洁净、提交范围单一、测试证据完整且契约一致。
- 仅在提交已进入目标分支且 worktree 洁净后，使用 `git worktree remove <明确路径>` 和 `git branch -d <明确分支>` 清理；不得用 `rm -rf`，不得清理未合并、待取证或他人资源。
- 推送、删除远端分支和其他远端写操作必须获得用户当前明确授权。
- 用户已授权本项目在每个完整阶段验收、合入 `main` 且工作树洁净后同步推送 `origin/main`；阶段内任务分支和临时提交不推送。推送前必须先 fetch 并确认是安全快进，禁止 force push。
- 用户要能刷新正式站点看到刚做完的改动。会改变 8080 页面、接口或解析结果的工作，合入 `main` 且工作树洁净后，必须按既有备份→可追溯镜像→切换流程更新正式容器；用户刷新 `http://localhost:8080` 即可验收。仅测试、文档或运行行为不变的改动不切服，交付时必须明确说刷新看不到以及原因。

## 5. 架构与数据硬约束

- 保持模块化单体和轻量自托管：正常部署只运行一个应用实例并选择 SQLite、MySQL、PostgreSQL 之一；不得无决策引入微服务、第二套正式账本或强制外部基础设施。
- 现有 `Transaction`、`Account`、分类和标签是正式余额与现金流的唯一权威；PF 能力位于 `personalfinance` 命名空间、`pf_` 表和独立前端 feature，核心账本不得反向依赖 PF。
- 原始账单行、正式交易、对账决定和贷款计划是独立实体，通过关系连接；来源账户映射与来源身份分离，修改映射不得改变历史身份。
- 同来源身份必须持久化、版本化并由数据库唯一约束作最终并发裁决；仅金额相同或时间接近不得自动合并。相同身份的金额、币种、方向等核心语义冲突必须保留证据并转人工处理。
- 所有用户业务表和查询都必须带 `uid` 隔离，并同时兼容 SQLite、MySQL、PostgreSQL，不得把正确性建立在单库特性上。
- PF schema 只通过连续、forward-only、可重入的显式迁移演进，不能以 XORM Sync2 作为唯一机制；迁移租约、fencing、取消传播和 MySQL 锁规则按实施规划的冻结契约执行。
- PF 读写必须通过 `personalfinance` repository 的隐私 session/事务入口，即使启用 `log_query` 也不得记录原始字段绑定值。
- 保留现有导入 API；未经迁移方案和用户确认不得破坏旧流程。改变硬约束前先更新需求/架构决策，并说明数据迁移、上游同步和回滚影响。

## 6. 验证与完成

- 禁止过度审计和过度测试。审查与验证必须和改动风险、影响范围、验收条件成比例；形成最小充分证据后立即停止，不得为了“更放心”扩展到无关模块、重复已通过且未受影响的检查，或提前运行只属于阶段收口的全量/三库门禁。
- 设计冻结只核对影响公共契约、数据正确性、安全或回滚的事项；结论稳定后立即实现。无相关代码变化时不得重复运行同一组全仓或三库门禁；全量门禁只在计划的集成验收节点运行，修复后优先补跑受影响检查。
- 按范围先跑相关测试，再跑所需全量门禁：Go 使用 `go test ./...`、`go vet ./...`；前端使用 `npm test`、`npm run build`；`npm run lint` 含 `--fix`，执行后立即检查 diff。
- 数据层的迁移、唯一约束、条件更新和事务回滚最终必须在 SQLite、MySQL、PostgreSQL 验证。
- 导入层至少覆盖重复文件/身份、身份冲突、同额独立交易、跨用户隔离、并发、失败整体回滚和日志隐私。
- 改变某子系统对外行为的任务，必须在同一次集成中更新 `docs/现行说明/` 对应页面，主负责人验收时核对。现行说明单页保持精炼（约两三屏），只写行为、数据流、扩展点与版本规则，不写日期、任务 ID，不贴代码块。
- 交付前执行 `git status --short`、`git diff --check` 和范围匹配的 diff 审查；不得提交构建产物、真实账单或敏感测试数据。
- 工具链或外部服务不可用时，明确列出未运行项、原因和剩余风险，不得把未验证写成通过。
- 规划状态只使用 `未开始`、`进行中`、`待验收`、`已完成`、`阻塞`。执行会话只能报告待验收；只有集成、规定验证、文档更新和安全清理全部完成后，主负责人才能标记已完成。
- 阶段收口必须报告：可用场景、模型/API/页面变化、明确边界、测试证据、未决项、迁移/回滚/隐私风险和清理结果。
- 对用户可见的产品改动，验证通过后不得停在「代码已改、站点未更新」；切服完成前不算做完。
