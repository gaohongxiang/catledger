# 仓库代理协作规则

本文件适用于整个仓库，保存长期有效、每个开发会话都必须遵守的规则。阶段状态、负责人和完成证据只维护在 `docs/个人财务实施规划.md`，不要在本文件复制动态进度。

## 1. 开始工作前

1. 第一次进入本项目的会话必须完整阅读 `docs/个人财务实施规划.md`。
2. 涉及产品口径、会计语义或验收标准时，同时阅读 `docs/个人财务需求.md` 的相关章节。
3. 检查当前目录下是否存在更深层的 `AGENTS.md` 或 `AGENTS.override.md`；更深层规则只覆盖其目录范围。
4. 执行 `git status --short --branch`，确认当前分支、工作树和已有用户修改；不得覆盖、重置或顺手提交不属于当前任务的改动。
5. 在规划文档任务看板中确认任务 ID、依赖、负责人、目标分支、允许修改范围和验收条件。没有登记的跨模块工作先由主负责人拆分。

文档职责：

- 本文件：长期协作和工程强约束；
- 需求文档：产品目标、业务规则和验收口径；
- 实施规划：技术架构、任务状态、依赖和交付证据；
- 代码、迁移和自动测试：已经落地行为的最终证据。

## 2. 中文优先

- 面向人的项目文档、规划、任务说明、评审结论和必要代码注释使用中文。
- 新增内部文档的文件名在不受工具或生态约束时可使用中文；`AGENTS.md`、`README.md`、`LICENSE` 等约定文件保留标准名称。
- Go、TypeScript、API、数据库字段、配置键、命令行参数和第三方协议使用英文标识，并遵循现有项目命名风格。
- 用户界面文字不得把中文硬编码进组件，必须走现有国际化体系；新增功能至少维护 `zh_Hans` 和 `en`，其余语言按仓库校验要求处理。
- 不翻译第三方许可证、协议字段、原始错误码或为了兼容必须保持原样的内容。

## 3. 单一主负责人

- 每个实施阶段或集成批次必须有且只有一个主负责人会话。主负责人拥有任务拆分、依赖排序、接口冻结、分支集成、总体验收和规划状态更新权。
- 其他工作会话是执行会话。每个执行会话只领取一个边界清楚的任务，必须知道任务 ID、基线提交、分支/worktree、可修改目录、依赖和完成条件。
- 执行会话不得自行扩展到相邻模块，不得直接合并 `main`，不得替其他任务修改状态，不得宣称整个阶段完成。
- 执行会话完成后向主负责人提交结构化回报：任务 ID、分支与 worktree、提交、修改文件、测试结果、未决风险、迁移/回滚影响和建议合并顺序。
- 规划文档的全局任务看板默认只由主负责人修改。执行会话报告“待验收”，由主负责人验证并合并后标记“已完成”，避免多个 worktree 同时编辑同一张表。
- 主负责人需要更换时，旧主负责人必须先把所有进行中任务的实际分支、worktree、提交、测试和阻塞更新到规划文档；新主负责人核对工作树后再接管，不能依赖聊天记忆交接。
- 当前用户请求的授权边界始终优先；本文件不会自动授权提交、合并、推送、删除远端分支或其他外部写操作。

协作载体按任务持续时间和文件边界选择：

- 短时、边界明确且不修改生产文件的调查、评审、测试分析，优先由主负责人创建子智能体并行完成；子智能体只提交结构化结论，主负责人负责核验和采纳。
- 持续实现、需要独立提交或可能跨越多个上下文周期的模块，必须使用独立执行会话、独立分支和独立 worktree，使用户可以单独查看、接管和复核。
- 数据模型、迁移、公共接口和状态机等共享契约由主负责人串行冻结；子智能体或执行会话不得各自创建竞争版本。
- 子智能体一旦需要修改文件，也必须先获得明确的文件所有权；会产生提交的任务应升级为独立分支/worktree，不得在主工作区与主负责人并写。
- 同一任务不得同时分派给子智能体和独立执行会话。任何并行结果都只是候选交付，未经主负责人复核、集成和门禁验证不得标记完成。

## 4. 并行开发边界

主负责人应优先并行处理真正独立的工作，但不能为了并行而制造接口返工。

适合并行：

- 已冻结证据解析契约后的支付宝与微信解析器；
- 已冻结 API 和类型契约后的后端实现与前端页面；
- 不修改生产代码的测试夹具、隐私审查和文档核对；
- 修改文件集合互不重叠、依赖方向明确的模块。

必须串行或先冻结契约：

- 数据模型、迁移版本和唯一索引；
- 来源身份算法及其版本编码；
- session 级账本适配器和正式交易不变量；
- 同一个 API 契约、同一状态机或同一公共文件；
- 上游同步与正在进行的阶段集成。

并行前由主负责人记录文件所有权和依赖。发现两个任务需要修改同一核心文件时，应重新切分、先合入公共接口，或改为串行，不允许两个会话各自实现一套再事后拼接。

## 5. 分支与 worktree

- `origin/main` 是产品权威和稳定分支；`upstream/main` 只是受控更新源。禁止把产品主线重置为上游。
- 功能开发使用 `codex/<task-id>-<简短英文说明>`；阶段集成可使用 `codex/pf-phase-<阶段>`。分支名保留英文兼容性，任务说明使用中文。
- 每个并行任务使用独立分支和独立 worktree。一个 worktree 只服务一个任务；执行会话不得进入或修改其他会话的 worktree。
- 阶段分支先冻结共享模型和接口，再从同一已记录基线创建可并行任务分支。依赖任务合入后，下游由主负责人决定合并或变基策略，不得各自改写已共享历史。
- 禁止对共享或已发布分支 force push；禁止使用 `git reset --hard`、未经确认的 rebase 或其他会丢失工作的操作。
- 合并前必须确认任务分支工作树干净、提交范围单一、测试证据完整，并由主负责人复核与其他任务的接口一致性。
- 合并和验收通过后才清理。先确认提交已进入目标分支且 worktree 无未提交内容，再使用 `git worktree remove <明确路径>` 和 `git branch -d <明确分支>`；不得用 `rm -rf` 代替，也不得清理用户或其他任务创建的 worktree。
- 未合并、存在未提交内容或仍需回退取证的分支/worktree 不得删除。远端分支删除和推送仍需当前用户明确授权。

## 6. 架构硬约束

- 保持模块化单体，不为本项目拆分微服务或第二套正式账本。
- 保持轻量自托管属性：正常部署只运行一个应用实例并选择 SQLite、MySQL、PostgreSQL 之一；个人财务扩展不得要求三种数据库同时运行，也不得无架构决策引入常驻消息队列、搜索集群或其他强制外部服务。
- 现有 `Transaction`、`Account`、分类和标签是正式余额与现金流的唯一权威。
- 个人财务能力放在 `personalfinance` 命名空间、`pf_` 表前缀和独立前端 feature；核心账本不得反向依赖个人财务模型。
- 原始账单行、正式交易、对账决定和贷款计划是不同实体，通过关系表连接；不得把来源字段直接堆入 `Transaction`。
- 外部来源账户与账本账户映射分离；修改映射不得改变历史来源身份。
- 同来源精确去重必须持久化、版本化，并由数据库唯一约束作最终并发裁决。金额相同、时间接近不能单独自动合并。
- 相同来源身份的金额、币种或方向等语义不变量冲突时，保留证据并进入人工处理，不得覆盖或自动入账。
- 所有用户业务表和查询必须带 `uid` 隔离；同时兼容 SQLite、MySQL 和 PostgreSQL，不把正确性建立在单库特性上。
- PF schema 从第一版使用显式版本化迁移；XORM Sync2 不能作为长期演进的唯一机制。
- 原始账单、账户标识和完整交易内容不得进入普通日志、聊天输出或未脱敏测试夹具。
- PF 读写必须通过 `personalfinance` repository 的隐私 session/事务入口，强制关闭 XORM SQL 参数日志；即使部署者打开 `log_query` 也不得记录原始字段绑定值。
- 保留现有导入 API，先通过独立命名空间增加可运行垂直切片；未经迁移方案和用户确认不得破坏旧流程。
- PF migration 必须从 v1 连续编号、forward-only、可重入；长迁移拆成小于租约周期的命名步骤，每步前用 claim token 和有效租约完成 fenced 续租，不能把不可中断的长回填塞进一个步骤。
- PF migration 的 DDL、schema 元数据和方言目录查询必须传播取消信号。MySQL 续租及成功/失败收口必须在短事务内先锁定 migration ledger 行、锁后读取数据库时间，再按旧租约与 token 条件更新；不得用语句开始时钟判断锁等待后的租约有效性。

详细理由和模型以实施规划为准；需要改变上述约束时，先更新需求/架构决策并说明数据迁移、上游同步和回滚影响。

## 7. 验证规则

按改动范围运行最小充分验证：

- Go：`go test ./...`、`go vet ./...`，或先运行受影响包再运行全量门禁；
- 前端：`npm test`、`npm run build`；
- 类型与 lint：`npm run lint`，但该命令包含 `--fix`，执行后必须立即检查 diff；
- 数据层：迁移、唯一约束、条件更新和事务回滚最终必须在 SQLite、MySQL、PostgreSQL 验证；
- 导入层：覆盖重复文件、重复来源身份、身份冲突、同额独立交易、跨用户隔离、并发确认、失败整体回滚和日志隐私；
- 交付前：`git status --short`、`git diff --check` 和与任务范围相符的 diff 审查。

若工具链或外部服务不可用，必须说明未运行的检查、原因和剩余风险，不得把“未验证”写成“已通过”。构建产物、真实账单和敏感测试数据不得提交。

## 8. 状态与完成定义

规划任务只使用以下状态：

- `未开始`：尚未分配或依赖未满足；
- `进行中`：已有负责人和独立工作区；
- `待验收`：执行会话已交付，等待主负责人复核、集成或完整门禁；
- `已完成`：已合入目标分支、完成规定验证、更新文档，并清理可安全删除的临时 worktree/本地分支；
- `阻塞`：存在明确外部依赖或需要用户决策，必须记录原因。

“代码写完”“测试在任务分支通过”或“提交数量足够”都不等于已完成。主负责人结束一个阶段时必须报告：可用场景、模型/API/页面变化、明确边界、测试证据、未决人工项、迁移/回滚/隐私风险和清理结果。
