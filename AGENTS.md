# 招财猫记账本项目规则

本文件维护长期约束；任务、契约、阶段状态和完成证据维护在 `docs/招财猫记账本实施规划.md`。

## 事实与协作

- 按任务查规划看板及其需求、业务规则、架构和 `docs/现行说明/` 指针。跨模块工作先核对任务 ID、依赖、负责人、基线和文件所有权；共享契约由一个负责人协调。
- 需求定义产品口径，业务规则定义判断标准，规划维护架构契约与状态；代码、迁移和测试证明实际行为。用户可感知行为变化同步需求与业务规则；公共接口、数据表或身份规则变化先明确契约；现行说明与代码冲突时修订说明。
- 小范围 bug 修复、文案和样式微调默认直接在 `/Users/gaohongxiang/projects/catledger` 修改，并在用户已打开的同目录微信开发者工具中验证，不为小修另开 worktree。持续功能或确需隔离的并行开发再使用独立命名分支与 worktree；交付时同步到用户实际使用的目录并核对工具窗口路径。并行任务应有独立范围，共享数据模型、迁移、身份和幂等规则串行协调。模型和推理档位沿用当前会话或用户指定。
- 首发界面只维护简体中文；代码标识和外部协议保持英文。不得改变账单原始字段和编码语义。
- AppSecret、密钥、Token、原始账单、完整账户与交易信息、真实敏感夹具不得进入提交、普通日志或聊天。AppID 非密钥；仓库使用测试配置，个人环境配置不提交。

## 架构与数据

- 客户端使用微信原生小程序，不引入跨端框架、桌面客户端或移动 Web。常规业务在 `catledger-api` 云函数，账单解析在 `catledger-import`；不引入云托管或常驻服务，除非实测导入负载证明云函数不足。
- 正式业务数据使用 CloudBase MySQL。云存储仅保留待解析或短期追溯的原始文件，入账后按保留策略清理；数据库不存文件二进制或 Base64。
- 用户业务表、查询、唯一约束、存储路径和幂等键使用不可伪造的服务端身份；不信任客户端 `uid`。
- `Transaction`、`Account` 和分类是余额、现金流与统计的权威；原始行、来源身份、整理决定、正式交易和贷款计划保持独立实体及关系。
- 同来源身份持久化，并以数据库唯一约束裁决并发；金额、时间或文本相似不足以自动合并。核心语义冲突保留证据并转人工处理。
- 导入默认整批原子入账；上传、解析、整理和入账可重试且幂等，单文件失败不污染其他文件。
- schema 使用连续、forward-only、可重入的显式 MySQL 迁移，不依赖运行时自动建表。云函数无状态，不依赖实例本地持久化；日志不输出原始账单字段或数据库绑定值。

## 分支与发布

- `origin/main` 是小程序主线；`origin/catledger-web` 与 `web-v2-final` 保留旧 Web 版。不得用 Web 分支或上游重置 `main`。历史节点用标签，不新增长期开发分支。
- 功能分支使用 `codex/<task-id>-<short-name>`。保留已有修改；禁止 force push、`git reset --hard` 和未经确认、可能丢失工作的 rebase。
- 合并前确认范围、必要验证与契约一致。任务提交进入目标分支且 worktree 洁净后，用 `git worktree remove <路径>` 与 `git branch -d <分支>` 清理，不用 `rm -rf`。
- 提交、合并、推送和其他远端写入依据用户授权；已授权的完整阶段在验收、合入 `main` 且工作树洁净后可安全快进推送，无需重复确认。
- 发布云函数通过 `manageFunctions(action="updateFunctionCode")` 时，传 `functionRootPath=<worktree>/cloudfunctions` 和 `functionName=<函数目录名>`；根路径必须直接包含各函数目录，不用 `contentPath` 代替。发布后用 `queryFunctions(getFunctionDetail)` 核对 `Active / Available`、运行时、VPC、环境变量键和触发器。
- 小程序可见改动用微信开发者工具预览，发布前真机走查；正式上传、审核、发布未完成时，只能报告已验证的开发状态。

## 验证与完成

- 验证与受影响行为、风险和任务验收条件匹配。结构或配置变化检查相关语法、微信项目路径和函数入口；数据层变化覆盖迁移、约束、事务回滚、用户隔离与幂等；导入变化覆盖重复、身份冲突、同额独立交易、并发、单文件隔离与日志隐私。
- 业务判断、状态流转、映射、批量、恢复或展示转换改变时，同步 `docs/招财猫记账本入账逻辑说明.md` 的触发、输入优先级、分支、事务、失败重试及代码/测试入口；子系统行为改变同步 `docs/现行说明/`。不把规划写成已实现。
- 交付核对当前任务差异和 `git diff --check`；不提交构建产物或敏感数据。工具不可用时说明未验证项，不写成通过。
- 规划状态使用 `未开始`、`进行中`、`待验收`、`已完成`、`阻塞`；主负责人仅在集成、规定验证、必要文档及安全清理完成后标记已完成。
