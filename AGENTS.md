# 仓库代理协作规则

本文件只保存整个仓库长期有效、每个开发会话都必须遵守的规则。阶段状态、负责人、冻结契约细节和完成证据只维护在 `docs/招财猫记账本实施规划.md`。

## 1. 开始工作与事实来源

1. 首次进入项目只需阅读 `docs/招财猫记账本实施规划.md` 的 0.1～0.6 节和第 19 章；领取任务后再按看板指针读取需求、规则、架构和现行说明的相关章节。
2. 检查目标目录是否存在更深层的 `AGENTS.md` 或 `AGENTS.override.md`；更深层规则只覆盖其目录范围。
3. 执行 `git status --short --branch`，不得覆盖、重置或顺手提交不属于当前任务的改动。
4. 在规划看板核对任务 ID、依赖、负责人、基线、分支/worktree、文件所有权和验收条件；未登记的跨模块工作先由主负责人拆分。

事实优先级：本文件定义长期规则，需求文档定义产品口径，业务规则定义对错标准，实施规划定义架构契约与动态状态，`docs/现行说明/` 描述当前行为，代码、迁移和自动测试是已落地行为的最终证据。

文档升级路由：用户可感知的行为变化先改需求、再改业务规则；公共接口、数据表或身份规则变化先在实施规划冻结契约；账单模板或解析细节更新代码并同步现行说明。现行说明与代码冲突时以代码为准并修订文档。

## 2. 语言与敏感信息

- 面向人的文档、任务说明、评审结论和必要注释使用中文；代码标识、API、数据库字段、配置键和第三方协议保持英文。
- 小程序首发界面只维护简体中文；账单原始字段、文件编码和外部协议不得因界面语言被破坏。
- AppSecret、云环境密钥、Token、原始账单、完整账户标识、完整交易内容和真实敏感夹具不得进入提交、普通日志或聊天输出。
- AppID 不是密钥，但仓库默认使用测试配置；个人环境配置不得提交。

## 3. 主负责人、执行会话与模型

- 每个阶段或集成批次只能有一个主负责人，负责拆分、契约、集成、验收和规划状态。
- 执行会话一次只领取一个边界明确的任务；交付包含任务 ID、分支/worktree、提交、修改文件、测试、风险和回滚影响。
- 持续实现或需要提交的工作使用独立任务分支和 worktree；短时只读调查才可使用子智能体。
- 数据模型、迁移、公共接口、身份与幂等规则由主负责人串行冻结；并行候选未经复核不得视为已集成。
- 默认使用 `gpt-5.6-terra/high`；机械核对可用 `terra/medium`。只有共享契约、迁移、并发一致性、发布或已证明不足的高风险任务才临时升级 `gpt-5.6-sol/max`。禁止 `low`、`xhigh`、`ultra`。
- 用户当前请求的授权边界始终优先；本文件不自动授权提交、合并、推送或远端删除。

## 4. 分支、worktree 与发布

- `origin/main` 是微信小程序产品主线；`origin/catledger-web` 与 `web-v2-final` 保存迁移前的 Web 版。不得把 `main` 重置为 Web 分支或上游。
- 远端不设 `develop`、`mini` 等长期开发分支；除 `main` 与 `catledger-web` 外，历史节点使用标签，任务分支验收合入后清理。
- 功能分支使用 `codex/<task-id>-<short-name>`；每个任务独占分支和 worktree。
- 禁止 force push、`git reset --hard`、未经确认的 rebase 和其他会丢失工作的操作。
- 合并前确认工作树洁净、提交范围单一、测试证据完整且契约一致。
- 仅在提交进入目标分支且 worktree 洁净后，用 `git worktree remove <明确路径>` 和 `git branch -d <明确分支>` 清理；不得用 `rm -rf`。
- 推送及其他远端写操作需要用户当前明确授权。完整阶段验收、合入 `main` 且工作树洁净后，可按用户已有授权安全快进推送 `origin/main`，禁止 force push。
- 小程序用户可见改动优先在微信开发者工具和真机预览验证；未完成正式上传、审核和发布时，只能称为“开发版已验证”。

## 5. 架构与数据硬约束

- 客户端使用微信原生小程序，不引入跨端框架、桌面客户端或移动 Web。
- 后端使用微信云开发：常规业务集中在 `catledger-api` 云函数，账单解析集中在 `catledger-import` 云函数；不引入云托管和常驻服务器，除非导入负载经实测证明云函数无法满足。
- 正式业务数据使用 CloudBase MySQL；云存储只保存待解析或短期追溯所需的原始文件，成功入账后按保留策略清理，不把文件二进制或 Base64 写入数据库。
- 所有用户业务表、查询、唯一约束、对象存储路径和幂等键必须包含不可伪造的服务端用户身份；客户端传入的 `uid` 不可信。
- `Transaction`、`Account` 与分类是正式余额、现金流和统计的唯一权威；原始账单行、来源身份、整理决定、正式交易和贷款计划是独立实体，通过关系连接。
- 同来源身份必须持久化并由数据库唯一约束做最终并发裁决；仅金额相同、时间接近或文本相同不得自动合并。核心语义冲突必须保留证据并转人工处理。
- 账单导入默认整批原子入账；上传、解析、整理和入账可重试且幂等，单文件失败不得污染其他文件。
- schema 只通过连续、forward-only、可重入的显式 MySQL 迁移演进；不得依赖运行时自动建表保证正确性。
- 云函数无状态；不得依赖实例本地文件持久化。日志不得输出原始账单字段或数据库绑定值。

## 6. 验证与完成

- 验证与风险、影响范围和验收条件成比例；形成最小充分证据后停止，不扩展到无关模块。
- 结构和配置变更至少校验 JSON/JavaScript 语法、微信项目路径、云函数入口和 `git diff --check`；可交互页面还需微信开发者工具预览，发布前需真机走查。
- 数据层必须覆盖迁移、唯一约束、事务回滚、跨用户隔离和幂等；导入层还需覆盖重复文件/身份、身份冲突、同额独立交易、并发、单文件失败隔离和日志隐私。
- 改变子系统行为时同步更新 `docs/现行说明/`。现行说明保持精炼，只写行为、数据流、扩展点和版本规则。
- 交付前执行 `git status --short`、`git diff --check` 和范围匹配的 diff 审查；不得提交构建产物、真实账单或敏感数据。
- 工具链或外部服务不可用时，列明未运行项、原因和风险，不得把未验证写成通过。
- 规划状态只使用 `未开始`、`进行中`、`待验收`、`已完成`、`阻塞`。只有集成、规定验证、文档更新和安全清理全部完成后，主负责人才能标记已完成。
